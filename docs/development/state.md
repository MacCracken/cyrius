# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.6.3 — BUILT AND RELEASE-GATE GREEN, not yet tagged.** The repair release for the six issues the 6.6.2 ecosystem sweep filed, plus a refold of all twelve stdlibs. Closes the open issue queue **6 → 0**. `#inline` no longer disarms every following `#derive` (a 10-repo hard block, five of them with no local fix); `continue` binds to its own loop at every nesting level; `CYRIUS_DCE=1` no longer emits a PT_LOAD the code was never patched against; `cyrius distlib` no longer OOM-kills CI runners (20,320 MB → 57 MB); `cyrius.lock` is emitted sorted so a committed lock verifies on another machine; `#derive` composes with `public` and the generated accessors inherit it. **Prior: 6.6.2 — SHIPPED.** |
| **cycc** | x86 **1,251,864 B** — ⚠ **v6.6.3 IN FLIGHT, not yet released** (+4,136 B over 6.6.2's 1,247,728). cycc is BYTE-IDENTICAL after the CI repair (it is built from `main.cyr`, which that repair did not touch). The delta is the `#inline` pass-1/pass-2 repair across `main.cyr` and its six per-target forks — ⚠ **the first cut reached ONE fork of seven and turned native-aarch64 CI red**; each fork carries TWO dispatch regions and both halves are now present in all seven. Pass 1 CONSUMES token 163 (an unconsumed directive there terminates the declaration scan, which is what disarmed every later `#derive`), and `main.cyr`'s own pass-2 dispatch arms `_inline_pending` rather than letting 163 fall into the `_alloc_pending` else. **seed-derive GREEN** — `seed → cybs → cycc` byte-identical, the load-bearing leg for any front-end token change. ⚠ The stdlib refold (12 libs) contributes **zero** bytes: cycc includes none of them, so the fixpoint is structurally blind to that half of the release and the corpus gates are the only evidence. A size match is not evidence a change is inert, only that cycc does not use the path.|
| **Bootstrap / cross-OS** | **v6.6.3: ecb · ach · cass · pi all `SELFHOST_OK` + `LIBTEST_OK` on REAL hardware**, run ONE AT A TIME — fixed /tmp + remote paths clobber under concurrency. ⭐ `pi` is the load-bearing host this release: 6.6.3 changes `src/backend/aarch64/fixup.cyr`'s data-segment vaddr, and compiling the aarch64 fork locally proves it PARSES, not that cycc reproduces itself on ARM. seed **29,024 B** → cybs → cycc byte-identical. A green CI check is NOT this leg. ⭐ **v6.6.3 — the pi leg now ALSO self-hosts `src/main_aarch64_native.cyr`, the fork ARM users actually install.** It had only ever self-hosted `main_aarch64.cyr`, the CROSS source — the same shape as the macOS rot this leg exists to prevent, and the reason the full release gate passed GREEN while `#inline`-before-a-declaration failed to compile on the native ARM fork. Mutation-proven load-bearing: pointing the second compile back at the cross source makes pi's SELFHOST fail. |
| **Gates** | **v6.6.3 — check.sh 242 / 0 GREEN + 157 shell gates; release-gate.sh GREEN.** ⚠ `tracked_paths_portable` is RED until the deletion of `c -l)|XX|` is COMMITTED — it reads the INDEX (what CI checks out), so removing the file from the worktree does not clear it. **Seven** new gates this release, every one mutation-proven in both directions: `deps_lock_sorted` (behavioural — the order IS the artefact, unlike its structural sibling `deps_family_expansion_ordered`), `distlib_leaf_lookup_memory` (peak RSS; mutation takes it 44 MB → 1,614 MB), `derive_with_public` (5 axes — axis 5 is the anti-vacuous half that catches an unconditional `public`), `dce_data_vaddr_frozen` (STRUCTURAL deliberately — no in-tree fixture crosses a 2 MB bucket under DCE, and the reason is written into the gate), plus `inline_before_derive` and `nested_continue_binds` as .tcyr. ⚠ **DERIVE the shell-gate count** — `find tests/gates -name '*.sh' | wc -l`. |
| **Corpus** | **308** `.tcyr` (**70** in `crossos/` — ⚠ **238 (77%) never run on non-x86 hardware**: the cross-OS leg's selector is that directory, so anything outside it is x86-only until CI's full-corpus aarch64 job sees it — the cross-OS selector is that DIRECTORY, the `vr01_` prefix was retired at v6.5.11) · **103** `lib/*.cyr` (`boxed.cyr` restored the boxed tagged-union primitives) · **84** `programs/*.cyr` · api-surface **5,156** · heap map **102** regions (gate-derived; ⚠ a raw `grep -cE '^#   0x' src/main.cyr` says **145** because it also matches 18 `FREED` markers — run `sh tests/gates/memory/heapmap.sh`). |
| **Active minor** | **v6.6.x** — value-form `Result` + language ergonomics. Opened at v6.6.0; **v6.5.x closed at v6.5.73** (there is no `.74` — that number was cut in error and re-cut as 6.6.0). Slot plan in [roadmap.md](roadmap.md): `.2`–`.6` are a reserved **repair window**, then the proposal queue, then the committed ergonomics list. |
| **Shipped this minor** | **.0** the `Result` / `Option` / `Either` **value-form flip** — construction allocates ZERO bytes (was 1600 B per 100 `sock_send`), shipped WITH the ecosystem (8 sibling stdlibs migrated at source, pin-bumped, released and re-vendored); it changed the ARITY of every value, so `payload()` and `tagged_new()` are DELETED; plus top-level `var t, v = f();`, the mixed-return diagnostic, and ⛔ **the v6.5.57 aggregate-copy P0 found at the cut** (live seventeen releases, invisible to the fixpoint because cycc's own source never uses the shape) · **.1** the two filed issues — `f64_exp`/`f64_exp2` **NaN for ±inf** in BOTH implementations (polyfill + emitted x87; the guard must precede the range reduction, which computes `inf - inf`) and AGNOS `clock_now_ns()` reading the **frozen `timer_ticks` clock** (#40 → #95 `rdtsc`) — plus the `cyriusly install` **ETXTBSY** failure (`cp` writes through the directory entry; now temp + atomic rename) and five stdlib folds. ⚠ Closed-minor detail for v6.5.x and earlier lives in `completed-phases.md` + the CHANGELOG; this row carries the CURRENT minor only, and was the v6.5.x list until the 6.6.1 refresh. |
| **In-flight** | **none — 6.6.1 cut.** All three filed issues closed and archived; five stdlib folds (patra 1.14.1, sakshi 2.5.1, sankoch 2.7.14, ganita 1.2.4, niyama 1.0.10), each fixed at source and re-pinned before vendoring. Shell gates **140 → 144**, each mutation-proven. ⭐ **The DCE fix is the one worth remembering: the filing said "PE / `--win` target only" and that bound was an artefact of which platforms the reporter builds, not of the bug.** `main_x86_macho.cyr` includes the same `x86/fixup.cyr`, so Intel-Mac Mach-O was crashing too, unreported — found by running the repro on ach rather than trusting the report's target matrix. One gate axis exists purely to fail a PE-only fix. |
| **Next up** | **Slot `.3` — per-item `private` silently privatises the whole file.** `.2`'s pinned occupant (the `cyrius build` overwrite guard) SHIPPED in v6.6.2 along with a much larger repair set than the slot anticipated. `_TL_VIS` handles token 153 by calling `_PRIV_MARK(FM_FILEID(...))` — a FILE-level flip — so `private fn h()` compiles clean and privatises the entire file, `main` included. Twelve-plus releases live, no diagnostic. Default taken in the roadmap: make the per-item form a hard error pointing at the file-level declaration. |
| **Open queue** | **0 open issues.** All four were fixed in v6.6.2 and each filing UNDERSTATED its own defect: the hisab SIMD one was filed as a 6.5.71 `#derive` regression and is a 21-handler frontend bug reachable via `callptr` since **6.0.70**, with a SILENT wrong-write mode the filing never saw; the chakshu `map_u64` one covered 1 of 5 broken paths and its claim that `map_u64_has` is "accidentally correct" was FALSE; the 79/111 token one called itself diagnostics-only when `callptr(fp, 41);` in statement position was a HARD ERROR; and the samvada libc-name one listed 8 reserved names where the derived intersection is **11**. ⭐ **Read a filing's scope as a report about what the reporter builds, never as the bug's boundary.** |
| **W1 status** | n/a — reactive-window bookkeeping belonged to the v6.5.x minor and is retired with it. v6.6.x reserves `.2`–`.6` as its repair window instead; see [roadmap.md](roadmap.md). |
| **Open decisions for the user** | **None blocking.** The three standing items are recorded in [roadmap.md](roadmap.md) *Open questions* WITH stated defaults, so work starts under them rather than waiting: the self_compile budget (owned by the later perf track — ⚠ note both halves of the old candidate pair, ≤700 ms and ≤1.20 MB, are now exceeded at 731–734 ms / 1,247,608 B, so it is an input to re-decide, not a target that was missed), the growth-tax audit (review with it), and macOS concurrency ordering (default: keep last; pull forward if a consumer appears). |

> Full arc detail, the 12-slot sequence and per-arc length estimates live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
