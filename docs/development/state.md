# Cyrius — Current State

> Refreshed every release. CLAUDE.md is preferences/process/procedures (durable);
> this file is **state** (volatile). Bumped via `version-bump.sh` post-hook.
>
> **Consolidated 2026-06-08 (v6.1.4):** the per-patch session-close log
> (104 entries, ~5,600 lines back to v5.x) + the stale v6.0.4-frozen
> structured sections were pruned — that detail is canonical in
> [`CHANGELOG.md`](../../CHANGELOG.md) (per-patch) and
> [`completed-phases.md`](completed-phases.md) (arc retrospective). This file
> now holds only the **active cycle** + current state.

## Current state

| | |
|---|---|
| **Version** | **6.3.14** (v6.3.x cycle — **Language Refinements**; **AGNOS syscall peer — 8 missing wrappers (LIB-ONLY)**. The agnos kernel implements all 64 syscalls (0–63 contiguous) but `lib/syscalls_x86_64_agnos.cyr` omitted 8 the kernel dispatches. Added new `SysNrAgnosProc` enum + 8 `sys_*` wrappers: `sys_klug`#36 (klug log ring/dmesg), process/exec `sys_execwait`#37 + `sys_spawn_path`#43 + `sys_exec_redirect`#62, framebuffer `sys_fbinfo`#38 + `sys_blit`#39 (4-arg a4=r10), input `sys_kbscan`#42, `sys_sched_yield`#44. Proactive before the AGNOS base stack (kavach/bote/t-ron/thoth/phylax/aegis) ports to `--agnos`. cycc BYTE-IDENTICAL (lib-only); a `--agnos` program calling all 8 emits the right `syscall #N`; api-surface +8 additions; check.sh 109/109; ecb+cass+pi SELFHOST_OK; bench 533 ms. Closes the cyrius half of `issues/2026-06-30-agnos-syscall-peer-incomplete-8-wrappers.md`. NEXT: v6.3.15 = array-locals default-on (16-align array stack slots). See CHANGELOG [6.3.14].) PRIOR: **6.3.13** (v6.3.x cycle — **Language Refinements**; **str_builder concurrency ROOT-CAUSED + per-thread array-local fix (OPT-IN)**. The "str_builder miscompile" framing was WRONG: the real, FUNDAMENTAL cause is **`var arr[N]` LOCALS are allocated at a fixed GLOBAL/BSS address shared by EVERY thread** (scalar locals already stack/per-thread; array locals went through the global var table `dbase+offset`→`movabs`). Any concurrent path using an array-local aliases one global buffer → ~87% cross-thread splice (str_builder, json, fmt buffers, multi-worker-TLS `BAD_SIGNATURE`). Found via ultracode 3-agent workflow — the MINIMAL-REDUCTION agent nailed it varying the byte source (`&array-local`=identical 0x600C18 across all 8 threads vs scalar `lea[rbp-N]`); the "clean replica" wrote a per-thread int, never derefed an array. **FIX (opt-in `CYRIUS_STACK_ARRAYS=1`):** `PARSE_ARRAY` routes array locals to per-thread STACK slots (struct-local multi-slot mechanism: ceil(size/8) slots, fillers + named slot at the deepest offset → `&arr`=`lea[rbp-disp]`=arr[0]); the existing `&arr`/`arr[i]`/store paths already had `FINDLOCAL`→`ELOAD_LOCAL_ADDR` branches → NO per-backend emit change, ONE shared-frontend flip covers x86+aarch64+all 7 forks. `THREAD_STACK_SIZE` 64KB→2MB (large array-locals on thread stacks; heap rejected — bump alloc has no free). **Validated opt-in:** sb_fail 0, per-thread x86+aarch64(qemu), cycc self-hosts (binary SHRANK 1,111,576→1,023,464 as arrays left BSS), seed→cybs→cycc byte-id; default gated-off → flag-off differential byte-id vs 6.3.12. **`array_local_threadsafe.cyr` + `_array_local_threadsafe_gate` (check.sh 108→109): 8-thread, opt-in exit 0 / default corrupts. check.sh 109/109; ecb+cass+pi SELFHOST_OK; bench 547 ms; cycc 1,111,576→1,111,616 B (+40 gated code).** **NOT YET DEFAULT-ON:** the default-on flip hit SSE m128 16-ALIGN regressions (AES-NI/crypto/TLS use `pxor xmm,[arr]` needing 16-aligned arrays; global arrays got it via v5.5.21 totvar pad, stack slots only 8-aligned) + TLS-probe compile → reverted to opt-in. NEXT: v6.3.14 = array-locals default-on (16-align array stack slots) — `issues/2026-06-30-array-locals-stack-default-on-m128-align.md`. See CHANGELOG [6.3.13].) PRIOR: **6.3.12** (v6.3.x cycle — **Language Refinements**; **W^X — `cyrld` emits separate code/data PT_LOAD segments** for every userland ELF. Was ONE `RWE` PT_LOAD (code writable + data executable — textbook W^X violation); now TWO page-aligned segments: text `R E` (ELF hdr + PHs + `.text`, NOT writable) + data `RW ` (`.data`+`.rodata`+`.bss`, NOT executable). On agnos (1.50.6 loader already `PF_X`-aware → ZERO kernel work) this is full segment-level W^X. **DEFAULT ON; `CYRIUS_WX=0` opts out** to single `RWE`. Mach-O/PE/kernel/`.so`/object paths unaffected (`_emit_fmt==0`+kmode 0 guard); x86 + aarch64 both (separate backends). **MECHANISM:** data vaddr rounded up to a fresh **2 MB page** (`_wx_data_vaddr`; agnos huge-page granularity), file offset to the ELF `p_align` (4 KB x86 / 64 KB aarch64) so `p_vaddr ≡ p_offset (mod p_align)` → ≤2 MB *virtual* gap, near-zero file bytes. The 2nd PH shifts `.text` 120→176, so `_entry_base`/`e_entry` move to match. **FIXUP ↔ EMITELF_USER in LOCKSTEP** via shared `_wx_data_vaddr(base+text_foff, cp)`: `dbase` feeds every var/string reloc, so patched code lands on the relocated data segment. **TWO-STEP BOOTSTRAP to flip default-on** (old single-seg cycc can't emit W^X → cc1 transition intermediate, cc2 stable W^X fixpoint; cc2==cc3). seed→cybs→gen2==build/cycc W^X (cybs emits single-seg gen1; gen2 W^X — `_wx_*` compile through cybs). **`wx_probe.cyr` + `_wx_segments_gate` (check.sh 107→108): reads emitted PHs — default e_phnum==2, PH0 flags==5 (R E), PH1==6 (R W), runs; CYRIUS_WX=0→1 PH flags==7 (RWX). Verified x86 + aarch64 + `CYRIUS_TARGET_AGNOS` (readelf -lW); ecb+cass(unaffected, Mach-O/PE)+pi(W^X, real aarch64) SELFHOST_OK; CYRIUS_WX=0 differential byte-identical vs 6.3.11; bench 538 ms; cycc 1,107,280→1,111,576 B (+4,296).** NEXT: v6.3.13 = str_builder concurrent-corruption root-cause (P1). See CHANGELOG [6.3.12].) **Earlier releases (6.3.12 back to v5.x): see CHANGELOG.md** — trimmed from here (state.md keeps current + ~2 priors; the rest is canonical in CHANGELOG). |
