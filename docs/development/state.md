# Cyrius — Current State

> Refreshed every release. CLAUDE.md is preferences/process/procedures (durable);
> this file is **state** (volatile). Bumped via `version-bump.sh` post-hook.
>
> **Consolidated 2026-06-08 (v6.1.4):** the per-patch session-close log
> (104 entries, ~5,600 lines back to v5.x) + the stale v6.0.4-frozen
> structured sections were pruned — that detail is canonical in
> [`CHANGELOG.md`](../../CHANGELOG.md) (per-patch) and
> [`completed-phases.md`](completed-phases.md) (arc retrospective). This file
> now holds only the **active cycle** + current state.

## Current state

| | |
|---|---|
| **Version** | **6.3.5** (v6.3.x cycle — **Language Refinements**; **Phase-0 language substrate: CO-01 forward-call ABI prescan + AR-01 monomorphization substrate (gated proof)** — the two generics prerequisites from the 2026-06-10 deep-dive. **CO-01:** a call lexically BEFORE its callee's def used to auto-register mask 0 (silent drop of `: Str` coercion / SIMD routing / struct-return retptr ABI). New **pass-1 fn-signature prescan** (`_prescan_fn_sig`, all 7 forks, replaces the inline fn-skip) registers every top-level fn + its param masks/count/variadic/return-type BEFORE pass-2 emits calls → forward calls resolve the same ABI as backward calls (structs/globals were already pass-1 order-independent; this extends it to fn sigs). Shared `_classify_param_type`/`_classify_return_type` (logic-preserving refactors, differential byte-identical) keep prescan↔def in lockstep; prescan extracts on a throwaway cursor then resets + replays the verbatim old skip (net cursor unchanged). Covers Str/cstring/Result/Option/Tagged/SIMD param masks + `: Struct` return with explicit `var p: T = mk()`; reaches decls before the first top-level statement (pass-1 stops there = decls-first). GOTCHA: `_prescan_params` must consume `)` or struct-ret never classifies (mask 0 → segfault). `tests/tcyr/forward_call_abi.tcyr` passes new / fails old. Filed `2026-06-28-inferred-struct-local-from-call-segfaults.md` (P2 — `var p = mk()` INFERRED type segfaults even non-forward; pre-existing var-decl/retptr bug, NOT CO-01). **AR-01:** the emit-time replay path (PARSE_FNCALL `GFINL!=0`) was always INTACT; only the body-token capture was off (`_INLINE_OK=0` per backend — x86 SIZE v1.11.3, aarch64 metadata overlap since relocated). New opt-in `_MONOMORPH_OK` (`util.cyr`, default 0; `CYRIUS_MONOMORPH=1` in all 7 drivers) ORs into the capture gates → flag OFF byte-identical codegen, flag ON fires the intact value-replay. `_monomorph_substrate_gate` (check.sh 103→104) compiles `tests/fixtures/monomorph/inline_proof.cyr` ±flag: both exit 42 (byte-correct) + binaries DIFFER (active); confirmed on REAL pi aarch64 (old aarch64 capture corruption gone). Deferred v6.3.7: TYPE substitution (generics) + forward-ref + past the ≤2-param/≤16-token ceiling. **Self-host fixpoint + seed→cybs→cycc byte-identical; check.sh 104/104; tcyr 195/195; every behavior change differential-verified byte-identical (flag off); ecb+cass+pi SELFHOST_OK (real hw) + qemu/wine/cxvm; bench self_compile 509 ms (flat, 500–549 band); cycc 1,077,136→1,079,440 B (+2,304).** See CHANGELOG [6.3.5].) PRIOR: **6.3.4** (v6.3.x cycle — **Language Refinements**; **bare-metal deliverable #7** — kernel-freestanding `lib/tls_native` link + in-kernel handshake smoke. **Completes the three OPEN bare-metal design deliverables #5/#6/#7** (#1–#3 shipped .27/.28; **#4 forbidden-module-check was NEVER built** — filed `2026-06-28-bare-metal-forbidden-module-check-unbuilt.md`, P3, the one remaining bare-metal gap). Premise-check: the freestanding hooks ALREADY existed (transport vtable v6.2.4 + entropy hook v6.2.28 = the documented "freestanding-TLS / bare-metal kernel hook") and `tls_native` ALREADY compiles under the bare-metal kernel target — the gap was the PROOF (every existing handshake e2e drives client↔server over `sys_socketpair`+`sys_fork`+`sys_getrandom`, none the hooks alone; deliverable #4 forbidden-module-check was never built → filed as a finding, out of the #7 row scope). `tests/tcyr/tls_native_freestanding.tcyr` runs a FULL TLS 1.3 client↔server handshake + bidirectional app-data over an in-memory `MAP_SHARED` loopback (transport vtable) + a deterministic entropy hook + a clock hook — **ZERO socket/getrandom/clock syscalls on the TLS path**. Endpoints run in separate address spaces (fork → isolated crypto state — sigil's racing HKDF/AES-NI/SHA-NI bank + lazy table/cpuid-probe scratch is NOT built for two concurrent in-memory endpoints, the rabbit hole that killed the threads approach); the channel is an mmap page the vtable `memcpy`s, not a socket; lock-step SPSC synced with the v6.3.3 #6 mfence/dmb. `tests/fixtures/freestanding_tls/kernel_link.cyr` + `_tls_freestanding_link_gate` prove the native stack (record/keysched/AEAD/x25519/ed25519) links into a freestanding kernel ELF (~1.2 MB, no libssl/PT_INTERP). Filed `tls13-server-get-version-zero` (P3 — the 1.3 server ctx leaves `get_version`=0; the smoke asserts the 1.3-only AEAD suite `0x1302` instead). **Test/fixture/gate-only → cycc CODEGEN byte-identical (only the version string changed @ version-bump, 1,077,136 B); self-host + seed→cybs→cycc derivation trivially hold; check.sh 102→103 (`_tls_freestanding_link_gate`); testsuite tcyr 193→194; handshake passes x86_64 + REAL pi (aarch64), 5/5 no flakiness; bench unchanged; ecb+cass+pi SELFHOST_OK.** *live* in-kernel boot stays AGNOS-consumer-gated. See CHANGELOG [6.3.4].) PRIOR: **6.3.3** (v6.3.x cycle — **Language Refinements**; **bare-metal deliverables #5 + #6**. **#5** `[sections] base` makes the kernel ELF load base settable from `cyrius.cyml` — de-dup'd `_kernel_load_base(S)` accessor (x86 + aarch64 `fixup.cyr`) replacing the literal repeated across `_entry_base` + 3 kernel emitters (the 5-site garble hazard), fed by a `CYRIUS_KERNEL_BASE=0x<hex>` env (`_parse_hex_env` in `runtime.cyr`, cell `0x18FCE8`) whose manifest surface is the `[sections] base` key (cbt appends the env, bare-metal builds only) + a base-aware D6 build-report (`fmt_hex0x`, no drift). **Resolves the kernel-load-base-settable issue** (folded into #5 — same feature). **#6** adds the last missing x86 inline-asm primitives — memory fences `mfence`/`lfence`/`sfence` (`0F AE F0`/`E8`/`F8`); `cli`/`sti`/`hlt`/`cpuid`/port-I/O shipped .27/.28, aarch64 `dmb`/`dsb`/`isb` already complete (premise-check found the roadmap STALE on #6). Every cycc-side change is **inert for non-kernel compilation** → **self-host fixpoint + seed→cybs→cycc byte-identical**; **ELF32 + ELF64 kernels byte-identical pre/post** (`cmp`). check.sh **101→102** (`_sections_base_override_gate`: `CYRIUS_KERNEL_BASE=0x200000` shifts e_entry→`0x2000A8` AND PT_LOAD p_vaddr→`0x200000` in lockstep); `tests/tcyr/inline_asm_fences.tcyr` EXECUTES the fences (unprivileged); **qemu-boot-gate green + an OVERRIDE kernel built at `0x200000` BOOTS under QEMU emitting AGNOS** (fixups followed the base, not just a header check); **pi+ecb+cass SELFHOST_OK** (real hardware); bench self_compile **504 ms** (flat, 500–549 band); **cycc 1,075,616→1,077,136 B** (+1,520: fences + `_kernel_load_base` ×2 + `_parse_hex_env` + fork env reads; cbt growth is CLI-only); cycc-native-aarch64 regenerated **914,960 B** + pi own-fixpoint OK. See CHANGELOG [6.3.3].) **Earlier releases (6.3.2 back to v5.x): see CHANGELOG.md** — trimmed from here 2026-06-28 (state.md keeps current + ~2 priors; the rest is canonical in CHANGELOG). |
