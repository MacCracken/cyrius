# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.5.34 — gate GREEN on all 5 steps.** Fixpoint **1,182,944 B** (+16), **seed → cybs → cycc byte-identical**, `check.sh` **199 / 0**, corpus **282 / 282** (per-file exit codes, not the grep summary), cross-OS on REAL hardware — ecb + ach + cass + pi, each `SELFHOST_OK` + crossos **54/54**. ⚠ **Bench: the suite says `self_compile` 767 ms vs `.33`'s 711, and that comparison is INVALID** — `.33` was taken at load 0.34 and this box would not settle below 1.4. Interleaved best-of-5 A/B of the two binaries under identical current conditions: `.33`=742 ms, `.34`=740 ms, three rounds, **`.34` 2-3 ms FASTER**. No regression, no growth tax to triage. When a bench delta appears on a busy box, A/B before believing either number. ⭐ **Band E landed: `CYRIUS_IR=3` now agrees with the default backend on the WHOLE corpus, 282/282 on exit code AND stdout.** The pinned premise said 7 divergences; the slot-entry re-derivation found **4**, bisecting to **3 root causes**, one per pass. (1) **LASE** eliminated a `LOAD_LOCAL` after a `STORE_LOCAL` of the same slot — sound only at full width, since a sub-i64 store narrows into memory and leaves the untruncated value in rax while the load is the `movzx`/`movsx` that gives the value its type; `var x: i8 = 256` read back 256. The width had been in the node's flags since v6.3.35 and LASE never read it. (2) **`ir_const_fold`** skipped over the `IR_NOP`s it writes while KEEPING its state machine's `st`, so on the fixpoint driver's second pass a `LOAD_IMM` from before a fold paired with the `PUSH` after it: `3 + 1 - 5 + 2` folded to **5**. Needs 4+ terms AND an intermediate crossing zero, which is why 3-term chains always looked fine. (3) **`ESTOC`** (`mov [rcx], rax`, the struct field store) recorded NO IR node while emitting bytes byte-identical to `ESTORE64`, so DCE could not see the store reads rcx, killed the `mov rcx, rax` addressing it, and the store wrote through garbage — **SIGSEGV on any struct with 2+ field stores**. That is the SECOND occurrence of the v6.5.5 `ir3_switch_dce` class. ⚠ A fifth apparent divergence, `stdlib/sakshi_full.tcyr`, is a FALSE POSITIVE — it prints nanosecond timestamps and differs from ITSELF. Plus both open consumer filings (`CYRIUS_PKG_VERSION` from included files; `cyrius port --language=python`) and the **bayan 1.4.2 → 1.5.2** + **patra 1.13.9 → 1.13.10** folds. Prior: **6.5.33 — SHIPPED**. |
| **cycc** | x86 **1,182,944 B** — **+16 B** over `.33`. Three of the four source changes are inside `CYRIUS_IR`-gated passes and one (`ESTOC`) swaps an `IR_ENABLED(S) == 2` test for the `_IR_REC0` that records `IR_STORE64`; in default mode both take the emit path, so the compiler's OUTPUT is unchanged and only its own code moved. The `.30` page boundary is still absorbing growth, which is why the fixpoint stayed the same size through the first three bites and moved only when `lex_pp.cyr` grew. Seed 29,024 B → cybs → cycc byte-identical, re-verified after the `util.cyr`/`lex_pp.cyr` edits. |
| **Bootstrap / cross-OS** | **ecb** · **ach** · **cass** · **pi** — all `SELFHOST_OK` + crossos **54/54** on REAL hardware at **6.5.33**. ⭐ **FULL CORPUS re-measured 2026-08-20 (`CYRIUS_CROSS_OS_FULL=1`): ecb 282/0 · ach 282/0 · pi 282/0 · cass 250/32.** Three of four hosts are at ZERO — the long-carried "23 failures on ecb" was a **v6.5.10** figure never re-derived, and `platform-status.md` had been repeating it verbatim. cass's 32 are labelled PE-incompatible by the runner (`fork`/`socketpair` are POSIX-only) and need triaging into skip-with-a-reason versus real. ⚖️ The `CYRIUS_CROSS_OS_FULL=1` default flip is now a NARROW maintainer call rather than an open-ended one. ⚠ Run ONE host at a time — fixed /tmp + remote paths clobber under concurrency. ⚠ `cross-os-selfhost.sh <host>` with no SECOND argument runs the self-host ONLY and exits 0; the full-corpus flag is only consulted INSIDE the lib-test block, so `CYRIUS_CROSS_OS_FULL=1 … <host> crossos` is the invocation that actually widens it. |
| **Gates** | `check.sh` **199 / 0** at `.34` · **80** shell gate scripts (derived — COUNT, never increment). Three new, all mutation-proven: `ir-opt/ir3_substrate_correctness.sh` (5 axes; axis 5 is the opt-in full-corpus default-vs-IR=3 sweep, `CYRIUS_IR3_FULL=1`, ~8 min — run it at the release gate), `frontend/pkgver_visible_in_includes.sh` (4 axes incl. byte- and line-neutrality), `toolchain/port_language_arms.sh` (5 axes; axis 3 asserts a Python scaffold contains NO Rust vocabulary, with an anti-vacuous companion). ⚠ **Mutation lesson worth keeping**: reverting only the LOAD arm of the LASE guard SURVIVES its gate — the STORE arm is the load-bearing half, because refusing to TRACK a narrow store means no load can match it. The LOAD-arm conjunct is kept for correctness of the stated rule, not because a fixture forces it. Anyone re-mutating must revert both arms or wrongly conclude the fix is untested. The stale 8-name residual list in `ir3_fold_jump_span.sh` is deleted — all eight are closed. |
| **Corpus** | **282** `.tcyr` in 15 buckets (**54** in `crossos/`) · **101** `lib/*.cyr` · **97** `programs/**/*.cyr` · api-surface **5070** (**+152**, every one of them bayan's — the 1.5.0 PDF writer/reader; **zero removals**, so the fold is purely additive) · heap **100** regions / 0 overlaps. |
| **Active minor** | **v6.5.x** — generated-code quality (opened at v6.5.0; **v6.4.x closed at v6.4.86**) |
| **Shipped this minor** | **.0** file-scoped `public`/`private` · **.1** arity-aware overload dispatch + 4 stdlib folds · **.2** the `ir_const_fold` jump-span miscompile (**IR=3 mismatches 35 → 8**), `xrmdir`, yukti 2.3.0's six agnos ABI defects · **.3** diagnostic lines after `include`, `install.sh` ETXTBSY strand, `version-bump.sh` same-version early exit · **.4** `vec_sort_by`/`vec_select_nth` (the agnosai filing), sigil 3.12.2 / yukti 2.3.2 / sandhi 1.9.8 / mabda 4.0.8 folds, the vacuous sign-efi gate, fold-version table rot · **.5** the `CYRIUS_IR=3` switch-dispatch miscompile (filed as LASE, actually **DCE**) — closes `switch_dispatch`, one of the eight residuals .2 recorded, so **7 remain** — + bayan 1.4.0 · **.6** `sys_exit_group` on all 3 syscall peer families + `async_await_readable_ms`; macOS-x86's untranslated 231 → silent SIGSYS; `proj-tcyr`'s 8-bit exit truncation (256 failures scored PASS); `cyrius fuzz` blind to scaffolded harnesses; sandhi 1.9.9 + vani 1.1.3 folds · **.7** hisab's `alloc_reset` allocator-invalidation; the aarch64 `fchownat` private-alias band (≥1000, both real candidates owned); `#@incdir` file-relative includes with the CVE-16 escape closed at byte-0 + relative-only; `sys_chdir` called-but-undefined; `signal_default` + the `x*` family; subfolder callout for test/bench/fuzz; missing-output-dir bare FAIL · **.8** szal's `thread_join` lost-wakeup deadlock (P1); `thread_create_detached` (210 MB VA leak → 0); the `i64::MIN` formatter class across 12 sites incl. 2 in the compiler + sakshi 2.4.8; agnos `#97 chan_op` minted (`#96` fork deliberately not); `coverage`'s four fail-open/truncation defects; the doc/vet/deny/audit fail-open family; `distlib --all/--check`; the cross-OS gate batched + honest; bayan 1.4.1 fold · **.9** agnos `CH_ENDOW` + `sys_spawn_path_env` (both blocking agnos bite 7); the three-state futex mutex (392→48 ns, and W1 item 3's stated prerequisite was wrong); the growable arena + exhaustion policy — an exhausted arena was a SIGSEGV layers away, and adopting arenas made a leak into a crash · **.10** `distlib`'s `.deps` sidecar under-reported (8 of 12 for setu) and thereby DISABLED `cyrius deps`' consumer check — now declaration ∪ include-scan, base bundle only; `alloc_via` was two-thirds call plumbing (15.1 → 11 ns) · **.11** test suite → subfolders (`vr01_` prefix → the `crossos/` **directory**) with three silent-green readers closed first, incl. CI scoring a fake PASS because cycc on EMPTY stdin exits 0 with a runnable binary; `MAP_ANONYMOUS` was Linux's 32 on Darwin (`mmap_anon()` → 0 for every caller); **macOS threading now RUNS the worker** via a serial fallback — the wrong constant had been the accidental safety net for the missing backend, and fixing it turned the no-op into SIGSYS; `dir_list`/`is_dir` per-call 4 KB → stack (`dir_walk` 4443 → 239 B/entry) · **.12** sigil **3.12.4** (written this release — closes 3.12.3's residual by localising the bignum engine scratch; the banking existed for a cyrius quirk WE fixed at 6.3.15 and never propagated) — an RSA verify BYPASS (888/400,000 forged accepted upstream; partial by its own account, PSS still banked); `CYRIUS_STACK_ARRAYS=0` silently re-opened it (our flag revoking their fix) → now warns; `lib sync` dropped package DIRECTORIES (`unicode`) at exit 0 — the un-updated twin of a v6.1.x `cyrius deps` fix — plus fail-loud on any unsatisfied declaration; cx emitted a SELF-RESTARTING .cyx at exit 0 for an undefined call (1026 loops; native gated since v6.3.2, cx never got it); tree walks followed symlinks (**41 passed for a corpus of ONE**) → `is_symlink` + depth cap, symlinked FILES still run; `bench_string` 56-byte stack overflow SIGSEGV since v6.3.15, invisible behind the non-blocking bench step; `*.fcyr`/`*.bcyr` DIRECTORIES compiled as files; `dir_list_into` (0 B listing) · **.13** cx `callptr` (opcode 105 — three defects that had to land together: `ELOAD_FN_ADDR` a return-0 stub, `main_cx.cyr` never setting `_fixup_base`, `ECALLIND` a hard error) + agnos `#98 ptrscan` · **.14** the tail-call frame-escape P0 (`return f(p)` freed the frame `p` pointed into; the v5.8.16 guard was SYNTACTIC and missed every aliased form) + sigil **3.12.6** (the whole PSS workspace localised — PSS verify had the same authentication bypass 3.12.3 fixed for v1.5, and the compiler defect is why three releases could not close it) + `distlib`'s bundle self-check, which compiled to `/dev/null` and had never run · **.15** the macOS-arm64 constant + syscall gap closure (**ecb full corpus 11 → 6**): ten constant VALUES the aarch64-Linux peer leaked onto Darwin — ESYSXLAT renumbers NUMBERS, never VALUES, and Linux `SIGCHLD` 17 IS Darwin's `SIGSTOP`, so a child-exit wait silently watched for a stop; `SIGPWR` was 30 = Darwin's `SIGUSR1`; plus the macho ESYSXLAT gap set (`pipe2`/`fchmod`/`faccessat` never mapped) where **Darwin's `pipe` returns both fds in REGISTERS** so a renumber alone was insufficient; and `sys_access` passed 3 args where Darwin validates the 4th — every macOS call returned −22, which had been silently SKIPPING `tls_native_scaffold`'s entire OpenSSL block (a vacuous pass, now honestly red); sakshi 2.4.9 + both fold tables de-rotted. **Reverted at .15**: a `Result` unboxing that passed EVERY gate and still reported a failed file open as SUCCESS in a retaining loop. ⭐ **ach's leg of the release gate then caught TWO of the .15 fixes one fork behind** — `SIGPWR = 30` was still live on the x86-macOS peer (30 IS Darwin's `SIGUSR1`), and `sys_fchmod` **SIGSYS'd on every Intel-Mac** because `EMACHO_SYSXLAT` had no `91 → BSD 124` route, so the number reached Darwin unprefixed. Both fixed at source; `sys_fchmod` had been broken on ach since it existed and only surfaced because .15 added the first crossos assertion that CALLS it · **.16** macOS `uname`/`sysinfo` per-arch routes, sakshi 2.4.10, two issues filed from measurement · **.17** hisab's capturing-closure SIGSEGV, a syntax error in an UNCALLED fn, `distlib` rejecting correct bundles, sankoch 2.7.7 · **.18** `platform/fdlopen` closed (the last full-corpus failure on both Macs; `#naked` had been consumed-and-IGNORED by two forks since v6.2.27), `cyrius fmt` corrupting string literals — it was rewriting **1,239 lines of this repo's own `src/main.cyr`** — sigil 3.12.7 · **.19** five consumer filings (four agnosai, one majra) + **CVE-39** (40 of 103 truncated constructs SIGSEGV'd or HUNG cycc) + **CVE-40** (recursive descent had NO depth bound — PROVEN stack exhaustion, ~16 KB/level; it took FOUR passes to cover the class) · **.20**–**.33** (see CHANGELOG — Slots 0/0b/1b, bands A–D, the D2/D3 unpinned releases, band T's consumer/DX cluster, and the `.33` cybs `parse_if_else` bootstrap P1) · **.34** **band E, the IR substrate**: three IR=3 miscompiles, one per pass (LASE width elimination, `ir_const_fold` state carried across its own NOPs, `ESTOC` recording no IR node so DCE killed the field store's address register) taking default-vs-IR=3 divergence to **0 of 282**; plus `CYRIUS_PKG_VERSION` from included files, `cyrius port --language=python`, and the bayan/patra folds |
| **In-flight** | **`.34` — COMPLETE, gate green on all five steps; awaiting the maintainer's push + tag.** ⚠ Owed, carried and unchanged by this release: (1) reachable-undef ASYMMETRY (PE exits 1 where Linux exits 0). (2) Windows async has no readiness registry. (3) stiva **Half B** unbuilt and, on `.26`/`.27` evidence, unjustified. (4) The `CYRIUS_CROSS_OS_FULL=1` default flip. (5) The seven residual `assigning non-pointer to typed pointer` warnings where the source is `i64`-declared. ⭐ **`CYRIUS_PKG_VERSION` in included files is now FIXED** — and the reason two attempts failed is recorded: this repo's issue file asserted that "globals declared below their use ARE visible", which is **false** (`fn f() { return XYZ; } … var XYZ = 7;` → `undefined variable`). The appended bytes always reached the lexer; the parser simply cannot see the declaration. Premise-check claims in issue files, not just pins. |
| **Next up** | **Band F — Slot 5, cross-BB regalloc + a vector register class.** ⛔ Not greenfield: a Poletto-Sarkar linear-scan picker is shipped and DEFAULT-ON, and the cross-BB defect is localised to **one line** — the picker computes real live intervals (`ra_first`/`ra_last`) then force-sets every interval's end to the fn end, so the expire step never fires and assignment is greedy-forever. The vector register class IS genuinely absent (the byte matcher only recognises REX.W mov to/from `[rbp+disp32]`). ⚠ Re-derive `_disp_adj`'s site count at slot entry — last measured **57** tree-wide (not the 19 the older pin carried), of which 44 are helper-replaceable. Band E's premise is now spent: the IR=3 divergence count is **0**, and the ceiling assertion lives in `ir3_substrate_correctness.sh` axis 5. |
| **Open queue** | **RE-DERIVED at slot close 2026-08-22: 9** open issues · **2** proposals · **343** archived. `.34` closed and archived BOTH consumer filings that arrived during `.33` — `pkgver-not-visible-in-included-files` and `cyrius-port-language-python` — taking the queue to 8. ⚠ **A NINTH arrived mid-slot and is NOT in `.34`**: `2026-08-22-versioned-wrapper-does-not-pin-cycc` (filed while certifying agnosai 2.0.5 — `~/.cyrius/versions/<pin>/bin/cyrius` resolves `cycc` through `$CYRIUS_HOME/bin` rather than relative to itself, so a release cannot be certified against its own manifest pin without mutating the global install; it warns, so nothing is silent). Untriaged, first input for `.35`. |
| **W1 status** | ✅ **CONSUMED — all 13 spent (.4 → .16).** ⛔ And **W2 is consumed too**: budgeted 3 at `.19`–`.21`, but `.19` shipped as five consumer filings and `.20`/`.21` are now Slots 0/0b. **W1 (13) + W2 (3) = 16 reactive releases of budget; `.4`–`.19` is exactly 16 releases of reactive spend.** Items 8 and 9 never landed and are re-homed to `.22` (Slot 1b). ⚖️ **Maintainer budget call surfaced in roadmap.md**: the observed reactive rate is ~80 % (16 of 20 releases) against W3+W4's remaining 9, so either widen the windows or let filings displace releases explicitly rather than silently. |
| **Owed to the maintainer** | **FIVE decisions open** (2026-08-11 re-triage; a sixth — `break` semantics — was SETTLED 2026-08-12: "break should leave — expected", C semantics confirmed, recorded in the guide + vidya + CHANGELOG, do NOT re-open). **1. Slot 9 design** — the committed "unbox the scalar case" was implemented at `.15` and **reverted**; both storage relocations are disproven, so the survivors are **escape analysis or a scope-tied arena with reclaim**, a different size class. The slot cannot be scoped until this is chosen. **2. Cross-OS full-corpus default flip** — flip for ecb/ach/pi now and hold the selector for cass, or hold all four behind the cass 7. **3. Reactive budget** — ~80 % observed vs W3+W4's 9 remaining. **4. `.21` packing** — confirm pulling the `output_buf` reclaim forward out of the closeout band to share the `input_buf` two-step bootstrap. **5. Per-item `private`** (roadmap open question 3) — `private fn h()` still compiles with **no diagnostic** and privatises the whole file including `main`; nine releases live. **6. `archived/README.md` index covers 40 of 317** — every 6.5.x-era archive is unindexed; backfill from here, or drop the all-files promise in `issues/README.md`. Plus the two standing answered questions — the **self_compile budget** and **committed item 5** (growth-tax audit) — which are now sharper: self_compile is **685 ms** against a candidate ≤700 ms cap, and cycc **1,154,816 B** against ≤1.20 MB, with Slots 3/5/6 all still to land. |

> Full arc detail, the 12-slot sequence and per-arc length estimates live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
