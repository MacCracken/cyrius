# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.6.6 — bumped 2026-09-20, tag PENDING** (the bump commit). The repair release for the 6.6.5 queue AND what reviewing it turned up: six filed issues + six roadmap items (bites 1–12), then ~40 further defects BATCHED into themed bites by the user's call rather than filed — call-ABI (14, 16, 21), lexer/preprocessor (5, 15), globals (19), write-safety (13, 17, 26), the CLI on Windows and off x86 (23, 24), the gate runner (25, 27). **27 bites, 193 commits, five parallel lanes.** `CVE-44` (installer staged its signature inputs at fixed `/tmp` names) and `CVE-45` (an included file could forge `#@file` and defeat `private`). Release gate GREEN on ecb/ach/cass/pi; bench 869 ms. ⚠ **After the user tags: `sh scripts/install.sh --refresh-only` once at the tagged commit.** **Prior: 6.6.5 — SHIPPED** (tag `6.6.5` @ `6b46e190`). |
| **cycc** | x86 **1,328,336 B** (`.text` **1,163,064**) at 6.6.6 — **+34,296** over 6.6.5's 1,294,040. No single patch dominates: it is this release's own surface — nine refusals that NAME what they refuse (struct/vector copy, pair return, SIMD return, async struct/vector, top-level struct call, operator arity, block-scoped top-level vars), the per-target self-host mapping, the checked output-write path in all seven drivers, and the PE open-flag decoder. **seed-derive GREEN after every compiler bite** (29,024-byte seed). `build/cycc-native-aarch64` **1,572,568 B**, regenerated at the closeout from this tree and proved on real pi — and from 6.6.6 the release gate's step 1b FAILS when it is not what the tree cross-builds, which is what ended its five-release drift. Last tagged: **v6.6.5, 1,294,040 B**. |
| **Bootstrap / cross-OS** | **v6.6.6: ecb · ach · cass · pi all `SELFHOST_OK` + `LIBTEST_OK` (99 crossos tests) on REAL hardware**, one host at a time. ⭐ The staging is per-run now (bite 8d): `cross-os-selfhost.sh` carried ~40 FIXED `/tmp` and remote names, which is why CLAUDE.md said to run one host at a time — that constraint is the defect, not the discipline. ⭐ **`cyrius self` had been self-hosting the WRONG FORK on every host but x86-64 Linux** (bite 23a): it hard-coded `src/main.cyr`, so pi compared an aarch64 ELF against an x86 one and printed `FAIL: cycc!=cycc` for a compiler that self-hosts byte-identical, and ecb got `Killed: 9`. Each host now maps to its own fork, verified on real ecb/ach/pi. seed **29,024 B** → cybs → cycc byte-identical. A green CI check is NOT this leg. |
| **Gates** | **v6.6.6 — check.sh GREEN, 226 shell gates** (`find tests/gates -name '*.sh' | wc -l`) + the driver's rows. ⭐ **`check.sh` used to SKIP EVERY SHELL GATE when any driver row was red** (bite 8b): it ran the driver under `set -e`, so one red row aborted before the gates ran and the summary still read like a full run — bite 14's red gate hid behind a stale doc-stamp row that way. Every gate runs now and a gate that did not run is reported **NOT RUN**, never as passed. ⭐ `sh scripts/check.sh <suite>` SELECTS since bite 27a; it used to run all 192 gates whatever you typed, and an unknown name was not even an error. ⭐ Release gate step **1b** is new: the tracked ARM binary must equal what the tree cross-builds. New this release, each mutation-proven: gates_never_write_tree, tool_writes_never_truncate, io_write_all_never_short, cyriusly_use_switch_integrity, manifest_read_whole_file, deps_lock_covers_every_file, release_verify_private_temp, stdlib_modules_self_sufficient, install_gates_ship_the_checksum, install_pillars_ship_the_checksum, check_driver_bounded, check_targeted_run_selects, check_stale_home_reaper, check_sh_targeted_path_cleans_up, check_driver_dies_with_check_sh, fork_version_parity, cx_tcyr_runs, cbt_fork_sites_have_pe_arm, cli_temp_dir_no_leak, self_host_src_per_target, emit_js_refused_off_x86_linux, tracked_sources_canonically_formatted, build_output_rename_checked, capacity_meters_host_fork, pulsar_is_x86_linux_host_verb, build_progress_line_not_spliced, build_temp_source_write_checked, output_write_failure_is_loud, macro_invocation_boundary, lexer_attribute_word_boundary, file_marker_forge_refused, directive_prefix_never_eats_bytes, pe_open_flag_translation, pe_compiler_reads_env, statfs_family_routed, stack_param_homing_matrix (extended), toplevel_block_var_scope, macro_pass_preserves_ifdef_filtering, private_does_not_block_own_declaration, duplicate_symbol_warning_at_scale, gvar_toks_cap_guards_the_store, cx_forward_read_constant_global, cx_relational_or_equal, struct_copy_type_checked, simd_return_shapes, crossos_runner_rejects_a_silent_binary, global_redeclaration_one_definition, toplevel_decl_block_closure, pp_directive_inside_multiline_string. |
| **Corpus** | **340** `.tcyr` (**99** in `crossos/`, both DERIVED: `find tests/tcyr -name '*.tcyr' | wc -l`) · **104** `lib/*.cyr` · **85** `programs/*.cyr` · api-surface **5,247** (`wc -l < docs/api-surface.snapshot`). ⚠ 241 of the 340 still never run on non-x86 hardware — the cross-OS leg's selector is the `crossos/` DIRECTORY. ⭐ The cross-OS runner no longer grades by EXIT CODE alone (a 6.6.6 review fix): a binary that runs no user code exits 0, and the 6.6.5 compiler produced exactly that for `crossos/macro_expansion_with_include.tcyr` — a PASS over the preprocessor defect the test is named for. Any test whose source calls `assert_summary` must now produce its own `N passed` line. |
| **Active minor** | **v6.6.x** — value-form `Result` + language ergonomics. Opened at v6.6.0; **v6.5.x closed at v6.5.73** (there is no `.74` — that number was cut in error and re-cut as 6.6.0). Slot plan in [roadmap.md](roadmap.md): `.2`–`.6` are a reserved **repair window**, then the proposal queue, then the committed ergonomics list. |
| **Shipped this minor** | **.0** the `Result` / `Option` / `Either` **value-form flip** — construction allocates ZERO bytes (was 1600 B per 100 `sock_send`), shipped WITH the ecosystem (8 sibling stdlibs migrated at source, pin-bumped, released and re-vendored); it changed the ARITY of every value, so `payload()` and `tagged_new()` are DELETED; plus top-level `var t, v = f();`, the mixed-return diagnostic, and ⛔ **the v6.5.57 aggregate-copy P0 found at the cut** (live seventeen releases, invisible to the fixpoint because cycc's own source never uses the shape) · **.1** the two filed issues — `f64_exp`/`f64_exp2` **NaN for ±inf** in BOTH implementations (polyfill + emitted x87; the guard must precede the range reduction, which computes `inf - inf`) and AGNOS `clock_now_ns()` reading the **frozen `timer_ticks` clock** (#40 → #95 `rdtsc`) — plus the `cyriusly install` **ETXTBSY** failure (`cp` writes through the directory entry; now temp + atomic rename) and five stdlib folds. · **.2** the repair release — `tagged_new()` restored for a live consumer the deletion survey could not see; two SIMD P0s (a destination read from a frame slot nothing wrote; a top-level intrinsic that SIGSEGV'd); `map_u64` answering untruthfully for its own sentinel keys on five paths; `object;` builds exporting libc-reserved names; a vacuous SIMD gate; `cyrius build` overwriting the running compiler; `check.sh` reporting green over seven gates that never ran · **.3** the six issues the 6.6.2 ecosystem sweep filed, plus the fork-parity and Windows-checkout CI repairs · **.4** the six issues filed after the 6.6.3 handoff (see *Version*) — the 64 KB string-literal pack, `&_private_fn` across files, the `public` marker leak, the writable released install slot, the silent stdlib re-lock, and the raw x86 syscall numbers that had killed `cyrius run/test` on native aarch64 for 59 releases. ⚠ Closed-minor detail for v6.5.x and earlier lives in `completed-phases.md` + the CHANGELOG; this row carries the CURRENT minor only, and was the v6.5.x list until the 6.6.1 refresh. · **.5** the nine issues open at 2026-09-17, one bite each (SIMD register picker; call-site rsp alignment + the PE/UEFI/x86-Mach-O entry bases; pass-1 visibility; the aggregate layout GUESSED since 5.8.17; 14 syscalls named and routed on aarch64; the bench floor + Windows QueryPerformanceCounter; the dep-cache tamper check that failed OPEN (`CVE-43`); one CLI argument rule; cyrlint cross-line) · **.6** the 6.6.5 queue AND what reviewing it turned up — 27 bites over five lanes: the callee's stack-param homing (a vector arg next to 6+ ints, and an x86 struct-returning fn reading its 6th param from the return address), five more call-ABI shapes, a redeclared global reading 0, a block-bodied closure dropping the rest of the program, four preprocessor passes executing directives inside strings, a forged `#@file` defeating `private` (`CVE-45`), `--version` missing from five of seven drivers, cx having no target macro (so its whole stdlib compiled to nothing), ⛔ **Windows `O_APPEND` overwriting from offset 0 and `O_TRUNC` not truncating**, `cyrius self` self-hosting the wrong fork on every host but x86-64 Linux, 14 tools truncating the file they replace on a short write, the installer's fixed-`/tmp` signature staging (`CVE-44`), and a gate runner that skipped every shell gate whenever one driver row was red |
| **In-flight** | **6.6.6 closing** — 193 commits (`3e35aed2` … the bump), five lanes merged, release gate GREEN end to end, bench recorded. ⚠ **After the user tags: `sh scripts/install.sh --refresh-only` once at the tagged commit** (the bump's stamp names the pre-bump HEAD). ⛔ Before any sibling pins 6.6.6: **yukti must drop its own `SYS_STATFS`** (6.6.6 names it in both Linux peers and routes 137→43 on aarch64; keeping yukti's aarch64 `43` keeps the bug, and the compiler now warns at that line). Sibling notes per repo are in each repo's roadmap. |
| **Next up** | **v6.6.7** — the tail 6.6.6 deliberately did not pack (roadmap.md *v6.6.7*): two filed issues (a top-level `for x in a..b` cannot use its own loop variable and nesting two SIGSEGVs; compile time is QUADRATIC in the global count) plus the consolidated list — stdlib self-sufficiency (46 of 103 modules, ratcheted at 26), `O_DIRECTORY`/`O_NOFOLLOW` on PE (a semantics decision, the user's call), 22 arch-neutral raw syscall literals unrouted on DARWIN, shell gates orphaned by a SIGKILLed `check.sh`, an enum constant past index 1024 not constant-folded, `[build].modules` ignored without a `[deps]` section, and ⚠ `check.sh` going RED under LOAD because a child killed at bite 8c's new deadline reports as an ordinary row failure instead of saying it timed out (measured at the 6.6.6 close: RED with four agents working, 291/291 GREEN quiet). |
| **Open queue** | **2 open issues** (`docs/development/issues/`, both pre-dating 6.6.6) · **4 open proposals**. The queue opened 6.6.6 at six issues and closed it at two: every one was fixed, and the ~40 defects the reviews found were batched into this release rather than filed, by the user's direction — "lets not end up with a dozen new issues". |
| **Ecosystem** | **The 6.6.2/6.6.3 sweep CLOSED 2026-09-12.** Derived from committed HEADs: **125** repos with a `cyrius.cyml` — **14** on 6.6.3, **110** on 6.6.2, and **1** below (agnos 6.6.1, another agent's). hisab is also another agent's and is **not** to be updated. dhvani **2.2.4** holds the sound stack's versions, with hisab kept at **2.11.2** (naad 2.2.4 and svara 3.5.5 were rebuilt and pass against it); jalwa **1.4.5** aligns its shared pins to dhvani. **Still open, recorded but not fixed:** same-arity duplicate fns (agnostic 22, mehman 19, hadara 2, aegis 1, kriya 1 — warnings only); ~30 `path = "../sibling"` + stale-`tag` pairs (deliberately not done). ⚠ **Stale docs:** `ecosystem-migration-6.6.2.md` still reads "~11% DONE, DO NOT ARCHIVE", and its outlier #4 is superseded by the (now archived, FIXED v6.6.4) raw-syscall issue; vidya's `types.cyml` structural facts are still stamped cycc 6.6.1 / 1,247,608 B. |
| **W1 status** | n/a — reactive-window bookkeeping belonged to the v6.5.x minor and is retired with it. v6.6.x reserves `.2`–`.6` as its repair window instead; see [roadmap.md](roadmap.md). |
| **Open decisions for the user** | **None blocking.** The three standing items are recorded in [roadmap.md](roadmap.md) *Open questions* WITH stated defaults, so work starts under them rather than waiting: the self_compile budget (owned by the later perf track — ⚠ note both halves of the old candidate pair, ≤700 ms and ≤1.20 MB, are now exceeded at 743 ms / 1,251,864 B (6.6.3), so it is an input to re-decide, not a target that was missed), the growth-tax audit (review with it), and macOS concurrency ordering (default: keep last; pull forward if a consumer appears). **Waiting on the user's git calls (sibling repos, not blocking cyrius):** commit + tag **patra 1.14.3**, **sigil 3.12.18** and **mirshi 1.11.2** (patra/sigil are pinned to 6.6.4, so tag them AFTER the cyrius 6.6.4 tag; mirshi stays on its 6.6.2 pin); commit the CHANGELOG placement fixes in bayan, mabda, sandhi, sigil, vani and yantra (each release shipped with its own entry buried); commit or discard the blank-line diffs in ganita, niyama and sankoch; discard the stale `cyrius.lock` edits in mabda, sigil and sankhya (their content differs from what shipped green); review samvada's 11 uncommitted doc files (dated 09-10, origin unconfirmed); commit the two new issue files (cyrius, kavach). |

> Full arc detail, the 12-slot sequence and per-arc length estimates live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
