# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.6.1 — the two filed issues fixed, five stdlibs folded.** `f64_exp`/`f64_exp2` returned **NaN for ±inf** in BOTH implementations (the `lib/math.cyr` polyfill and the emitted x87 sequence) — the range reduction computes `inf - inf`, and the exponent it assembles comes from `f64_to(inf)` saturating to `i64::MIN`, so the guard has to come FIRST, not inside the reduction. `clock_now_ns()` on AGNOS was bound to **#40 `uptime_ms` (`timer_ticks`, frozen with interrupts cleared)** — a stopped clock, rebound to **#95 `uptime_us` (`rdtsc`)**. Plus the `cyriusly install` **ETXTBSY** failure: `cp` writes through the directory entry, so installing over the running toolchain always failed part-way; now temp + atomic rename. **Prior: 6.6.0 — SHIPPED.** |
| **cycc** | x86 **1,247,728 B** — ⚠ **v6.6.2 IN FLIGHT, not yet released** (+120 B over 6.6.1's 1,247,608). The delta is real this time and the compiler is NOT byte-identical: `.2` changes the front end (the SIMD operand-slot reservation, the top-level guard, the 79/111 token renumber, and the libc-name symbol hiding), all of which cycc's own source exercises. **seed-derive GREEN** — `seed → cybs → cycc` byte-identical, which is the load-bearing leg for a token change (the `>>>` case at v6.4.74: cybs could not lex the new spelling and only seed-derive noticed). ⚠ The `lib/boxed.cyr` restore and the `map_u64` sentinel fix contribute **zero** bytes here — cycc includes neither `tagged.cyr` nor `hashmap.cyr`, so the fixpoint is structurally blind to both and the corpus gates are the only evidence. A size match is not evidence a change is inert, only that cycc does not use the path.|
| **Bootstrap / cross-OS** | **ecb** · **ach** · **cass** · **pi** — all four `SELFHOST_OK` on REAL hardware, run sequentially. ⚠ Run ONE host at a time — fixed /tmp + remote paths clobber under concurrency. An unreachable host is held open, never reported green. ⭐ **`.62` is what the leg is for**: it went RED on cass because a new parity block called `f64v256_*` UNCONDITIONALLY and **cass is an Intel Celeron J4125 — Goldmont Plus, SSE4.2, NO AVX2** — `STATUS_ILLEGAL_INSTRUCTION` 0xC000001D. The dev box has AVX2, so a Linux-only run could not have seen it. Guard SIMD builtins by CAPABILITY, never call them bare; and note the same unguarded shape in a SHELL gate **fails open** (a SIGILL'd binary exits instantly, so a timing ratio reports a huge win while measuring a crash). |
| **Gates** | v6.6.1 closed **GREEN 240 / 0**. ⚠ **v6.6.2 in flight: main suite GREEN 240 / 0.** The one red outside it is `agnos-crossbuild-gate.sh`, and it is ENVIRONMENTAL, not a `.2` regression — agnoshi pins **6.5.36**, which the 2026-09-07 toolchain wipe removed, so `_try_redirect_to_pinned` hard-errors before any compile. Reproduces identically with the pre-session wrapper. Restoring that version is Phase 0 of `ecosystem-migration-6.6.2.md`, deliberately NOT done mid-release: `install.sh` ACTIVATES what it installs and would repoint `~/.cyrius/lib` at a 6.5.36 stdlib. · **148** shell gates on disk under `tests/gates/<bucket>/` in 8 buckets. ⚠ **DERIVE this** — `find tests/gates -name '*.sh' | wc -l`. It has been wrong in three separate docs from hand-incrementing, and this very row read **139** while the Corpus row below read **134**, for the same tree, at the same time. |
| **Corpus** | **306** `.tcyr` (**70** in `crossos/` — the cross-OS selector is that DIRECTORY, the `vr01_` prefix was retired at v6.5.11) · **103** `lib/*.cyr` (`boxed.cyr` restored the boxed tagged-union primitives) · **84** `programs/*.cyr` · api-surface **5,156** · heap map **102** regions (gate-derived; ⚠ a raw `grep -cE '^#   0x' src/main.cyr` says **145** because it also matches 18 `FREED` markers — run `sh tests/gates/memory/heapmap.sh`). |
| **Active minor** | **v6.6.x** — value-form `Result` + language ergonomics. Opened at v6.6.0; **v6.5.x closed at v6.5.73** (there is no `.74` — that number was cut in error and re-cut as 6.6.0). Slot plan in [roadmap.md](roadmap.md): `.2`–`.6` are a reserved **repair window**, then the proposal queue, then the committed ergonomics list. |
| **Shipped this minor** | **.0** the `Result` / `Option` / `Either` **value-form flip** — construction allocates ZERO bytes (was 1600 B per 100 `sock_send`), shipped WITH the ecosystem (8 sibling stdlibs migrated at source, pin-bumped, released and re-vendored); it changed the ARITY of every value, so `payload()` and `tagged_new()` are DELETED; plus top-level `var t, v = f();`, the mixed-return diagnostic, and ⛔ **the v6.5.57 aggregate-copy P0 found at the cut** (live seventeen releases, invisible to the fixpoint because cycc's own source never uses the shape) · **.1** the two filed issues — `f64_exp`/`f64_exp2` **NaN for ±inf** in BOTH implementations (polyfill + emitted x87; the guard must precede the range reduction, which computes `inf - inf`) and AGNOS `clock_now_ns()` reading the **frozen `timer_ticks` clock** (#40 → #95 `rdtsc`) — plus the `cyriusly install` **ETXTBSY** failure (`cp` writes through the directory entry; now temp + atomic rename) and five stdlib folds. ⚠ Closed-minor detail for v6.5.x and earlier lives in `completed-phases.md` + the CHANGELOG; this row carries the CURRENT minor only, and was the v6.5.x list until the 6.6.1 refresh. |
| **In-flight** | **none — 6.6.1 cut.** All three filed issues closed and archived; five stdlib folds (patra 1.14.1, sakshi 2.5.1, sankoch 2.7.14, ganita 1.2.4, niyama 1.0.10), each fixed at source and re-pinned before vendoring. Shell gates **140 → 144**, each mutation-proven. ⭐ **The DCE fix is the one worth remembering: the filing said "PE / `--win` target only" and that bound was an artefact of which platforms the reporter builds, not of the bug.** `main_x86_macho.cyr` includes the same `x86/fixup.cyr`, so Intel-Mac Mach-O was crashing too, unreported — found by running the repro on ach rather than trusting the report's target matrix. One gate axis exists purely to fail a PE-only fix. |
| **Next up** | **Slot `.2` — the `cyrius build <src>` overwrite guard.** In this repo `cyrius.cyml` declares `output = build/cycc`, so `cyrius build <any-other-file>` writes the result over the compiler; it happened at the v6.6.0 cut and cost a recovered /tmp binary. Deliberately not packed into 6.6.0 (a `cbt/` change needing its own gate after the release gate had gone green). See roadmap.md Phase 1. |
| **Open queue** | **1 open issue** — `2026-09-08-lexer-token-types-79-and-111-double-assigned` (token 79 is both `object` and `f64_sqrt`; 111 is both `stack` and `callptr`; verified live, renumbering deferred because it touches codegen across 7 forks + the seed chain). · **3** open proposals · **387** archived issues. The queue was at **zero** after 6.6.1; this one came out of the vidya gotchas audit. |
| **W1 status** | n/a — reactive-window bookkeeping belonged to the v6.5.x minor and is retired with it. v6.6.x reserves `.2`–`.6` as its repair window instead; see [roadmap.md](roadmap.md). |
| **Open decisions for the user** | **None blocking.** The three standing items are recorded in [roadmap.md](roadmap.md) *Open questions* WITH stated defaults, so work starts under them rather than waiting: the self_compile budget (owned by the later perf track — ⚠ note both halves of the old candidate pair, ≤700 ms and ≤1.20 MB, are now exceeded at 731–734 ms / 1,247,608 B, so it is an input to re-decide, not a target that was missed), the growth-tax audit (review with it), and macOS concurrency ordering (default: keep last; pull forward if a consumer appears). |

> Full arc detail, the 12-slot sequence and per-arc length estimates live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
