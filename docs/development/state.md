# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.5.21 — SHIPPED.** Release gate: self-host fixpoint **1,168,352 B**, **seed → cybs → cycc byte-identical**, corpus **271/271**, gate scripts **58** (0 orphans / 0 dangling, re-derived both ways). Bench on a quiet box: `self_compile` **683 ms** (688 at .20, −0.7 %, flat), `phase_pp` **114 ms** (117 at .20 — clears the per-byte `#@file` check added to PP_PASS's hot copy loop). ⚠ `bench-history.csv` carries an INVALID row at 2026-08-13T23:43:28Z (788 ms / 134 ms) measured against a foreign `cargo` build at ~184 % CPU — ignore it; both numbers were inflated ~15 %, a box-wide scale factor rather than a code change. Orphan check was CLEAN, so the v6.5.19 leaked-child failure mode did NOT recur. **Two proposals completed and RETIRED** — `CYRIUS_PKG_VERSION` (third pin; it held) and tuples/multi-value returns — **and BOTH proposals' stated premises were wrong in a load-bearing way, ours not the consumers'.** The tuple filing asked for a feature that shipped at **v3.7.2**; underneath it were three silent miscompiles (cx returned the first value twice from `return 0;` emitter stubs; a `: f64` tuple lost its first value to a stale xmm0 on x86/PE — the exact Dekker shape; f64 type lost at the binding, which is why the `: f64` RETURN annotation had **two uses ecosystem-wide, both in our own tests**). The roadmap's `CYRIUS_PKG_VERSION` scope said `[package].version` "already resolves `${file:VERSION}`" — nothing did; `cyrius package` printed **"unknown"**. Plus sandhi 1.9.10 (agnosai P1: `SANDHI_OK` with a NULL body ptr on every response >~1 MiB) and a forged `#@file` no longer defeating `private`. ⚠ **Next CVE number is still 41** — the `#@file` hole is a `private` soundness bug, not a sandbox escape, and does not consume an identifier. Prior: **6.5.19 — SHIPPED.** Release gate GREEN on all 5 steps; push + tag are the maintainer's. **Five consumer filings closed** (four agnosai, one majra) — four were diagnostics/tooling telling the user something untrue, the fifth silent heap corruption under threads — plus **CVE-39** (cycc SIGSEGV'd or HUNG on 40 of 103 truncated top-level constructs) and **CVE-40** (recursive descent had NO depth bound; `fn f() {` × 514 SIGSEGV'd cycc from ordinary untrusted stdin). ⚠ **Next CVE number is 41** — 39 and 40 are consumed, and *neither appears in any `docs/audit/` file yet*; reconcile the identifier accounting before the closeout security re-scan. Prior: **6.5.18** — `platform/fdlopen` closed, the last full-corpus failure on both Macs; `cyrius fmt` corrupting string literals; sigil 3.12.7. |
| **cycc** | x86 **1,168,352 B** (+13,576 for `.21`: the multi-value return signature parser + arity-3 codegen + destructure contract, `PP_EMIT_PKGVER`, and the `#@file` neutralisation) ·  seed 29,024 B → cybs → cycc byte-identical. (**−32 B in flight for 6.5.20** against .19's 1,154,816 B: **bite 1** added `_sw_tbl_repair` — compaction stage 3b — plus the per-fn switch-table registry, closing a P1 silent miscompile where the v5.6.27 NOP-harvest compactor moved code without repairing the switch jump table, so every case body past index 0 was entered at a stale offset (SIGSEGV with a `default:`, WRONG ANSWER without one); `break` now leaves the switch/match with C semantics. **Bite 2** removed the six `#derive` re-anchor call sites and the struct-copy whitespace duplication, so `#derive` is line-exact with no marker (that half was −48 B on its own). **Finish-out** made the derive tail copy come AFTER the generated bodies — a multi-line tail such as `struct Z { a; } fn helper() {` had been splicing the generated fns into the user's function body (rc=0, no diagnostic, SIGSEGV) — routed `#derive(Deserialize)` through `PP_PARSE_STRUCT_DEF` so it stops deleting the struct and silently accepting malformed fields, and taught `_ra_frame_trim`'s unaligned byte scan to skip jump-table data. Prior, **+12,800 B for 6.5.19**: **CVE-39 (+112 B)** — cycc SIGSEGV'd or HUNG on **40 of 103** truncated top-level constructs, and `fn` / `var x` / `async` died with **ZERO bytes of stderr**. v6.4.78's past-EOF clamp in `PEEKT` sat INSIDE the `_had_error` guard on the premise that "a well-formed parse never advances past EOF"; the silent skippers (`SKIP_GENERICS`, `_capture_tparams`, the `var`/`match` paths) walk off the end BEFORE any diagnostic exists, so the clamp was bypassed and every `t == 12` EOF test in the tree silently failed. Clamp is now unconditional, plus `_wd_eof_tick` (a past-EOF backstop NOT gated on `_had_error`, since the v6.4.62 watchdog only detects a STALL and these loops ADVANCE), explicit EOF guards on the enum/struct/union/generics/match member loops, and `_ends_guard` — `ec` was never bounded against a 256-slot LOCAL array, so a **400-arm `match` or 400-case `switch`, ordinary valid cyrius, wrote off the stack frame**; v6.2.1 resized that array 32→256 and guarded its NEIGHBOUR but never fenced `ec`. Plus **`--syntax-only` (`_syntax_only`, +24 B)** so `cyrius lint` can ask "does this PARSE?" without resolution — an undefined VARIABLE is a parse-time abort `--allow-undef` cannot reach, so the compiler stopped at the first missing include and a file's syntax errors were unreachable; ⚠ routing those five sites through panic-mode `ERR_IDENT` was tried FIRST and REVERTED — it MANUFACTURED an `unexpected else` on well-formed `lib/fs.cyr` and put lint straight back to refusing valid downstream source. Also: the preprocessor now keeps the expanded stream line-faithful with the source — `#ifdef` padding + `#derive` re-anchor + dropping the `#@incdir` line + **`PP_EXPAND` emitting only the newlines an invocation CONSUMED** (its unconditional trailing newline had been masked by the unpadded `#define` line, so padding the directive made every function-like macro invocation report one line late) — **`FM_BUILD` (+4,096 B with the above)** counts a final line that has no terminating newline, so the last line of such a file stops reporting a raw expanded line past EOF with no `<file>:` prefix; the `duplicate fn` warning names both ends of the collision; and **`ERR_IDENT` (+264 B)** splits struct-field RESOLUTION failures out of the SYNTAX error class, so `cyrius lint` stops condemning a module that reads `.field` off a struct declared in a sibling file. Plus **CVE-40 (+4,168 B)** — recursive descent had NO depth bound, so `fn f() { ` repeated **514** times SIGSEGV'd cycc from ordinary untrusted stdin, and five more shapes did the same (nested blocks / `if` / `while`, parenthesised expressions, unary-minus chains, call chains). PROVEN stack exhaustion, not a table: the crash depth scales LINEARLY with RLIMIT_STACK (512 at 8 MB, 1026 at 16 MB, 2053 at 32 MB) = ~16 KB per nesting level. NEITHER existing watchdog could see it — `_wd_tick` needs a STALL and `_wd_eof_tick` needs a PAST-EOF read, and deep nesting advances GTI every level and never passes EOF. Fixed by a depth counter (`_rd_enter`, bound 256) at the FIVE chokepoints — PARSE_STMT, PARSE_FN_DEF, PEXPR, PARSE_TERM, PARSE_FACTOR — plus PE `SizeOfStackReserve` 1 MB → 8 MB so the bound is reachable on Windows (a bound alone would have been a Linux-only half-fix). ⚠ It took FOUR passes to cover the class: PARSE_STMT alone left three expression vectors live, PEXPR left the unary chain (it recurses one tier BELOW, in PARSE_FACTOR), and PARSE_TERM left nested `fn` (a nested definition re-enters PARSE_FN_DEF, never the statement wrapper) — the "grep the SHAPE, not the operator" trap again. Gate: `tests/gates/diagnostics/recursion_depth_bounded.sh`. Also **the fn RETURN-TYPE half of the lint P1**: `: T` with T declared in a SIBLING file went through the syntax-class `ERR_MSG`, set `_panic`, and the desynced resync MANUFACTURED `expected ';', got '='` — so `cyrius lint` condemned 3 files of **agnosai, a repo that BUILDS GREEN**. The struct-FIELD sites were fixed earlier this release; this is the site they missed. Was 1,142,016 B at .18.) |
| **Bootstrap / cross-OS** | **ecb** · **ach** · **cass** · **pi** — all `SELFHOST_OK` + `crossos LIBTEST_OK` on REAL hardware at **6.5.21**, **47/47 each** (derived; the crossos set grew 46 → 47 with `multi_return.tcyr`). Run ONE host at a time — fixed /tmp + remote paths clobber under concurrency. ⚠ **`cross-os-selfhost.sh <host>` without its SECOND argument runs the self-host ONLY and no crossos tests, exiting 0.** At the `.21` run that produced four `SELFHOST_OK` lines with the new gate never executed anywhere — a partial gate that reads exactly like the whole one. The release gate passes `"crossos"` as the selector (`release-gate.sh:113`); check `SELFHOST_OK` **and** `LIBTEST_OK` separately, and cross-check the ran-count against the local `find tests/tcyr/crossos -name '*.tcyr' | wc -l`. ⛔ Full-corpus figures below are from **6.5.19** and are STALE by construction — the corpus is now 271 and crossos 47, so every count in them is wrong; re-measure before quoting. Prior (2026-08-11, corpus 270): ecb 269/0 · ach 269/0 · pi 269/0 · cass 262/7 (one HANG at the 90 s timeout). ⚖️ The `CYRIUS_CROSS_OS_FULL=1` default flip remains a live maintainer decision. |
| **Gates** | `check.sh` **178 / 0** at .19; NOT re-run since (owed before tag). **58** shell gate scripts at `.21` (derived `find tests/gates -name '*.sh' | wc -l`, +1: `codegen/cx_multi_return.sh`), **0 orphans, 0 dangling** — re-derived BOTH ways with the `comm` recipe. ⚠ `.21` added a fourth instance of the vacuous-gate trap, in a NEW form: `cx_multi_return.sh` passed against the 6.5.20 compiler because it builds the cx compiler from `src/backend/cx/emit.cyr` in the WORKING TREE — swapping `build/cycc` does not revert the emitter, so the gate scored green on the very defect it exists to catch. Mutating the emitters back to their `return 0;` stubs is the only proof that works. Prior: `check.sh` **178 / 0** (169 at .18; **+9 shipped in 6.5.19** — `toolchain/auto_deps_verb_gate.sh`, `frontend/duplicate_fn_attribution.sh`, `frontend/lint_reports_unparseable.sh`, `memory/freelist_thread_safe.sh`, `toolchain/bench_timer_floor_measured.sh`, `toolchain/test_runner_bounded.sh`, `diagnostics/truncated_input_terminates.sh`, `diagnostics/recursion_depth_bounded.sh`) + **57** shell gate scripts under `tests/gates/<bucket>/` (counted with `find tests/gates -name '*.sh' \| wc -l`, never incremented), ALL live, **0 orphans and 0 dangling** — **48** registered by exact path in `programs/checks/*.cyr`, **10** driven from `scripts/check.sh` (`heapmap.sh` is in both; `main.cyr` also reaches it as `_heapmap_gate()`). ⚠ **This happened TWICE in 6.5.19 alone.** `toolchain/test_runner_bounded.sh` was written, committed and left registered NOWHERE (count 53→54, gate never ran); it was caught, and then `diagnostics/recursion_depth_bounded.sh` did exactly the same thing at 56→57 and was caught only by re-deriving at release time. A gate FILE is not a gate RUN — the `find` count rising is the symptom that looks like progress. Re-derive the orphan set BOTH ways every release, don't eyeball it:<br>`comm -23 <(find tests/gates -name '*.sh' \| sort) <(grep -ho 'tests/gates/[a-z-]*/[a-z0-9_]*\.sh' programs/checks/*.cyr scripts/check.sh \| sort -u)` — and `comm -13` for the reverse (registered but missing). New at .14: `toolchain/distlib_bundle_selfcheck.sh` (5 axes; the old check compiled to /dev/null and had never run) + `crossos/tailcall_frame_escape.tcyr` (mutation-proven: 4 of 7 fail against the 6.5.13 binary). New at .15: `crossos/signal_errno_peer_values.tcyr` (mutation-proven on ecb: 12/1 with Linux SIGCHLD restored) + 12 assertions added to `crossos/syscall_wrappers.tcyr` for pipe/fchmod/access, which had NONE. ⚠ **COUNT these, never increment** — a first pass here wrote 43 by adding one to .12's stale 42 and missing .13's `codegen/cx_indirect_call.sh`. ⚠ Re-derive with `find tests/gates -name '*.sh' \| wc -l`; earlier revisions read 10 → 16 → 19 because each release *incremented* the number instead of counting. |
| **Corpus** | **271** `.tcyr` in 15 buckets (**47** in `crossos/` — `multi_return.tcyr` added at `.21`; multi-value return had shipped at v3.7.2 with THREE lines of coverage, all integer-only in `lang/`, and had never run off-Linux, which is where all three of its silent miscompiles were living. Prior: **270** / **46** — `freelist_thread_safe` and `bench_timer_floor` moved there in flight at 6.5.19; they had shipped outside it, which is the same as not being gated off-Linux at all) · **100** `lib/*.cyr` · api-surface **4832** (+1 at .16: `syscalls_macos::sys_fdatasync` — the x86-macOS peer never had it, so `platform/fsync` did not merely fail on Intel-Mac, it failed to COMPILE) · heap **100** regions / 0 overlaps |
| **Active minor** | **v6.5.x** — generated-code quality (opened at v6.5.0; **v6.4.x closed at v6.4.86**) |
| **Shipped this minor** | **.0** file-scoped `public`/`private` · **.1** arity-aware overload dispatch + 4 stdlib folds · **.2** the `ir_const_fold` jump-span miscompile (**IR=3 mismatches 35 → 8**), `xrmdir`, yukti 2.3.0's six agnos ABI defects · **.3** diagnostic lines after `include`, `install.sh` ETXTBSY strand, `version-bump.sh` same-version early exit · **.4** `vec_sort_by`/`vec_select_nth` (the agnosai filing), sigil 3.12.2 / yukti 2.3.2 / sandhi 1.9.8 / mabda 4.0.8 folds, the vacuous sign-efi gate, fold-version table rot · **.5** the `CYRIUS_IR=3` switch-dispatch miscompile (filed as LASE, actually **DCE**) — closes `switch_dispatch`, one of the eight residuals .2 recorded, so **7 remain** — + bayan 1.4.0 · **.6** `sys_exit_group` on all 3 syscall peer families + `async_await_readable_ms`; macOS-x86's untranslated 231 → silent SIGSYS; `proj-tcyr`'s 8-bit exit truncation (256 failures scored PASS); `cyrius fuzz` blind to scaffolded harnesses; sandhi 1.9.9 + vani 1.1.3 folds · **.7** hisab's `alloc_reset` allocator-invalidation; the aarch64 `fchownat` private-alias band (≥1000, both real candidates owned); `#@incdir` file-relative includes with the CVE-16 escape closed at byte-0 + relative-only; `sys_chdir` called-but-undefined; `signal_default` + the `x*` family; subfolder callout for test/bench/fuzz; missing-output-dir bare FAIL · **.8** szal's `thread_join` lost-wakeup deadlock (P1); `thread_create_detached` (210 MB VA leak → 0); the `i64::MIN` formatter class across 12 sites incl. 2 in the compiler + sakshi 2.4.8; agnos `#97 chan_op` minted (`#96` fork deliberately not); `coverage`'s four fail-open/truncation defects; the doc/vet/deny/audit fail-open family; `distlib --all/--check`; the cross-OS gate batched + honest; bayan 1.4.1 fold · **.9** agnos `CH_ENDOW` + `sys_spawn_path_env` (both blocking agnos bite 7); the three-state futex mutex (392→48 ns, and W1 item 3's stated prerequisite was wrong); the growable arena + exhaustion policy — an exhausted arena was a SIGSEGV layers away, and adopting arenas made a leak into a crash · **.10** `distlib`'s `.deps` sidecar under-reported (8 of 12 for setu) and thereby DISABLED `cyrius deps`' consumer check — now declaration ∪ include-scan, base bundle only; `alloc_via` was two-thirds call plumbing (15.1 → 11 ns) · **.11** test suite → subfolders (`vr01_` prefix → the `crossos/` **directory**) with three silent-green readers closed first, incl. CI scoring a fake PASS because cycc on EMPTY stdin exits 0 with a runnable binary; `MAP_ANONYMOUS` was Linux's 32 on Darwin (`mmap_anon()` → 0 for every caller); **macOS threading now RUNS the worker** via a serial fallback — the wrong constant had been the accidental safety net for the missing backend, and fixing it turned the no-op into SIGSYS; `dir_list`/`is_dir` per-call 4 KB → stack (`dir_walk` 4443 → 239 B/entry) · **.12** sigil **3.12.4** (written this release — closes 3.12.3's residual by localising the bignum engine scratch; the banking existed for a cyrius quirk WE fixed at 6.3.15 and never propagated) — an RSA verify BYPASS (888/400,000 forged accepted upstream; partial by its own account, PSS still banked); `CYRIUS_STACK_ARRAYS=0` silently re-opened it (our flag revoking their fix) → now warns; `lib sync` dropped package DIRECTORIES (`unicode`) at exit 0 — the un-updated twin of a v6.1.x `cyrius deps` fix — plus fail-loud on any unsatisfied declaration; cx emitted a SELF-RESTARTING .cyx at exit 0 for an undefined call (1026 loops; native gated since v6.3.2, cx never got it); tree walks followed symlinks (**41 passed for a corpus of ONE**) → `is_symlink` + depth cap, symlinked FILES still run; `bench_string` 56-byte stack overflow SIGSEGV since v6.3.15, invisible behind the non-blocking bench step; `*.fcyr`/`*.bcyr` DIRECTORIES compiled as files; `dir_list_into` (0 B listing) · **.13** cx `callptr` (opcode 105 — three defects that had to land together: `ELOAD_FN_ADDR` a return-0 stub, `main_cx.cyr` never setting `_fixup_base`, `ECALLIND` a hard error) + agnos `#98 ptrscan` · **.14** the tail-call frame-escape P0 (`return f(p)` freed the frame `p` pointed into; the v5.8.16 guard was SYNTACTIC and missed every aliased form) + sigil **3.12.6** (the whole PSS workspace localised — PSS verify had the same authentication bypass 3.12.3 fixed for v1.5, and the compiler defect is why three releases could not close it) + `distlib`'s bundle self-check, which compiled to `/dev/null` and had never run · **.15** the macOS-arm64 constant + syscall gap closure (**ecb full corpus 11 → 6**): ten constant VALUES the aarch64-Linux peer leaked onto Darwin — ESYSXLAT renumbers NUMBERS, never VALUES, and Linux `SIGCHLD` 17 IS Darwin's `SIGSTOP`, so a child-exit wait silently watched for a stop; `SIGPWR` was 30 = Darwin's `SIGUSR1`; plus the macho ESYSXLAT gap set (`pipe2`/`fchmod`/`faccessat` never mapped) where **Darwin's `pipe` returns both fds in REGISTERS** so a renumber alone was insufficient; and `sys_access` passed 3 args where Darwin validates the 4th — every macOS call returned −22, which had been silently SKIPPING `tls_native_scaffold`'s entire OpenSSL block (a vacuous pass, now honestly red); sakshi 2.4.9 + both fold tables de-rotted. **Reverted at .15**: a `Result` unboxing that passed EVERY gate and still reported a failed file open as SUCCESS in a retaining loop. ⭐ **ach's leg of the release gate then caught TWO of the .15 fixes one fork behind** — `SIGPWR = 30` was still live on the x86-macOS peer (30 IS Darwin's `SIGUSR1`), and `sys_fchmod` **SIGSYS'd on every Intel-Mac** because `EMACHO_SYSXLAT` had no `91 → BSD 124` route, so the number reached Darwin unprefixed. Both fixed at source; `sys_fchmod` had been broken on ach since it existed and only surfaced because .15 added the first crossos assertion that CALLS it · **.16** macOS `uname`/`sysinfo` per-arch routes, sakshi 2.4.10, two issues filed from measurement · **.17** hisab's capturing-closure SIGSEGV, a syntax error in an UNCALLED fn, `distlib` rejecting correct bundles, sankoch 2.7.7 · **.18** `platform/fdlopen` closed (the last full-corpus failure on both Macs; `#naked` had been consumed-and-IGNORED by two forks since v6.2.27), `cyrius fmt` corrupting string literals — it was rewriting **1,239 lines of this repo's own `src/main.cyr`** — sigil 3.12.7 · **.19** five consumer filings (four agnosai, one majra) + **CVE-39** (40 of 103 truncated constructs SIGSEGV'd or HUNG cycc) + **CVE-40** (recursive descent had NO depth bound — PROVEN stack exhaustion, ~16 KB/level; it took FOUR passes to cover the class) |
| **In-flight** | **NONE — `.21` is complete pending the cross-OS leg + `check.sh`.** ⚖️ **`.21` was RE-PINNED by the maintainer** away from roadmap.md Slot 0b (the ONE heap-layout release: `input_buf` raise + `output_buf` band reclaim sharing a single two-step bootstrap) to "fold latest stdlibs + complete and retire two proposals". Slot 0b is **unspent and still owed** — it did not ship at `.21` and needs a new number. Maintainer note at re-pin: *"roadmap has been displaced a little this minor from bugs and a few other items but we will return to the roadmap after this release."* Prior: **6.5.20 — NOT STARTED.** Re-pinned by the 2026-08-11 backlog re-triage; see [roadmap.md](roadmap.md) Slot 0. **Bite 1 = the `switch`-case P1 silent miscompile** — with `default:` present, `p(2)` returns **exit 0 when 2 is expected**, `p(1)`/`p(3)` SIGSEGV, and on cx `break` in a case HANGS; re-verified on HEAD `build/cycc` 2026-08-11. ⛔ The filing's "per-backend (x86/aarch64/macho/PE/cx)" reason is **wrong** — `parse.cyr:453-454` forces `use_table = 0` on aarch64 and cx, so it is a **one-file frontend fix**. **Bite 2 = `#derive` line-numbering** — ⛔ **REBUILD, not rebase: the design-1 implementation recorded as "built and verified" is NOT ON DISK anywhere**, so all four verification sweeps (fixpoint, 589 `#derive` files, 300 randomized programs, the 240-case attribution sweep) are re-runs, and it must ship with its silent-code-loss remedy. Both need the same four-host gate — run **ONE** cross-OS cycle covering both. Rides free: the D1/D2 dead-code removal. |
| **Next up** | **`.20` Slot 0 — the two pinned bugs** (switch-case P1 first, then `#derive` line-numbering), then **`.21`** the ONE heap-layout release (`input_buf` raise + `output_buf` band reclaim share a single two-step bootstrap), then **`.22`** the DX/diagnostics finish-out (Slot 1's homeless (b)–(e), plus the four orphaned fold-ins W1/W2 never spent: `CYRIUS_PKG_VERSION`, the `folds_agnos_parity` SKIP list, the two cyrlint gates, the bare-metal forbidden-module check). Then the codegen spine in its committed order — Slot 3 IR substrate (`.23`–`.24`) → Slot 5 regalloc (`.25`–`.26`) → Slot 6 SIMD residency (`.27`–`.28`). ⛔ **W1 AND W2 ARE BOTH FULLY CONSUMED at `.19`** — 16 reactive releases against a 16-release budget, while the pinned codegen spine sits at **zero**. W1 items 8 and 9 are re-homed to `.22`; do not go on listing them as "next up" in a window that no longer has numbers. |
| **Open queue** | **DERIVED 2026-08-13: 11** issues · **2** proposals · **318** archived issues + **29** archived proposals. (The 2026-08-11 revision below read "12 · 3 · 316 + 27" — 3 of its 4 components were wrong when re-derived the same way two days later, which is exactly why this row says DERIVE, never quote.) **Net at `.21`: −2 proposals**, both ARCHIVED as shipped (`2026-06-25-source-level-version-constant`, `2026-08-13-tuple-multi-value-returns`). ⚠ Retiring these two exposed that **no written lifecycle governs proposals at all**: `proposals/README.md` and `proposals/archived/README.md` do not exist, and **0 of 27** pre-existing archived proposals carried a `— RESOLVED` heading. Worse, the two ISSUE lifecycle docs contradict each other — `issues/archived/README.md` omits the index step and adds "don't resurrect archived files in place", which `issues/README.md` directly contradicts by recording and endorsing exactly such a restore at v6.5.19. **That doc-vs-doc contradiction is a live maintainer decision.** Prior row (stale): **12** issues · **3** proposals · **316** archived issues + **27** archived proposals — all four **DERIVED 2026-08-11**, never incremented (`ls docs/development/issues/*.md | grep -vc README`; `find docs/development/issues/archived -name '*.md' ! -name README.md | wc -l`). ⚠ Earlier revisions of this row read "11 · 3 · 318" and "10 · 2 · 304"; the 318 counted `archived/README.md` as an issue. **Treat any number here you did not just derive as stale.** **Net at the 2026-08-11 re-triage: +1 open (11 → 12), 0 archived.** ⛔ The +1 is a **restore, not a filing**: `2026-06-28-bare-metal-forbidden-module-check-unbuilt` was bulk-renamed into `archived/` on 2026-07-10 **with no resolution banner, for work never built** — premise re-checked live (`grep -rn 'host_only|kernel_ok' src cbt lib` → no implementation) — while `roadmap.md` simultaneously named it a live fold-in. Archiving asserts done-ness, so an unbuilt requirement in `archived/` is hidden from whoever opens the slot. **Nothing was archived**: nothing in the open queue is fully shipped. ⭐ `2026-08-05-cross-os-full-corpus-23-failures-on-ecb` is **largely shipped and was RETITLED + RESCOPED rather than archived** — a real `CYRIUS_CROSS_OS_FULL=1` run on all four hosts, one at a time, gives **ecb 269/0 · ach 269/0 · pi 269/0 · cass 262/7**; the ecb leg was run twice independently. state.md's standing instruction to settle it with a real run before archiving is **answered**. It stays open because it gates a live decision (the default flip), not a bug. **Both `.20` filings are pinned and now carry `**Placement:**` lines** — 3 of 11 open issues failed the README's own "every row must read 1 1" self-check, including both `.20` filings; all 12 pass now, and the rule was extended to `proposals/` (all 3 lacked it, which is exactly how the version-constant proposal lapsed two pins unnoticed). |
| **W1 status** | ✅ **CONSUMED — all 13 spent (.4 → .16).** ⛔ And **W2 is consumed too**: budgeted 3 at `.19`–`.21`, but `.19` shipped as five consumer filings and `.20`/`.21` are now Slots 0/0b. **W1 (13) + W2 (3) = 16 reactive releases of budget; `.4`–`.19` is exactly 16 releases of reactive spend.** Items 8 and 9 never landed and are re-homed to `.22` (Slot 1b). ⚖️ **Maintainer budget call surfaced in roadmap.md**: the observed reactive rate is ~80 % (16 of 20 releases) against W3+W4's remaining 9, so either widen the windows or let filings displace releases explicitly rather than silently. |
| **Owed to the maintainer** | **FIVE decisions open** (2026-08-11 re-triage; a sixth — `break` semantics — was SETTLED 2026-08-12: "break should leave — expected", C semantics confirmed, recorded in the guide + vidya + CHANGELOG, do NOT re-open). **1. Slot 9 design** — the committed "unbox the scalar case" was implemented at `.15` and **reverted**; both storage relocations are disproven, so the survivors are **escape analysis or a scope-tied arena with reclaim**, a different size class. The slot cannot be scoped until this is chosen. **2. Cross-OS full-corpus default flip** — flip for ecb/ach/pi now and hold the selector for cass, or hold all four behind the cass 7. **3. Reactive budget** — ~80 % observed vs W3+W4's 9 remaining. **4. `.21` packing** — confirm pulling the `output_buf` reclaim forward out of the closeout band to share the `input_buf` two-step bootstrap. **5. Per-item `private`** (roadmap open question 3) — `private fn h()` still compiles with **no diagnostic** and privatises the whole file including `main`; nine releases live. **6. `archived/README.md` index covers 40 of 317** — every 6.5.x-era archive is unindexed; backfill from here, or drop the all-files promise in `issues/README.md`. Plus the two standing answered questions — the **self_compile budget** and **committed item 5** (growth-tax audit) — which are now sharper: self_compile is **685 ms** against a candidate ≤700 ms cap, and cycc **1,154,816 B** against ≤1.20 MB, with Slots 3/5/6 all still to land. |

> Full arc detail, the 12-slot sequence and per-arc length estimates live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
