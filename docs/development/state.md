# Cyrius — Current State

> Refreshed every release. CLAUDE.md is preferences/process/procedures (durable);
> this file is **state** (volatile). Bumped via `version-bump.sh` post-hook.
>
> **Consolidated 2026-06-08 (v6.1.4):** the per-patch session-close log
> (104 entries, ~5,600 lines back to v5.x) + the stale v6.0.4-frozen
> structured sections were pruned — that detail is canonical in
> [`CHANGELOG.md`](../../CHANGELOG.md) (per-patch) and
> [`completed-phases.md`](completed-phases.md) (arc retrospective). This file
> now holds only the **active cycle** + current state.

## Current state

| | |
|---|---|
| **Version** | **6.3.7** (v6.3.x cycle — **Language Refinements**; **non-capturing closures `|x| body` actually work** — slot-entry premise-check found them a SCAFFOLDED-BUT-NON-FUNCTIONAL, untested, unused feature (corpus 'closure' hits were all `|x|` math abs-value; parse code existed but mis-emitted), NOT 'working closures missing capture.' Planned base-first split (user 2026-06-29): this slot fixes the base, **lexical capture = v6.3.8 follow-up**. FIVE codegen bugs, all from the closure being an anon fn emitted INLINE in the enclosing fn while sharing its mutable state: (1) PARAM PASSING (`|x| x`→0) — inherited `_cur_fn_regalloc` → ESTOREPARM shifted params past phantom callee-saved slots + body read stale regs; fix=save/reset/restore all `_cur_fn_*`; (2) FRAME never EPATCHFRAME'd (ESUBRSP placeholder stayed 0 → 0-byte frame clobbered param slot 0 on first push; v6.2.41 'output-identical' claim was untested); (3) zero-param `||` lexed as logical-or, never reached the parser; (4) enclosing-local CLOBBERING — closure reused the SHARED local tables (names/depths/types/slice `S+0x5D9D000`+) from slot 0, overwriting the enclosing fn's locals; fix=snapshot/restore the 4 tables; (5) regalloc post-pass byte-scan REWROTE the inline closure → SIGILL; fix=`_cur_fn_has_closure` skips the post-pass. + unique `__clNNNNN` naming (was single-digit → 11th closure aliased 1st in REGFN). **`tests/tcyr/closures.tcyr`** = first-ever closure tests (8 cases). Guide Closures section + vidya `closure_inline_emit_shares_enclosing_fn_state`. **Self-host byte-identical (closure-path-only; differential vs 6.3.6 confirms cycc unaffected); seed→cybs→cycc OK; check.sh 104/104; closures run on x86 + aarch64(qemu) + PE(wine); ecb+cass+pi SELFHOST_OK; bench 501 ms; cycc 1,079,440→1,081,696 B (+2,256).** Roadmap: v6.3.7=base closures, inserted v6.3.8=lexical capture, slots shift (generics→.9, async/float→.10, gates→.11-13, closeout→.14). See CHANGELOG [6.3.7].) PRIOR: **6.3.6** (v6.3.x cycle — **Language Refinements**; **ecosystem refold + AGNOS `sys_symlink` peer** — a LIB-ONLY maintenance cut, **cycc self-host byte-identical** (only the version string moved). Four stale vendored stdlib folds brought current from upstream (each reproducible byte-identical via `cyrius distlib`): **sigil 3.9.4→3.9.7** (SECURITY — concurrent-TLS-handshake race killed via 64-lane per-thread crypto banking; streaming Poly1305 `poly1305_init/_update/_finalize`; `ecdsa_p256/p384_warm` prewarm prims; per-lane RSA residue zeroize; `.bss` grows ~14 MB lazy), **patra 1.12.6→1.12.7** (per-handle tail-page cache; handle 64→88 B; internal `tbl_insert/5→/6`), **sandhi 1.6.13→1.7.0** (h2-promote IPv6 arity fix + doc/test), **vani 0.9.5→0.9.6** (pin bumps + chrono include). Plus **`sys_symlink` + `SYS_SYMLINK=63`** in `lib/syscalls_x86_64_agnos.cyr` (mirrors `sys_link`#32's 4-arg a4=r10 ABI; agnos kernel `symlink`#63 @ 1.51.0; `target`=link TEXT 1..4096 B, not a resolved path) — unblocks `--agnos` symlink creation (ark M3 `.so→.so.N` install, agnova, kriya `ln -s`); verified agnos-target resolve + emits syscall #63. api-surface snapshot regenerated (additions-only: 5 sigil crypto + `sys_symlink/4` + internal patra arity — NO consumer-facing removals). **Self-host + seed→cybs→cycc byte-identical; check.sh 104/104; ecb+cass+pi SELFHOST_OK; bench self_compile 502 ms (flat); cycc 1,079,440 B (unchanged — lib-only).** Roadmap: language slots shifted (closures→.7, generics→.8, async/float→.9) + 3 gate slots appended at the v6.3.x back (W^X cyrld split, str_builder concurrent-corruption, var-decl codegen pair). on-agnos symlink round-trip = downstream ark M3 exerciser. See CHANGELOG [6.3.6].) PRIOR: **6.3.5** (v6.3.x cycle — **Language Refinements**; **Phase-0 language substrate: CO-01 forward-call ABI prescan + AR-01 monomorphization substrate (gated proof)** — the two generics prerequisites from the 2026-06-10 deep-dive. **CO-01:** a call lexically BEFORE its callee's def used to auto-register mask 0 (silent drop of `: Str` coercion / SIMD routing / struct-return retptr ABI). New **pass-1 fn-signature prescan** (`_prescan_fn_sig`, all 7 forks, replaces the inline fn-skip) registers every top-level fn + its param masks/count/variadic/return-type BEFORE pass-2 emits calls → forward calls resolve the same ABI as backward calls (structs/globals were already pass-1 order-independent; this extends it to fn sigs). Shared `_classify_param_type`/`_classify_return_type` (logic-preserving refactors, differential byte-identical) keep prescan↔def in lockstep; prescan extracts on a throwaway cursor then resets + replays the verbatim old skip (net cursor unchanged). Covers Str/cstring/Result/Option/Tagged/SIMD param masks + `: Struct` return with explicit `var p: T = mk()`; reaches decls before the first top-level statement (pass-1 stops there = decls-first). GOTCHA: `_prescan_params` must consume `)` or struct-ret never classifies (mask 0 → segfault). `tests/tcyr/forward_call_abi.tcyr` passes new / fails old. Filed `2026-06-28-inferred-struct-local-from-call-segfaults.md` (P2 — `var p = mk()` INFERRED type segfaults even non-forward; pre-existing var-decl/retptr bug, NOT CO-01). **AR-01:** the emit-time replay path (PARSE_FNCALL `GFINL!=0`) was always INTACT; only the body-token capture was off (`_INLINE_OK=0` per backend — x86 SIZE v1.11.3, aarch64 metadata overlap since relocated). New opt-in `_MONOMORPH_OK` (`util.cyr`, default 0; `CYRIUS_MONOMORPH=1` in all 7 drivers) ORs into the capture gates → flag OFF byte-identical codegen, flag ON fires the intact value-replay. `_monomorph_substrate_gate` (check.sh 103→104) compiles `tests/fixtures/monomorph/inline_proof.cyr` ±flag: both exit 42 (byte-correct) + binaries DIFFER (active); confirmed on REAL pi aarch64 (old aarch64 capture corruption gone). Deferred v6.3.7: TYPE substitution (generics) + forward-ref + past the ≤2-param/≤16-token ceiling. **Self-host fixpoint + seed→cybs→cycc byte-identical; check.sh 104/104; tcyr 195/195; every behavior change differential-verified byte-identical (flag off); ecb+cass+pi SELFHOST_OK (real hw) + qemu/wine/cxvm; bench self_compile 509 ms (flat, 500–549 band); cycc 1,077,136→1,079,440 B (+2,304).** See CHANGELOG [6.3.5].) **Earlier releases (6.3.5 back to v5.x): see CHANGELOG.md** — trimmed from here 2026-06-29 (state.md keeps current + ~2 priors; the rest is canonical in CHANGELOG). |
