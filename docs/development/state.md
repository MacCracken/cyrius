# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.4.65** — **Repair the oldest open issue (thread-local slot namespace has no allocator) + fold the pending stdlib updates.** Added `thread_local_alloc()` — a process-global slot counter **based at 16**, so allocator-managed lib slots (≥16) can NEVER alias the frozen 0-15 app/legacy range; the v6.3.25 hardcoded-slot collision class (sigil↔patra RECORD_LAYER_FAILURE) is now closed **structurally**, not by convention. sigil (3.12.1) + patra (1.12.12) migrated off their hardcoded literals AT SOURCE, each claiming its slot(s) once via a CAS-gated ensure helper; re-vendored. `TLOCAL_MAX_SLOTS` 16→128, Windows per-thread block 128→1024 B (macOS/agnos/foreign arrays already held 128 slots — a top-level `var[N]` is N*8 B since v6.4.10; the old `16*8` comment was stale). **Premise-corrected:** sandhi's `_SANDHI_RPC_POLICY_SLOT=16` is a record STRIDE, not a TLS slot — the issue's own note (and the registry comment) were WRONG; removed. sandhi needed no change. Also folded (byte-identical from each committed dist): bayan 1.1.0→1.2.0 (YAML sublib), mabda 4.0.5→4.0.7, niyama 1.0.5→1.0.6, yantra 1.0.0→1.0.1 — 0 removed public fns, no arity changes, no collisions, no dep drift. New gate `tests/tcyr/vr01_tls_alloc.tcyr` (real HW: allocator hands out unique slots ≥16 + slot-100 round-trip through the grown backing + collision-free across the 0-15/16+ boundary); rewrote `tests/thread_local_slot_collision.sh` to assert the allocator invariant (both libs allocate; no hardcoded low slot survives). cycc **byte-identical to the 6.4.64 tag** (lib-only, no `src/` change — thread_local.cyr + every folded dep are outside cycc's closure; pre-bump sha `d6f1c76d`, post-bump only the version string differs → `9bfe2056`, 1,103,568 B). Release gate GREEN (self-host fixpoint + seed-derive + check.sh **147** + cross-OS ecb/ach/cass/pi with `vr01_tls_alloc` LIBTEST_OK on each — the grown Windows block + 128-slot arrays exercised where they live); self_compile **618 ms**; api-surface 4653→4667 (+14: 12 bayan YAML + mabda nv + `thread_local_alloc`, 0 removed). Follow-up NOTED (not blocking): hoosh `TRACE_SLOT=1` (a downstream tool, not a vendored stdlib) should adopt `thread_local_alloc()` too. Resolution/post-mortem: `issues/archived/2026-07-01-thread-local-slot-namespace-no-allocator.md`. Prior: **6.4.64** — **THREE silent codegen/CLI bugs, all found by pushing on a rule instead of trusting it; plus sandhi 1.9.0.** **(1) P0 — Win64 calls with ≥10 args SILENTLY corrupted argument 1 and never wrote the stack args.** **(2) P1 — `getpeername`/`getsockname` reached the WRONG SYSCALL off x86-Linux**: they were missing from ESYSXLAT/EMACHO_SYSXLAT, and an untranslated number does not error — it invokes a different syscall. On aarch64, 52 became **`fchmod(fd, mode)`** with `mode` = the low bits of the sockaddr POINTER, and **VERIFIED on real pi it RETURNED SUCCESS** — so a caller believes it got a peer address while its buffer is untouched garbage (worse than an error). Darwin (arm64+Intel) hit sigpending/setpriority; Windows was never dangerous (unrouted → -ENOSYS, never a raw `0F 05`) but the reroute is still missing → FILED. **The ESYSXLAT fix was WRONG and CI caught it** — x86-52 collides with the aarch64 peer's own `SYS_FCHMOD = 52`, and the chain is cmp/b.ne/movz with NO early exit, so the entry remapped **fchmod → getpeername** (`sandbox_syscalls` RED on aarch64-native; a real tested syscall broken to fix an untested one). REAL FIX = the wrapper cyrius never had: per-arch `SYS_GETPEERNAME`/`sys_getpeername` (x86 52/51 · aarch64 **205/204** native · macOS x86+EMACHO→BSD 31/32 · macho dual-map **205→31/204→32** for macOS-arm64, same shape as the existing socket 198→97 · Windows **fail-closed -ENOSYS**, NOT a `return 0` stub — that would claim success with an unwritten buffer, cf. the v6.3.42 sys_mkdir/sys_unlink stubs VR-01 caught). Gate `vr01_getpeername_xlat.tcyr` PASS on real pi/ecb/ach. **DON'T re-attempt the ESYSXLAT entry** — CLAUDE.md already names this collision class and emit.cyr:698 flags it for fchmodat/53. sandhi 1.9.1 must adopt the wrapper (FILED); until then its peer API fabricates on aarch64/macOS-arm64. It hid because **cyrius's own net.cyr calls neither** — no consumer until sandhi 1.9.0. **GATE GAP — FILED, follow-up** (`2026-07-14-release-gate-cross-os-runs-only-vr01-glob.md`): the release gate's cross-OS leg runs only the `vr01_` glob (~27), but CI's aarch64-native job runs the FULL 248-tcyr corpus on hardware — which is exactly why the fchmod regression passed the gate GREEN and failed CI. A green release gate is NOT a green CI. **(3) `--check-lib-sync` shipped in .63 advertised-but-REJECTED** — the pre-scan set the flag but `build`'s arg loop never consumed it; sandhi 1.9.0 found it, and **.63's own gate passed it for the WRONG REASON** (it only tested the drift path, where the check exits before the parser runs, and asserted merely "exit != 0" — which a rejected flag also produces). **(4) sandhi 1.8.2→1.9.0 folded** — peer-address API for per-IP rate limiting, filed by yeo-cy-test (Argon2id /api/login at ~244 ms/attempt = an amplification lever). +6 fns, 0 removed, 0 arity changes, no collisions, no dep drift, zero in-repo includers → **fold is cycc-neutral** (the FIX is not: x86/emit.cyr is in 6 of 7 forks → seed-derive mandatory, ran GREEN). **Bonus:** sandhi already had **11 fns at ≥10 args** (max 14, `_sandhi_http_follow_a`), set-identical in 1.8.2 — its HTTP client was silently miscompiled on Win64 all along; (1) repairs it. New gates: `vr01_win64_stack_args.tcyr` (mutation-tested on cass: old 2/3 fail, new 5/0) + `vr01_getpeername_xlat.tcyr` (PASS pi/ecb/ach; mutation → LIBTEST_FAIL on pi). **THE DURABLE LESSON:** CLAUDE.md carried *"fns take ≤6 args cleanly; args 7+ have shown corruption — restructure instead"* — **a codegen bug written down as a language rule.** Wrong in BOTH directions (7-9 always worked; the cliff was 10, on ONE target), never re-measured because it read as settled, and on 2026-07-14 it was cited to file an issue **against sigil** asking a stdlib to contort around a defect in the compiler that compiles it — in the LANGUAGE repo. The user caught it (furious, rightly). Rule replaced with the measured behavior + **"if a rule here tells you to work around codegen, treat the rule as the bug report."** Same family: sandhi's comment CLAIMED the Mach-O backends translate getpeername "the same way net.cyr's socket band is" — they didn't; believing it would have shipped a fabricated-peer-IP security primitive. Release gate GREEN (seed-derive + ecb/ach/cass/pi); check.sh 147; cycc 1,103,568 B (the aarch64 table edit does not touch cycc's own bytes — it compiles from main.cyr, x86 backend only; sha `d6f1c76d`). Post-mortem: `issues/archived/2026-07-14-win64-stack-args-corrupt-at-10-plus.md`. Prior: **6.4.63 — Two consumer filings + the stdlib fold-in.** `ECALLPOPS`'s `_TARGET_PE` branch reversed the stack args (Win64 wants a5 at the LOWEST slot; the push stack has it highest) by shuttling them through a **fixed 5-register table** (`r10/r11/r14/r15/r12`) with `if (nextra >= 1..5)` pops + `if (nextra == 1..5)` write-backs. `nextra = n-4`, so **n≥10 fell off the end of its own table TWICE**: only 5 of ≥6 extras were popped → the 4 following reg-arg pops read SHIFTED slots → **rcx (arg 1) wrong**; AND no `nextra ==` branch matched → **stack args never written** → callee read garbage. Both SILENT, on a Tier-1 target. Measured on real cass: **arity 5-9 OK, 10/11/12 → arg 1 corrupted**. SysV/aarch64 fine at every arity (5-20) — SysV was migrated off the register shuttle to rsp-relative addressing at v5.6.24/v6.0.57 for THIS EXACT bug class; **the Win64 branch was never brought along.** Fix mirrors SysV, uniform for all n>4 (no per-nextra branches to run out of): load rcx/rdx/r8/r9 in place via `_emov_rsp_disp`; carve the frame BELOW the pushed args so the reversed copy can't overlap at any arity (an in-place reversal can't be done by any single ascending/descending shift); reverse-copy via **r10 (caller-saved** — v5.6.24's lesson: r12-r15 are callee-saved and regalloc pins caller locals there); new `_pe_call_frame(n)` as the ONE shared ECALLPOPS/ECALLCLEAN formula (32 shadow + nextra*8, 16-rounded, +8 parity pad for odd n — the old path got alignment free by popping all n slots first); new `_esub_rsp_imm`/`_eadd_rsp_imm` (a 17-arg call needs a 136 B frame, past imm8); ECALLCLEAN reclaims `frame + n*8`. **Live consumer:** sigil 3.12.0 (folded in .63) — `argon2id_into`=10 args, `argon2_hash_into`=17; a KDF returning a wrong-but-plausible hash is the worst shape of this. **Verified on REAL hardware:** cass arity **5-20 all correct** (was 10+ broken) · **cycc.exe SELFHOST_OK on real Windows** (the compiler is itself full of >4-arg calls — the load-bearing test) · x86 self-host byte-identical · seed-derive OK · release gate GREEN on ecb/ach/cass/pi · self_compile **617 ms** (back inside the historical 614-634 band — retroactively confirming .63's 650 ms was `powersave`-governor state, not code) · cycc 1,103,568 B. New gate `tests/tcyr/vr01_win64_stack_args.tcyr` (arity 5/9/10/17 + nested) runs on real hardware via the gate's `vr01_` glob — **mutation-tested on cass: old codegen 2 passed/3 failed, fixed 5/0**. **Why it survived ~a year (the durable part):** CLAUDE.md carried *"fns take ≤6 args cleanly; args 7+ have shown corruption — restructure instead"* — **a codegen bug written down as a language rule.** Wrong in BOTH directions (7-9 always worked; the cliff was 10, on one target), never re-measured because it read as settled, and on 2026-07-14 it was cited to file an issue **against sigil** asking a stdlib to restructure around a defect in the compiler that compiles it — in the language repo. The gate also structurally couldn't see it: sigil is outside cycc's closure, and cycc's own PE calls stay ≤9 args. Rule replaced with the measured behavior + **"if a rule here tells you to work around codegen, treat the rule as the bug report."** Post-mortem: `issues/archived/2026-07-14-win64-stack-args-corrupt-at-10-plus.md`. Prior: **6.4.63 — Two consumer filings + the stdlib fold-in.** **(1) agnos GPU-compute band `#82`/`#83`** (`lib/syscalls_x86_64_agnos.cyr`): `SYS_GPU_DISPATCH`/`SYS_GPU_DISPATCH_F64` + `sys_gpu_dispatch{,_f64}(a,b,c)` — 8x8 i32/f64 matmul on the AMD Cezanne iGPU driven straight from the agnos kernel (no amdgpu/ROCm); `#83` is deliberately UNFUSED so the GPU result is bit-identical to rosnet's CPU arithmetic. Consumer: `gpumm` 0.2.0. **The safety point:** on Linux `82`=`rename(2)`, `83`=`mkdir(2)`, so an ungated wrapper would hand matmul buffer POINTERS to the kernel as PATHS. The filing asked for an in-function `#ifdef` "exactly as `sys_readdir` does" — but `sys_readdir` has no such guard; the whole peer is included behind `#ifdef CYRIUS_TARGET_AGNOS` in `lib/syscalls.cyr`, which is STRONGER: off-agnos the fns don't exist, so a Linux build fails at COMPILE time (`refusing to emit binary with 2 reachable undefined function(s)`, no binary). No in-function guards added — the peer is wrong in its entirety off-agnos (`SYS_OPEN=7` = Linux `lseek`), so guarding 2 of ~80 would be false comfort. Gate `agnos-crossbuild-gate.sh` §1i, two legs (right numbers on agnos + ABSENT on Linux), mutation-tested. **(2) `./lib/` version skew is NAMED, not whispered** (`cbt/`): yeo-cy-test shipped `sigil 3.9.8` against a `3.11.1` pin — predating sigil 3.9.9's `_SIGIL_CBANK_SLOT` 0->8 fix (filed BY that same consumer) while their patra held TLS slot 0, the documented corruption condition — and the toolchain said only "a shadow exists", true of every project using lib/. Now `warning: ./lib/ shadows version-pinned <snap> — N bundled lib(s) differ:` + `sigil 3.9.8 (pinned: 3.11.1)` + `run cyrius lib sync --full`. Handles BOTH header formats (`# Version: X` distlib ×11, and sakshi's `# Bundled distribution of sakshi vX` — a `# Version: `-only parser would silently skip THE BASE LOGGER). `--check-lib-sync` makes it an error (CI). **Also fixes a SECOND, unfiled defect** found by premise-check: cycc's `_check_shadow_lib` byte-SIZE-compares ONE sentinel (`lib/alloc.cyr`) and returns SILENTLY when it matches — a stale sigil behind an unchanged-size alloc.cyr produced NO note at all (reproduced; NOT what bit yeo-cy-test — their alloc.cyr differed too, which is why they saw the note). **Cross-OS: the check now exists off Linux at all** (cycc's note compiles out on macOS/Windows/agnos-hosted cycc — the `#ifdef` keys on HOST, not emit target); new `_cbt_env_is_1` gives the opt-out the same reach (`lib/io.cyr`'s `getenv` is /proc-only → silently 0 off Linux). Source-repo carve-out uses the NAME-based `[package] name = "cyrius"` signal, NOT `file_exists("src/main.cyr")` — downstream repos (gnoboot/kybernet/yeo-cy-test) also build from src/main.cyr, and that conflation (the v6.0.1 mkdir bug) would have silently disabled the check for every consumer it protects; **caught by the release test, not review**. Gate `tests/lib_freshness.sh` (check.sh 146->**147**), mutation-tested; verified on REAL Linux + ecb + ach + cass. **(3) stdlib fold-in — sigil 3.11.1->3.12.0, patra 1.12.9->1.12.10, mabda 4.0.2->4.0.5**, byte-identical from each sibling's committed dist; all outside cycc's closure. Additive: **+20 public fns, 0 removals, 0 arity changes, no dep drift, no collisions**; `_SIGIL_CBANK_SLOT` stays 8. **patra 1.12.10 fixes a LIVE bug**: 1.12.9 SILENTLY DROPPED an INSERT row (yukti's pattern) — proven 1.12.9 exit 41 vs 1.12.10 exit 0. sigil 3.12.0 = BLAKE2b + Argon2id (RFC 7693/9106), **no cyrius caller** (cyrsign/cyrsign-efi don't touch it) → inert here; its 9-17-arg fns are over the ≤6-arg register-ABI rule and unverified on aarch64/PE/Mach-O — FILED, not shipped as verified. mabda 4.0.5 = JPEG behind `#ifdef MABDA_JPEG`, never defined here. cycc **codegen unchanged** (1,103,568 B, +0; pre-bump bit-identical to the .62 tag; post-bump the only delta is the version string); release gate GREEN (seed-derive + cross-OS ecb/ach/cass/pi); 246/246 tcyr; api-surface 4621->4643 (+22, 0 removals); self_compile 650 ms — **not a regression**: same bytes as the binary that recorded 627 ms, delta is `powersave`-governor clock state (~2.3 GHz vs 4.47 max), nothing to bisect. Prior: **6.4.62 — DX diagnostics Release 2: panic-mode multi-error reporting — cycc reports many errors per compile instead of fail-fast, AND never hangs/crashes on hostile stdin (the fuzz wall that broke 2 prior attempts, now broken).** The 4 central emitters (`ERR`/`ERR_EXPECT`/`ERR_MSG`/`ERRDUPVAR`) print-once-then-return + set `_had_error`/`_panic` (not SYS_EXIT); `_sync_skip` (GTCNT-bounded skip to ;/}/EOF) resyncs; a **`PARSE_STMT` wrapper** (body → `_PARSE_STMT_IMPL`) is the single recovery chokepoint (sync on `_panic` + a guaranteed-progress guard). **THE KEY that unblocked it — a `_had_error`-gated watchdog in `PEEKT`**: after the first error, if GTI stalls >500 000 reads (far above any legal descent depth → never false-fires on valid nested code) cycc aborts cleanly, universally bounding ANY desync spin the per-loop guards miss (the piece attempts 1-2 lacked — the heavy fuzz always found another spinning loop). Output gate ALL forks (inside the 2 `EMITELF` fns + cx write path; all 6 forks' final `SYS_EXIT 0`→`_had_error`; `_had_error` moved to shared util.cyr). Everything inert until the first error → **valid self-compile byte-identical** (fixpoint + seed-derive; all 6 forks build). **Robustness gated by VR-02: `CYCC_FUZZ_ITERS=300` (1500 mutated parses) = 0 crashes/0 hangs.** New gate `tests/dx_multi_error.sh` (check.sh **146**); cross-OS byte-identical ecb/ach/cass/pi; self_compile ~627 ms · cycc 1,103,568 B (+72 B). **Scope:** the ERR_EXPECT (346) syntax class; the 25 inline `SYS_EXIT` errors (undefined-variable etc.) stay fail-fast — filed follow-up. Committed in **bites** (local). Prior: **6.4.61 — consumer-filed net.cyr `sock_accept` per-poll alloc leak + DX multi-error prep.** **net-sock leak** (agnosticos/mishran): a non-blocking accept-poll daemon called `sock_accept` every tick, almost always nothing-pending, and on the no-free bump allocator each poll boxed a fresh 16-byte `Result` (+ a dead peer-addr buffer on Linux/macho) → unbounded leak → NULL-fault (measured **40 B/poll → 4,000,000 B / 100k polls; now 0 B**). Fix A: `accept(NULL,NULL)` (client_addr/addrlen written-never-read). Fix B: a lazy shared **`Err(_NET_EAGAIN)` singleton** for the would-block path (box once, same pointer each poll → 0 steady-state alloc; moved `_NET_EAGAIN`+singleton out of the AGNOS `#ifdef`). Gate `tests/net_accept_no_leak.sh`; net.cyr not in cycc → byte-identical. **DX multi-error prep:** EOF-hardened the 3 unguarded `while(PEEKT!=<term>)` skip-loops (`parse_types:316`/`parse_decl:585`/`parse_fn:2254`) — byte-identical (valid input never hits them) but bounds the desync-crash surface for the recovery core. Full release gate GREEN (check.sh **145**; cross-OS byte-identical ecb/ach/cass/pi; self_compile 619 ms · cycc 1,103,496 B). **The DX multi-error RECOVERY core (emitters print-and-return + `_panic` + `_sync_skip` + output gate + negative corpus) is scheduled for v6.4.62** — a focused complete-in-one release (user's call: it's crash-risk parser work, off the marathon tail). Committed in **bites** (local). Prior: **6.4.60 — DX diagnostics (Release 1 of 2): column numbers + a source-excerpt with a caret on every error.** cycc reported only `error:<source>:LINE: <msg>` (line, no column, no excerpt) + an internal `at fail: fn=…` dump; now `error:<source>:3:5: expected ';', got return` followed by the offending source line + a `^` caret. **All ~452 current-token error sites** gained it for free by routing through one shared emitter. Bite 1: the lexer packs each token's start byte-offset into the **HIGH 32 bits of the existing `tok_lines` word** (line in the low 32) — zero new heap/brk/heap-map change (the token trio is boxed in; a 4th array would force a two-step reloc); `GTLINE` masks `& 0xFFFFFFFF`, new `GTOFF` reads the offset (provably safe: sole writer `ADDTOK`, sole reader `GTLINE`). Bite 2: `_err_col` back-scans `preprocess_out` from the offset to derive the column; `_err_head` prints `<file>:line:col`; routed the 5 emitters + 20 ad-hoc inline prints. Bite 3: `_err_excerpt` slices the offending line + writes a caret; **deleted the two `at fail:` internal dumps** (zero test coverage). Bite 4: `tests/dx_diag_format.sh` gate (check.sh 143→**144**). Column = current parse token (exact for `expected/got`; a few backward-looking diagnostics read one token past — line exact, caret disambiguates). All on error paths → **self-host byte-identical** + seed-derive green (ADDTOK packing kept inline for cybs) + cross-OS byte-identical ecb/ach/cass/pi. **Multi-error (Release 2) deferred+filed** (bounded panic-mode; the dead `_had_error` hook is ready; riskier — desync crash exposure). Committed in **bites** (local). Prior: **6.4.59 — Intel-Mac (x86_64 Mach-O) revival: the usable toolchain + packaging + a first-class release gate.** The x86-macho COMPILER has self-hosted byte-identical on real Intel (`ach`) since v6.0.43, but the toolchain around it was broken for ~2.5 minors — undetected because **ach was never in the release gate** (the exact rot setup). Premise-check at 6.4.58 (on real ach, per the mandate): `cyrius build` → `[aarch64] compiler not found: /root/.cyrius/bin/cycc_aarch64`. Seven bites: (1) **wrapper arch** — `cbt/cyrius.cyr main()` unconditionally `set_arch(ARCH_AARCH64)` for ANY macOS → nested under `#ifdef CYRIUS_ARCH_AARCH64` so x86 keeps ARCH_X86; (2) **wrapper env/HOME** — `_macho_fill_environ` read the vestigial `_macho_init_rsp` (=0, a `main()`-as-gvar-init timing bug) → now reads **r15** via `_macho_argv_base()` (HOME→`$HOME/.cyrius` not `/root`); (3) **cycc `_read_env`** un-stubbed on x86-macho (r15 envp walk → native Intel cycc honors `CYRIUS_*`); (4) retire the now-dead `_macho_init_rsp`/`_macho_capture_args`; (5) **`_lint_macho_buf`** VR-04 structural lint + a Linux `CYRIUS_MACHO=1` cross-emit leg; (6) **release.yml** build-macos → `build-macos-x86-tarball.sh` (ships cycc + wrapper, not formatters-only); (7) **the systemic fix — `ach` added to `release-gate.sh`** (+ exit-42 rot-guard) + a `cyrius audit` ach install gate (peer of ecb/cass). Verified on real ach: `cyrius build` fn-main-42 → `[x86_64] OK` → exit 42 (via install.sh→~/.cyrius AND CYRIUS_HOME); SELFHOST_OK + VR-01 LIBTEST_OK (25); **full release gate GREEN on ecb + ach + cass + pi**. Closes the High issue open 13 months (2026-06-02). cycc x86-ELF/aarch64/PE byte-identical (all macho-gated); build/cycc −40 B (retired capture block). Committed in **bites** (user-directed, local). Prior: **6.4.58 — deferred-item batch: cx correctness + cx cleanups + the Windows atomic-write finish-out** (four P2/P3 residuals from v6.4.54 / v6.4.57 closed together). **cx correctness:** cxvm's `%` was broken for EVERY modulo (divide clobbers the dividend → `%` returned `(n/d)%d`; the filed "`fmt_int` prints `01234`" was really the operator) — `EIDIV` now stashes the dividend in **r13** and `EMOVDR` computes `r0 = r13 % r1`; AND 64-bit immediates dropped bits 48-63 (`CX_MOVI` emitted only 3 of 4 chunks) — added the 4th word via a new **cxvm opcode 253 (movhk)** + decoder. **cx cleanups:** naked-asm 4-byte realign un-gated to cx (`_AARCH64_BACKEND==1 || _TARGET_CX==1` — cx is strictly-4-byte, call targets = `off*4`); the last 3 stale **`0xE92000`** fn-offset users migrated to the canonical `_fnt_offsets`. **Windows finish-out:** `file_create_exclusive` is now atomic on Windows (EOPEN_PE maps `O_CREAT|O_EXCL` mask `0xC0` → **CREATE_NEW**, fails `ERROR_FILE_EXISTS`) + `xunlink` removes on Windows (new **`EDELETEFILEW_PE`**, syscall `0xF035` → kernel32!DeleteFileW, `parse_expr` `61493`, argc==2; return-0 stubs in the cx + aarch64 forks since `parse_expr` calls it in every fork). cx correctness is gated by a new **`_cx_v6458_modulo_immediate_gate`** (compiles inline fixtures with `cycc_cx`, runs under `cxvm`, exit-code oracle) — an exit-42 tcyr can't live in `tests/tcyr/` (every tcyr runs as x86 under the exit-0-on-pass contract AND never exercises the cx fix); the FIRST release-gate run caught the misplaced tcyr (fixpoint+seed green, check.sh RED). `vr01_atomic_write.tcyr` removed its two `CYRIUS_TARGET_WIN` residual gates + added an xunlink round-trip — **23/23 on real cass**. cycc x86 self-hosts **byte-identical** (PIECE 1 cx-only; PIECE 3's PE emitters live in cycc's `.text` but are inert on the self-compile, +4128 B) + seed→cybs→cycc + cross-OS ecb/cass/pi all green; check.sh **143**; self_compile ~613 ms. Committed in **bites** (user-directed, local). Prior: **6.4.57 — portable crash-safe atomic file write (thoth's model edit/create tools) + the scalar-float slot-2 follow-ons.** New `lib/io.cyr` primitives: **file_rename** (portable same-fs rename — POSIX 2-arg / AGNOS 4-arg / **Windows MoveFileExW reroute 0xF034 (EMOVEFILEEX_PE)**, widening both UTF-8 paths to UTF-16 + MOVEFILE_REPLACE_EXISTING), **file_write_atomic** (unique-temp + looped write + fsync + rename, unlink-on-failure — leaves the original intact on a short write), **xfsync** (sys_fsync on Linux/macOS incl. a new SYS_FSYNC=74 + 74→BSD-95 ESYSXLAT on x86-macho + aarch64-macho, sys_sync on AGNOS, no-op on Win), **file_create_exclusive**. Scalar-float follow-ons: f64/f32 **param arithmetic** (SLTYPE-tag `: f64`/`: f32` params F64_TYID/F32_TYID) + float **compound-assign** (`x += y` → EMIT_F*_BINOP) + f64/int **compare-mix warning** with literal-0 suppression + the **ERR_MSG len over-read** fix. Verified `vr01_atomic_write.tcyr` on x86 + **real pi/ecb/cass** (MoveFileExW proven on real Windows; the 74→95 fsync on real macOS) — the real-cass run caught that the test's `/tmp`+`sys_unlink` (POSIX-only) had masked the working reroute; made the gate relative-path + `#ifndef CYRIUS_TARGET_WIN` for two documented Win residuals. cycc self-hosts **byte-identical** (io.cyr not in cycc; the reroute + ESYSXLAT + param/compound-assign code all inert on the self-compile). Committed in **bites** (user-directed, local). Windows residuals filed (O_EXCL→CREATE_NEW, DeleteFileW). Prior: **6.4.56 — f32 scalar arithmetic + comparison + a stricter float typecheck — COMPLETES the scalar-float slot (#2)** (begun in .55 with f64 scalar return). An `f32`-typed value now routes `+−*/` to `EMIT_F32_BINOP` and comparisons to `EF32_CMP`, mirroring the f64 path in single precision: **x86/PE/Mach-O** `movd` + addss/subss/mulss/divss + ucomiss (reusing the EF64_CMP setcc + IEEE-754 NaN parity-fold ladder byte-for-byte); **aarch64** NEON S-regs (fmov s,w + fadd/fsub/fmul/fdiv s0,s0,s1 + fcmp, same cset table incl. the NaN-safe `<=`); **cx** widen-op-narrow (f32widen/f64-op/f32narrow). The f32-local load propagates **F32_TYID** into operator dispatch. **Stricter typecheck (WARN-only** — an error would break the i64-boxed idiom, ADR-002): warns on `f64 + non-f64` (the `x + 1` = `x + <int-1-denormal>` footgun) + two **SESTYPE-normalization bug-fixes** (a `f64_lt(..)` builtin result is now i64-boolean not F64_TYID; a call result is i64 not its last arg's leaked type — also the D1 seam). Deferred+filed: the compare-mix warning (`if (x>0)` false-positives — literal 0's bits are 0.0 — needs literal-0 suppression). cycc self-hosts **byte-identical** (no scalar f32 / f64-arith in cycc → the emitters are present but inert) PLUS ~12 **behavior-preserving** corpus byte-diffs from the SESTYPE fixes (full corpus 243/243 pass — stated honestly, NOT "0 diff"). New gate `vr01_f32_scalar.tcyr` on x86/aarch64/cx + real pi/ecb/cass. Slot #2 COMPLETE. Prior: **6.4.55 — scalar `f64` as a function RETURN type (returned in xmm0 per SysV, sentinel -9)** — the ergonomic unlock so the f64 libraries (naad/hisab/goonj) can read as `fn osc_next_sample(): f64` instead of boxing every double in an i64. cyrius boxes an f64 as its IEEE-754 bit-pattern in the GP accumulator, so marshaling is one move each end: callee `movq xmm0,rax` before the epilogue (EMOVQ_X0_A), caller `movq rax,xmm0` + F64_TYID-tag after the call (EMOVQ_A_X0) so a bare `+−*/`/compare on the call result routes to the f64 emitters (mulsd), not integer ops. Both are real `movq` on x86/PE/Mach-O, **no-op on aarch64/cx** (value already in x0/r0). UNGATED on Win64 (a single double isn't a >8B composite → no retptr, unlike f64v2/f64v4). cycc self-hosts **byte-identical** (cycc declares no `: f64` fns → every new branch inert on the self-compile). New gate `vr01_f64_scalar_return.tcyr` on x86/aarch64/cx + real pi/ecb/cass. First deliverable of the scalar-float slot (#2); **f32 scalar arithmetic+comparison + stricter typecheck → v6.4.56** (maintainer-scoped .55 tight to f64-return). Known limit (filed): a `: f64` fn's bare param arithmetic mis-dispatches (workaround `var y:f64=x`); param-tagging deferred. §1 of the FP-arc issue closed. Prior: **6.4.54 — cx finish-outs (slot #1 cx half): value-form SIMD params/returns + the "var-capture" bug — both filed cx issues MISDIAGNOSED, root-caused by a bytecode-level multi-agent investigation.** (1) The "var-capture" bug = a raw **3-byte x86 DCE stub** (`xor eax,eax;ret`, copied from the x86 forks) emitted on a **strictly-4-byte** VM: each stub shifted following fns off 4-alignment → a later call `(target−cp)/4` truncated onto a `00`=HALT → a lucky stale r0=0 (`return assert_summary()` "passed" only by *also* derailing). Fixed with a 4-aligned cx stub (`movi r0,0;ret`). (2) The "f32v4 value-return SIGSEGV" = the cx **forward-call resolver** (`ftype==2`) reading a stale table (0xE92000, not `_fnt_offsets`), byte-not-4-byte units, and a low-32-bit write that clobbered the call opcode — every forward call derailed. Fixed to mirror ECALLTO; **cx corpus correctness ~doubled (49→99/221 tcyr match x86)**. (3) **value-form SIMD params/returns** via a GP register-pair transport (SIMD ordinal k → r(16+2k) lo/hi; f64v4 = 32B quad) + `CYRIUS_HAS_VAL_SIMD_PARAMS` predefined on cx. **cx-fork-ONLY** — x86/aarch64/PE self-host + seed→cybs→cycc byte-identical; new cx-bytecode regression gate (fwd-call + f32v4/f64v4 value-form, 3/3). check.sh **142**. Follow-ons filed (pre-existing cx gaps DE-MASKED by the fix, NOT bundled): `fmt_int` digit-shift, high-half 64-bit immediates load as 0, naked-asm realign, retire-stale-0xE92000-table. Slot #1 (SIMD + cx finish-out) is now COMPLETE. Prior: **6.4.53 — SIMD value-form finish-outs (slot #1 SIMD half): dup-arg `f(v,v)` tail-call correctness fix + i64v2 packed multiply.** The dup-arg bug's root cause was the **tail-call path** (`PARSE_RETURN`) marshaling value-form vector args through **integer** registers with no XMM/q-reg second pass — a *duplicated* local left the 2nd vector reg **stale** → callee read garbage (distinct args survived by luck). Fix (`parse_fn.cyr`): a tail callee with a non-zero `_fnt_simdmask` is diverted off the tail path to the normal `PARSE_FNCALL` path (which has the XMM/q-reg 2nd pass) on **all targets** — the v6.4.31 guard was `_TARGET_PE`-only; non-SIMD tail calls stay byte-identical (differential 0). **i64v2 packed multiply** (`i64v2_mul`/`_mul_ptr`, the integer-SIMD arc's last unbuilt op): x86/PE/Intel-Mac **pmuludq** synthesis (`a·b = a_lo·b_lo + (a_lo·b_hi + a_hi·b_lo)<<32`), aarch64 NEON **extract/mul/insert** (no `.2d` int mul), cx **native** 64-bit lane mul. **Verified on REAL hardware**: pi + ecb + cass all `SELFHOST_OK` + `LIBTEST_OK (3)` (`vr01_simd_f32v4_neon` dup-arg tail call + `vr01_simd_cx` i64 `iv_mul` + `vr01_simd_ints_neon`); check.sh 141; seed-derive OK; self_compile ~605 ms; cycc size flat. The two **cx** items of slot #1 (cx value-form SIMD + cx `var`-capture) are the clean-split **v6.4.54**. Prior: **6.4.52 — macOS + Windows large-single-allocation path; `output_buf` is now 1 GiB on ALL platforms** (the v6.4.51 carryover). `alloc()` on **macOS** serves a request that won't fit the remaining 256 MiB reserve with its OWN dedicated **unhinted `mmap(0, size)`** (macOS overcommits anon mmap; this replaces the hinted 1 MiB grow loop macOS ignored); on **Windows** with a dedicated **`VirtualAlloc`** (eager `MEM_COMMIT` — a 1 GiB commit succeeds on a normal box, verified cass; `MEM_RESERVE`-only is a future codegen-change optimization). The 6 native drivers drop the .51 16 MiB fallback (a guard hard-errors on true OOM instead of a page-0 SIGSEGV); `_output_cap` = 1 GiB everywhere. **Verified on REAL hardware**: `vr01_alloc_1gib` (alloc 1 GiB + touch both ends) PASSES on **ecb (macOS) + cass (Windows) + pi**; cross-OS `SELFHOST_OK`+`LIBTEST_OK (22)`; check.sh 141; seed-derive OK; self_compile ~603 ms. Prior: **6.4.51 — Three Pin-4 consumer-filed reactive fixes.** (1) **`signal_ignore(SIGPIPE)` + `Signal` enum** (`lib/syscalls.cyr`) + the macOS Mach-O ESYSXLAT `rt_sigaction 134/13 → BSD sigaction 46` translation — closes the sandhi SIGPIPE server-DoS (macOS was High, no workaround); **verified on real ecb (macOS)** via `vr01_signal_ignore` (socketpair SIGPIPE → EPIPE, not process-kill). (2) **`output_buf` 16 MiB→1 GiB ON LINUX** (off-heap `_output_base`; a 19.8 MB single-TU binary now compiles) — **macOS/Windows fall back to the fixed 16 MiB region** because their allocators can't produce a >256 MiB single region (macOS 256 MiB reservation + hint-grow it ignores; Windows fixed no-grow); `_output_cap` tracks the real per-platform buffer, so no crash/regression. The dedicated macOS/Windows large-alloc path is **carried to v6.4.52** ([issue](issues/2026-07-11-macos-windows-large-single-allocation-path.md)). (3) **`lint_error_enum_namespace`** cyrlint note rule — bare `ERR_*` enum members reserved for sakshi (base logger); leaf libs must prefix. cross-OS pi+ecb+cass `SELFHOST_OK`+`LIBTEST_OK (21)`; check.sh 141; seed-derive OK; self_compile ~602 ms. Prior: **6.4.50 — End-of-compile capacity warnings unified across all 7 drivers (Pin 3).** Extracted the inline `warning: var_table / fixup_table / string_data / code buffer at N%` block — which lived only in `main.cyr` + `main_win.cyr` — into one shared `_capacity_warnings(S, codebuf_cap, codebuf_growable)` in `common/util.cyr` and wired every driver, so the 5 previously-silent forks (aarch64 native/cross/macho, x86-macho, cx) now emit the same soft warnings (why `thoth`'s codebuf warning only ever showed on x86). Native forks report vs the 64 MiB growable codebuf (`67108864, 1`); cx vs its fixed 512 KiB `0x54A000` region (`524288, 0`). Logic-preserving: cycc self-host + seed-derive + cross-OS (pi/ecb/cass) byte-identical, differential 0/0 (default + DCE), dead-code floor unchanged, +24 B. **Also folds latest stdlib: sandhi 1.7.3→1.8.2, sakshi 2.4.5→2.4.6, vani 1.1.0→1.1.1 (byte-identical re-vendor from each sibling's committed dist; no compiler include → cycc unaffected).** Prior: **6.4.49 — code buffer: 8 MiB initial (off-heap `alloc`) + 8 MiB linear grow steps (was a fixed 3 MiB in-heap region + ×2 doubling); the misleading `code buffer at N%` warning re-based to the real 64 MiB ceiling.** Prompted by `thoth`'s "91% of the ~3.14 MB cap" — the codebuf has GROWN transparently since v6.2.0; cycc codegen byte-identical, +32 B. Prior: **6.4.48 — per-profile `distlib` `.deps` subsetting (a `sha` profile no longer pulls the whole crypto suite's deps) + sigil 3.11.1 refold: UEFI Secure Boot ENROLLMENT (`.esl`/`.auth` — arc #3 residual B closed).** Prior: **6.4.47 — `cyrius sign-efi`: native UEFI Secure Boot Authenticode signing** (roadmap arc #3 bite 1 — thin glue over sigil's `authenticode_pe_sign`, via a separate `cyrsign-efi` helper so the 14 MB crypto stays out of the CLI; de-risked UEFI-acceptable on a real gnoboot EFI). Prior: **6.4.46 — new `>>>` arithmetic (sign-preserving) right-shift operator** (the drishti signed-shift fix; `>>` stays logical — reverse of JS/Java, because `>>` logical is load-bearing for crypto rotates + the compiler union flag) **+ stdlib foldin (sigil 3.11.0 + vani 1.1.0) + a doc/hygiene cleanup sweep.** Prior: **6.4.45 — async arc 5b "W" step R3: the overlapped AcceptEx server — the async "W" arc is COMPLETE.** Adds the `listen` ws2_32 reroute (`0xF033`) + a target-agnostic `async_accept(rt, listen_sock)`: Windows via overlapped `AcceptEx` (fetched through `WSAIoctl`/WSAID_ACCEPTEX like R1's ConnectEx, 8-arg callptr, `SO_UPDATE_ACCEPT_CONTEXT` on completion); Linux via the epoll readiness model (park on the listen fd, then `accept`+CLOEXEC — `accept` 43→202 on aarch64 via ESYSXLAT, no compiler change); agnos stubbed. sandhi/daimon can now `accept()` on Windows. The permanent Windows gate grew a self-connecting server leg (`async_accept` races `async_connect` on one reactor). Per-release detail → [CHANGELOG](../../CHANGELOG.md). |
| **cycc** | x86 **1,103,568 B** (+0 — NO `src/` change this release: thread_local.cyr + all six stdlib folds are outside cycc's include closure. Pre-bump the binary was *bit-identical* to the 6.4.64 tag, sha `d6f1c76d…`; post-bump the only byte delta is the embedded version string, `9bfe2056…`, same length → same size. Check the sha, not the size) · self-host fixpoint byte-identical (build/cycc==gen1==gen2) · `build/cycc == cycc(src)` · seed-derive byte-identical (gen2==gen3==build/cycc) · check.sh **147** · self_compile **618 ms** (in the historical 614-634 band) |
| **Bootstrap / cross-OS** | seed (29 KB asm) → cybs → cycc byte-identical · **ecb (macOS-arm64) + ach (Intel-Mac x86-macho, NEW in the gate v6.4.59) + cass (Windows/PE) + pi (aarch64)** `SELFHOST_OK` + **VR-01 LIBTEST_OK on all four** (release-gate GREEN) · **the x86-macho usable toolchain works on real Intel — `cyrius build` fn-main-42 → [x86_64] OK → exit 42 via install.sh→~/.cyrius; ach has an exit-42 rot-guard + a real install gate (v6.4.59)** · **the full Windows IOCP async surface runs end-to-end on real cass** — R1 client (resolve→connect→send→recv) + R2 timers/interval/subprocess/`join_all`/`select` + R3 the AcceptEx server (self-connecting loopback), all folding to RC 42 in `tests/win/async_iocp_pe.cyr` every release · `f32v8_make` all-8-lanes correct on cass · the aarch64 epoll reactor works on pi (v6.4.42) · cx portable-`.cyx` SIMD round-trip on each host's native cxvm · **`>>>` arithmetic shift self-hosts byte-identical on all three (asrv on pi/ecb, sar on cass; v6.4.46)** |
| **Active minor** | **v6.4.x** — ABI / Language-Features (opened at v6.4.0; v6.3.x closed at v6.3.45) |
| **In-flight arc** | **Issue-sweep in progress (oldest-first repairs).** v6.4.65 closed the OLDEST open issue — the thread-local slot namespace had no allocator (filed 2026-07-01) — with `thread_local_alloc()` + the sigil/patra source migrations, and folded the pending stdlib (bayan/mabda/niyama/yantra). Issue ARCHIVED. Next sweep candidates (oldest-first): the 2026-07-02 IR/regalloc + IR3-fixpoint issues (BOUNDED but IR-substrate-gated → v6.5.x), 2026-07-03 macOS-threading, 2026-07-06 SIMD register-residency (all substrate-gated). Prior: v6.4.63 cleared two consumer filings (agnos GPU band `#82`/`#83` + the yeo-cy-test lib-freshness check) and folded the latest stdlib (sigil 3.12.0 / patra 1.12.10 / mabda 4.0.5 — patra fixes a LIVE silent INSERT-row drop). Both filings ARCHIVED; two follow-ups FILED (converge/retire cycc's now-redundant shadow-lib sentinel — a `src/` change, so seed-derive applies; and sigil argon2's 9-17-arg surface, unverified on aarch64/PE/Mach-O — inert here, sigil-side to answer).  Slot #2 (scalar-float) ✅ COMPLETE (.55 f64-return + .56 f32-arith/typecheck); **v6.4.57** cleared the scalar-float slot-2 follow-ons (f64/f32 param arithmetic + float compound-assign + compare-mix warning + ERR_MSG len) **and shipped thoth's portable atomic file write** (file_rename/file_write_atomic/xfsync/file_create_exclusive; Windows MoveFileExW reroute verified on real cass). **v6.4.58** batched four deferred residuals (cx `%`/64-bit-immediate correctness + cx cleanups: naked-asm realign, retire `0xE92000` + the Windows atomic-write finish-out: `file_create_exclusive` O_EXCL→CREATE_NEW + `xunlink` DeleteFileW `0xF035`). **v6.4.59** revived the **Intel-Mac (x86_64 Mach-O)** usable toolchain — wrapper arch/env(HOME) via r15, cycc `_read_env` un-stub, vestigial retirement, `_lint_macho_buf`, packaging, and **ach is now a first-class release gate** (closed the 13-month High issue). **v6.4.60** shipped **DX diagnostics Release 1** (column + source-excerpt + caret); **v6.4.61** fixed the net-sock accept-poll leak + EOF-hardened the parser for multi-error. Next pickup = **DX diagnostics Release 2 (bounded multi-error) = v6.4.62** — the recovery core (emitters print-and-return + `_panic` + `_sync_skip` + output gate + negative corpus), scheduled complete-in-one; then **function visibility `pub`/`private` LAST**. Slot #1 (SIMD + cx finish-out) ✅ COMPLETE (.53 SIMD + .54 cx; the .54 forward-call fix ~doubled cx corpus correctness). Prior arcs COMPLETE: UEFI Secure Boot arc (#3) — signing (`cyrius sign-efi`, v6.4.47) + enrollment (`.esl`/`.auth` via sigil 3.11.1's `efi_sigdb`, v6.4.48). Async arc 5b COMPLETE (R1 .43 / R2 .44 / R3 .45).** The IOCP-Windows "W" step is done: client (resolve/connect/send/recv) + timers (with_timeout/interval) + subprocess (spawn/run_process) + combinators (join_all/select) + the AcceptEx server (async_accept), all target-agnostic and release-gated on real cass. Foundation + the 5 tokio-parity gaps (.33–.41) + consolidation (.42) preceded it. Arc was FOUNDATION-FIRST; cx SIMD (.32) + Win64 value-form SIMD (.31) also COMPLETE. Two pre-existing Win64 ABI bugs fixed along the way (.44): PE-reroute 16-align, retptr deep-stack-param homing. |
| **Next up** | **Immediate pickup = function visibility (`pub`/`private`) — the LAST arc of the minor — OR the DX multi-error inline-error follow-up (smaller).** **Pre-pickup note (2026-07-14):** the `pub`/`private` PROPOSAL is STALE — its header still reads "No decisions committed" and lists 4 open decisions, but roadmap.md:700-706 already ran the arc-open `_`-audit and FORCED the outcome (marker = **hybrid**, default = **PUBLIC**; the "derive-from-`_` = zero-churn" idea is DISPROVED — 165 `_`-fns are called cross-file). Read the roadmap, not the proposal. The ONE genuinely open design question is decision #2 (**boundary: file vs dep**), and roadmap.md contradicts ITSELF on it — Phase 1 commits to a per-fn **file**-id substrate as "the real work" while the Risks bullet says subsystem-spanning `_` helpers mean "file=module is too fine a unit". Settle that BEFORE Phase 1; picking wrong burns the most expensive phase. Also note `pub` is lexer token 73 but DEAD (consumed-and-ignored at `parse_fn.cyr:1570`) — and so is `shared`, so "pub is already wired" overstates the substrate. The **maintainer-corrected order** (2026-07-11 re-pin, `feedback_no_codegen_parking_in_v7`): slot #1 ✅ (.53/.54) → scalar-float slot #2 ✅ (.55/.56 + .57) → **Intel-Mac revival ✅ (v6.4.59)** → **DX diagnostics R1 ✅ (v6.4.60: column + excerpt)** → **DX R2 ✅ (v6.4.62: panic-mode multi-error + the PEEKT anti-hang watchdog)** → **function visibility (`pub`/`private`) LAST**. DX follow-up (non-blocking, [filed](issues/2026-07-12-dx-multi-error-reporting.md)): convert the 25 inline `SYS_EXIT` errors (undefined-variable etc.) to multi-report + a smarter `_sync_skip`. 6.4.x is a LARGE, ongoing minor (~45–99 releases; plenty remains after .53). NOTHING codegen ever parks to 7.x (7.x = language book + legal only). `tantu` extraction is a **reserved future-minor** deliverable (repo name held) — NOT next, do not sequence it. Filed follow-ons: single-waiter-per-fd multiplex; mid-body suspend substrate = FUTURE ARC (v6.5.x IR substrate + no consumer); `_cur_fn_ret_stash` disp↔idx substrate. thoth `--win` end-to-end = post-release downstream re-vendor (sandhi re-vendors `async` from .45). |
| **Committed after** | **cx finish-out (.54) → scalar-float completion → Intel-Mac (x86_64 Mach-O) revival → DX diagnostics → function visibility (`pub`/`private`) LAST.** **v6.5.x = perf-quality** (IR/regalloc substrate + passes; SIMD register-residency is IR-substrate-gated there) · **v6.6.x = language ergonomics** · **macOS-arm64 threading → v6.5.x** · **RISC-V → v6.7/v6.8**. Full slot table + the pinned deferral order in [roadmap.md](roadmap.md) "Deferral backlog — pinned order". |

> Full arc detail, per-arc length estimates, and the Phase-5 5a/5b plan live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
