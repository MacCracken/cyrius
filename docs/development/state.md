# Cyrius — Current State

> Refreshed every release. CLAUDE.md is preferences/process/procedures (durable);
> this file is **state** (volatile). Bumped via `version-bump.sh` post-hook.
>
> **Consolidated 2026-06-08 (v6.1.4):** the per-patch session-close log
> (104 entries, ~5,600 lines back to v5.x) + the stale v6.0.4-frozen
> structured sections were pruned — that detail is canonical in
> [`CHANGELOG.md`](../../CHANGELOG.md) (per-patch) and
> [`completed-phases.md`](completed-phases.md) (arc retrospective). This file
> now holds only the **active cycle** + current state.

## Current state

> **v6.3.x EXPANSION in effect (user 2026-06-30).** v6.3.x does NOT close at .16 — the
> whole v6.4.x ABI/Perf arc + the 2026-06-10 governance cluster (minus LEGAL-01, deferred
> to public release) are pulled into v6.3.x; Intel-Mac arc → v6.4.x tail (moved 2026-07-03, was v6.3.x tail); v6.4.x
> OPENING SEQUENCE committed 2026-07-03 (integer SIMD first for ML/AI → array-typed struct fields → UEFI Secure Boot signing (gnoboot) → pub/private — roadmap_6.md). Sequence: perf-prereqs (bench un-blind .17, differential gate
> .20) → governance body (.21–.23) → **consumer-filed bug fixes (.24–.25: codegen+agnos pack, multi-worker TLS)** → FFI + perf arc (.26–.30) → deps/language/lib
> pull-ins (**Phase D** .31 = the modularity-arc lever-1 completion / generics tail .32 /
> protobuf .33) → Intel-Mac (.34–.35) → VR-01 (.36) → closeout. **.18 and .19 were consumer-filed stdlib pull-ins** (the AGNOS base-stack
> migration to 6.3.15): .18 the undersized-array hardening sweep, .19 the ws_server header
> rename + agnos `sys_fstat` peer (the differential gate + all downstream slots shifted +1).
> See [roadmap.md](roadmap.md).

| | |
|---|---|
| **Version** | **6.4.0** (v6.4.x cycle OPENS — **`CYRIUS_MONOMORPH` default-on flip: generics are now on by default; `CYRIUS_MONOMORPH=0` is the opt-out**. The generics feature was verified correct behind the flag at v6.3.39; this de-risking slot FLIPPED the default. **All 4 pinned flip criteria met:** flag-on differential **status-diff=0 / codegen-diff=0** over the 322-input corpus (byte-identical for EVERY existing program, both default+DCE modes); flag-on cycc self-host byte-identical fixpoint; flag-on cross-OS on **ecb+cass+pi** real HW (SELFHOST_OK+LIBTEST_OK); two-step + seed→cybs→cycc byte-id. **The flip was NOT a one-liner — two prerequisites (why it was a dedicated slot):** **(1) DECOUPLE** monomorph from GENERAL inlining — `_MONOMORPH_OK` shared the inline-capture gate with `_INLINE_OK`, so flipping on re-enabled general fn inlining for every program (the v1.11.3 x86 SIZE regression, measured **+25.6%** on flag-on cycc: 1.02→1.29 MB). Gated the capture (SFBS+SFBE) to generic fns only: `_INLINE_OK==1 || (_MONOMORPH_OK==1 && GFTP(S,fi)!=0)`. **(2) FRAME-TRIM** — the v6.3.29 broad `_MONOMORPH_OK!=0` blanket disabled the callee-saved frame-trim for EVERY fn under the flag (a further +16 KB); narrowed to `_MONOMORPH_OK==1 && GFTP(S,fi)!=0` (passing `fi` into `_ra_frame_trim`) — a generic fn's base AND its token-replayed instances share the generic `fi`, so this covers exactly the picker-skipped cases while every non-generic fn still trims. **cycc has zero generic fns → every fn trims → flag-on cycc == flag-off cycc BYTE-FOR-BYTE (1,024,552 B).** All 7 driver forks flipped (util.cyr default 0→1; forks read `=0` as opt-out). Gate semantics updated: generics-gate rejection checks use `CYRIUS_MONOMORPH=0`; the v6.3.5 substrate gate REPURPOSED to prove the decouple (monomorph leaves non-generic fns un-inlined). check.sh 128; self_compile 559 ms; cycc 1,024,552 B. **Generics are now a default-on LANGUAGE FEATURE.** **LESSON: a "flip the default" slot can hide entangled prerequisites (the inline-capture coupling + the frame-trim blanket) — the de-risking criteria (flag-on self-host + differential) exist to surface exactly these; getting to byte-identical needed a PRECISE per-fn signal (GFTP), not a broad flag check.** NEXT: **v6.4.1 = alloc_reset() info-leak fix**, then the v6.4.x opening sequence (integer SIMD → array-typed struct fields → UEFI signing → pub/private). See CHANGELOG [6.4.0].) PRIOR: **6.3.45** (v6.3.x cycle — **CLOSEOUT: the v6.3.x minor closes before v6.4.0**. Ships code (integration tests + gates), not docs-only. **Cross-feature integration** (the v6.3.x language features COMPOSE): `integration_closures_threads.tcyr` (default — closures × per-thread array-locals × concurrency + the .44 single-owner mutex) + gated fixtures `generic_closure.cyr` (CYRIUS_MONOMORPH=1) + `async_closure_generic.cyr` (CYRIUS_ASYNC=1 + CYRIUS_MONOMORPH=1, via a new `_self_host_pipe_env2` two-env helper) + 2 gates → **check.sh 126→128**. **Minor-close judgment audits** (heap-map / dead-code / refactor / code-review / cleanup / security, run as an 8-agent workflow): heap map **135 region entries, 56 live, 0 overlaps**; DCE floor **61 fns / 24,239 B** (all documented false-positives — no removal); **doc-hygiene sweep** = 9 byte-identical heap-map/stale-name comment fixes (0xF7B000 ir_cp FREED, brk 94.6→115 MB, struct_ftypes [16384]→[65536], cc3cx→cycc_cx, cc5_win_linux→cycc_win, gvar_toks 64→4096). Archived the 2 .44-resolved issues → `issues/archived/`; deleted the orphaned `str_to_cstr.cyr` fixture. **Filed** (codegen-touching / needs self-host+differential → v6.4.x-early, NOT a byte-identical closeout): `v6345-closeout-audit-backlog` (5 refactor consolidations of the .44 EMIT_GVAR_INITS parallel-copy class — PARSE_FIELD_LOAD/STORE, EWRITE/EREAD fd-prologue, scalar-name→width ladder, EVLOAD/EFIELD, EMIT_GVAR_INITS; 2 dead-code sweeps; **2 latent bugs** — L1 lex_pp `#ifdef` flag-table 16-slot silent-corruption cap, L2 `_msx` imm8 ≥128 truncation) + `sigil-authenticode-pe-hash-oob-read` (vendored → sigil source / v6.4.x UEFI). **Residual P3s triaged → ALL defer to v6.4.x** (tls13-get-version, bare-metal-forbidden-module-check #4, syscall-write-byte-length-gate, bare-local-array-slot-write-lint). cycc **byte-identical** (1,024,552 B — tests/gates/comments/docs only, no codegen change); fixpoint + seed→cybs→cycc byte-id; ecb+cass+pi SELFHOST_OK + LIBTEST_OK; self_compile 565 ms; vidya minor-close sync. **v6.3.x CLOSES.** NEXT: **v6.4.0 opens** — integer SIMD → array-typed struct fields → UEFI signing → pub/private; + the CYRIUS_MONOMORPH default-on flip. See CHANGELOG [6.3.45].) PRIOR: **6.3.44** (v6.3.x cycle — **untracked-issue sweep: cross-OS + stdlib correctness (.44 of the .44→.45-closeout tail, user 2026-07-03)**. An `issues/*.md`-vs-roadmap cross-ref found 7 untracked; **3 premise-checked as already-resolved** (le8byte-struct-byval-2nd-call → v6.3.39 exits 5; monomorph-inline-instance-clobbers A1 → v6.3.35 exits 37; monomorph-engine-bug-inventory → generics arc closed .39, residual gated → v6.4.0 flip). Three fixes shipped: **(1) single-owner `mutex_*`** — defined in BOTH `thread*.cyr` + `sync*.cyr` → 3 `duplicate fn` warnings on every consumer linking both → `sync.cyr` is the single owner (`thread.cyr` includes it once; 3 `thread*.cyr` drop their copies; include-once dedup); api-surface regen 9 relocations/0 real adds; gate `tests/mutex_single_owner.sh`. **(2) global-scope `Str`-field struct-literal → `unexpected '}'`** — `#derive`-INDEPENDENT (plain struct reproduced it); `EMIT_GVAR_INITS`'s pass-2 replay was a parallel copy of `PARSE_STRUCT_INIT`'s positional loop missing the v5.10.7 `IS_STR_FIELD` fix (flattened the Str field → PCMPE'd a 2nd `len` expr → hit `}`); mirrored the fix; default byte-id (differential codegen-diff=0, the 1 status-diff is the new fail→pass tcyr). **(3) getpid/getppid cross-platform — HEADLINE, found-by-ports: getpid was ALSO broken on aarch64-macOS, not just getppid.** The macho syscall-xlat SOURCE numbers are arch-specific — x86-macho feeds `syscalls_macos.cyr` (39/110), aarch64-macho feeds `syscalls_aarch64_linux.cyr` (172/173); aarch64 getpid(172) ran UNTRANSLATED → returned positive GARBAGE (the .43 callable-only smoke passed on it!), getppid(173) → 0. Fixed aarch64 ESYSXLAT `172→20`+`173→39`, x86 `110→39` (getpid `39→20` pre-existed), Windows getpid `0`-stub → `GetCurrentProcessId` (0xF01C PE reroute + aarch64/cx stubs); `vr01_process_smoke.tcyr` tightened to positive-PID assertions → run-verified on **ecb + cass + pi**. The `.44` premise-check corrected the filing's wrong claim (macOS getpid "already works") — a reasoning-about-the-x86-table-only miss. **macOS thread backend** (P2, `thread_create` no-op on ecb) re-pointed to the v6.4.x Mach-O/Intel-Mac tail. fixpoint + seed→cybs→cycc byte-id; check.sh 125→**126** (mutex gate); ecb+cass+pi SELFHOST_OK + LIBTEST_OK; self_compile 565 ms; cycc 1,024,552 B (+64 — getpid PE reroute + macho getppid xlat, compiler code). **LESSON: hardware ports catch what a green CI check + a disasm-of-x86 never would.** NEXT: **.45 closeout.** See CHANGELOG [6.3.44].) PRIOR: **6.3.43** (v6.3.x cycle — **VR-01 + VR-04 cross-host stdlib verification + foldins (sigil 3.10.0, patra 1.12.8) — the LAST v6.3.x work before closeout**. **Headline: a real compiler bug found by ports — Windows `sys_write`/`sys_read` only ever worked for std handles (fd 0/1/2), NEVER file handles.** `EWRITE_PE` (src/backend/x86/emit.cyr) always `neg;sub`'d the fd into a std-handle index → `GetStdHandle` → WriteFile, so a real CreateFileW handle wrote to garbage. Never caught: no test ever wrote to a FILE on Windows (tests/win/ = dir-list/nanosleep/callptr/dxgi, all stdout). Fixed with an UNSIGNED `cmp rax,2; ja` fd-branch (fd>2 uses the handle directly), mirroring EREAD_PE's v6.0.45 fix. + `sys_mkdir`/`sys_unlink` Windows stubs wired to their existing CreateDirectoryW/DeleteFileW reroutes. **VR-01** = 8 `tests/tcyr/vr01_*` platform tcyr (dispatcher API, exit-0, PE-safe): syscalls_fileops/meta, fs_dirlist, thread_spawn, sync_mutex, alloc_grow, args_smoke, process_smoke — the 8 files zero prior tcyr touched; **Linux + cass 8/8 + ecb 8/8**. **LIBTEST promoted to a STANDING per-host gate** (release-gate.sh passes the `vr01_` glob + requires LIBTEST_OK on ecb/cass/pi). **VR-04** = PE structural lint (`_lint_pe_buf` + local cross-emit); Mach-O lint deferred to the v6.4.x Intel-Mac arc. **Foldins**: sigil 3.9.9→3.10.0 (+17 fns incl. the UEFI Authenticode crypto = P1 of the v6.4.x UEFI arc) + patra 1.12.7→1.12.8 (api-surface neutral); both lib-only → cycc byte-id. **Filed** (found-by-ports, tests `#ifdef`-guarded until fixed): macOS threading broken (thread_create null/no-op — needs a thread_macos.cyr) + getpid cross-platform (Win32 0-stub, macOS wrong BSD number). fixpoint + seed→cybs→cycc byte-id (the EWRITE_PE codegen change is cybs-safe); check.sh 125; ecb+cass+pi SELFHOST_OK + LIBTEST_OK; self_compile 568 ms; cycc 1,024,488 B (unchanged — EWRITE_PE not called on Linux emit). **LESSON: authoring platform tcyr + running on REAL cass/ecb is how you find years-old silent stdlib rot — a green CI check would never have caught Windows file-I/O being std-handle-only.** See CHANGELOG [6.3.43].) PRIOR: **6.3.42** (v6.3.x cycle — **`lib/protobuf.cyr` proto3 wire codec: double/float completion + docs (consumer-filed, hoosh)**. **PREMISE-CHECK, third in a row (.40 codec existed, .41 "256" was really 1024, .42 lib already shipped):** the roadmap said "implement protobuf" but `lib/protobuf.cyr` **already shipped in v6.2.17** (commit `4a107da9`, 2026-06-17 — the day AFTER the slot was granted) — complete (varints, zigzag, 4 wire types, writers, decode+skip), tested, api-surface-registered; the slot was stale (re-added by an audit that missed it). So .42 = the FINISH (user-chosen after AskUserQuestion): `pb_write_double`/`pb_read_double` (f64 ↔ wire-1 fixed64 — an f64 value IS its i64 bit pattern) + `pb_write_float`/`pb_read_float` (f64 ↔ wire-5 fixed32, narrow via `f32_from` / widen via `f32_to`, both native builtins since 6.2.18, no math.cyr include) — closing the `double`/`float` the header advertised but had no helper for (the .40 "advertised-but-no-fn" class); + a guide **Protobuf** section (was undocumented); + 7 round-trip asserts (`protobuf.tcyr` 42→49, incl. 3.5 float bit-exact). Gotcha: the tcyr is ONE `main()` scope + cyrius forbids var redecl → `var dlen` collided with an existing one → `pf_`-prefixed the new names. Lib-only + api-surface tool → **cycc byte-identical**; fixpoint + seed→cybs→cycc byte-identical; ecb+cass+pi SELFHOST_OK; check.sh 125; self_compile 561 ms; cycc 1,024,488 B (unchanged). api-surface snapshot regenerated additive-only (+4 fns, 0 removals, verified `comm -23` empty). **LESSON: premise-check a roadmap entry against the tree — `git log -- <file>` + `git describe --tags --contains <commit>` reveals if it already shipped.** See CHANGELOG [6.3.42].) PRIOR: **6.3.41** (v6.3.x cycle — **raise the initialized-globals per-compilation-unit cap 1024 → 4096 (consumer-filed, thoth) + fold sankoch 2.4.9**. thoth vendors many first-party dist bundles (bote-core/libro/t-ron/avatara/vyakarana/darshana/sit/sankoch) into ONE unit → crossed 1024 top-level non-literal `var …=…` inits (`too many initialized globals (max 1024)`), forcing lean distlib profiles (sankoch `[lib.zlib]`, sit `[lib.read]`) just to fit a consumer's budget. Fix (parse_decl.cyr): the `gvar_toks` deferred-init table relocated `0x198000[8192]=1024` → reclaimed `0x729000[32768]=4096` (the .27 output_buf gap, below TS@0x800000, verified free across ALL targets — no `S+0x…` in `0x729000..0x800000` in any source, checked via both literal offsets AND cached base-pointers). Both cap checks (`>=1024`→`>=4096`) + the `EMIT_GVAR_INITS` replay reader retargeted; `gvar_cnt` stays `0x19A000` (in-place growth was blocked by `jump_target_cnt` at `0x19E000`). **Counting rule** (now in the guide's Global Initializers section): only a NON-literal top-level init (call/ident/expr) takes a `gvar_toks` slot — a bare int-literal init (`var x=42;`) uses the static-init fast path `_vgsi_base`, enum members are const-folded (neither counts); the stale guide "~64 global vars with initializers" line corrected to 4096; the downstream `CLAUDE.md` "256 initialized globals" reconcile (live cap was 1024, not 256) spawned as a separate task (this repo carried no such line). Compile-time scratch offset → **default codegen byte-identical** (differential codegen-diff=0 over 313 inputs BOTH modes; the sole status-diff is the intended new fixture — fails old cap-1024, passes new); heap-map audit clean (99 regions, 0 overlaps — `file_map_str` ends exactly at 0x729000, gap 0). `many_initialized_globals.tcyr` (1100 call-init globals, within the tcyr-suite gate). **+ sankoch 2.4.7→2.4.9 folded** (the "zlib breakout" + CRC slice-by-8; `lib/sankoch.cyr` re-vendored from `~/Repos/sankoch/dist` — 288 public fns, arities identical → api-surface NEUTRAL, cycc byte-identical; cyrfmt/services/bzip2-roundtrip consumers rebuild+pass; snapshot refreshed pre-check to avoid ping-pong). fixpoint + seed→cybs→cycc byte-identical; ecb+cass+pi SELFHOST_OK; check.sh 125; self_compile 561 ms; cycc 1,024,488 B (unchanged — cycc's own source has well under 1024 deferred globals). **LESSON: a heap relocation to raise a table cap needs a band verified free across ALL targets via cached base-pointers too, not just the main.cyr map — nearly picked 0x1FC000, which lex_pp.cyr's #derive field tables use.** See CHANGELOG [6.3.41].) PRIOR: **6.3.40** (v6.3.x cycle — **`#derive(Serialize)` f64 field support** (consumer-filed svara, from the derive-json-codec proposal). **Premise-check win: a codec derive ALREADY exists** — `#derive(Serialize)` emits `Type_to_json`/`Type_from_json`/`Type_from_json_str` (i8/i16/i32/i64/Str/nested) + the typed `json_v_*` DOM the proposal wanted already lives in bayan; building a new `#derive(json)` would DUPLICATE it (user chose "extend Serialize"). The real gap was **f64** — emitted undefined `f64_to_json`/`f64_from_json` → hard "refusing to emit binary with reachable undefined fn" error (same class as the v5.9.35 i64 gap). Fix (lex_pp.cyr PP_DERIVE_SERIALIZE): f64 detection ("f64\0") in ALL 3 codec fns — `_to_json` via `fmt_float_buf` (fmt.cyr) into a shared `_dfb[80]` scratch; `_from_json`/`_from_json_str` via `f64_parse` (**math.cyr — NEW include requirement for f64-deriving structs**; f64_parse reads a byte ptr, so `str_data(v)` / captured token). Fractional `1234.5` round-trips; composes with i64/Str. `derive_serialize_f64.tcyr` (5). **Arrays deferred → v6.4.x (2nd premise-check): array-typed struct fields DON'T EXIST in the language** — `struct { x: T[]; }` is a hard parse error; needs a struct-field-array LANGUAGE feature first (filed roadmap_6.md). derive-json + native-float proposals archived (native-float Tier A shipped). Filed pre-existing P3 (Str field + global-scope codec → "unexpected }"). Default byte-identical (differential status-diff=0 both modes — f64 branches only fire for f64-field structs); fixpoint + seed→cybs→cycc byte-identical; ecb+cass+pi SELFHOST_OK; check.sh 125; self_compile 548 ms; cycc 1,024,488 B (+4448). **LESSON: premise-check a consumer's proposal against the CURRENT codebase — the codec + typed DOM existed, only f64 was the real gap.** See CHANGELOG [6.3.40].) PRIOR: **6.3.39** (v6.3.x cycle — **generics FINALIZE bite 2/2: the deep specialized-instance struct ABI + cross-target monomorph parity + a default-path regalloc fix — the generics arc CLOSES**. Root-caused via a 3-bug workflow (isolated worktrees + adversarial-verify, all CONFIRMED), each fix implemented + verified. **≤8B (DEFAULT-path, x86)** — a ≤8-byte struct passed by value to the same callee twice read 0: the regalloc SAFETY scan (parse_fn.cyr:3496) tested the ModRM byte `== 0x85` (rax) exactly, missing a `lea rN,[rbp+disp]` with N≠rax (a ≤8B struct field-store emits `lea rcx,…`) → the address-taken slot was wrongly register-allocated (memory store never wrote the reg) → reads 0. Fix = also bar regalloc for any `lea`-of-slot via `(modrm & 0xC7)==0x85`. Logic-preserving (differential GREEN); x86-only (NO aarch64 bug — separate regalloc, confirmed). **B-explicit (gated)** — `var m: M<i32> = mk<i32>()` hard-errored; the inferred form already re-derives the receive from the callee's BASE and works, so fix = reset `pscale` for an explicit specialized receiver to re-enter that path (ONE line; the agent's disasm "m.b=0" did NOT reproduce on a clean tree — premise-check agent findings). **B3 (gated, roadmap-flagged HIGH-risk UNPROVEN — LANDED)** — a struct type-arg on a generic fn `wrap<Point>(pt)`: (1) the i64 BASE can't parse `p.x` → SKIP the never-called base body to a `rax=0` stub, detected via a NEW per-fn `_cur_fn_tp_pmask` (bit=param whose declared type is a tparam) + a body scan for `<that-param>.field` (NOT the tparam NAME — the accessed token is the PARAM); (2) the `-3` blanket predated the .36 structmask + this ≤8B fix that made the by-value struct-param ABI sound (a plain `fn wrap_point(p: Point): i64` already works) → relaxed for single-tparam; the instance binds T→struct BEFORE its param loop (cleared after) + sets the address-pass mask from the resolved sid when >8B. Works 8B/16B/24B; multi-tparam struct still rejected. **Cross-target parity** — the v6.3.33 "read `CYRIUS_MONOMORPH` before pass-1" fix was in main.cyr ONLY; moved it before pass-1 in main_aarch64/win/x86_macho.cyr → every generics fixture now runs on aarch64 (qemu → 42) + aarch64 native self-host byte-identical (gen2==gen3). Fixture `generics_struct_abi.cyr` + gate `_generics_struct_abi_gate` (check.sh 124→**125**). **`CYRIUS_MONOMORPH` stays OPT-IN — the default-on flip is PINNED TO v6.4.0 (user 2026-07-03; needs a flag-ON differential status-diff=0 + flag-ON cycc self-host + flag-ON cross-OS first).** fixpoint + seed→cybs→cycc byte-identical; differential status-diff=0 both modes; ecb+cass+pi SELFHOST_OK; self_compile 555 ms; cycc 1,020,040 B. Closes the generics arc (v6.3.9/.10 → .33 → .35 → .36 → .37 → .38 → .39). See CHANGELOG [6.3.39].) PRIOR: **6.3.38** (v6.3.x cycle — **generics FINALIZE bite 1/2: A-broad gated residual + bayan 1.0.4 / mabda 4.0.2 refold** (user-directed split 2026-07-03 — cut A + the folds now, the deep-ABI rest as .39). **A-broad** — an explicit-type-arg 2-arg generic call whose arg1 contains another call clobbered arg0 (`add<i64>(8, add<i64>(16,32))`→64 not 56; `acc = add<i64>(acc, id<i64>(10))` loop→20 not 30): the inline-replay stored arg0 into `saved_flc` but bumped `GFLC` only AFTER the arg loop → a nested inline call reused `saved_flc` as its own param base. Fix = reserve the param slots (`SFLC` + `_flc_hwm`) BEFORE the arg loop (.37's `_flc_hwm` reserved frame SIZE; this reserves the slot INDICES — parse_fn.cyr:1031). Gated (`CYRIUS_MONOMORPH=1`) → default byte-identical. **+ bayan 1.0.3→1.0.4** (TOML `"""` triple-double-quote multi-line strings silently dropped — parser recognized only `'''`; byte-identical `cyrius distlib` re-vendor; 0 new public fns). **+ mabda→4.0.2** (4.0.0 = pure-Cyrius native NVIDIA/nouveau GPU backend behind the v3.0 abstraction, no public-API removal; +43 `mabda::native_nv_*`/`backend_nvidia_new`/`wgpu_adapter_vendor_id` public fns → api-surface snapshot regenerated 4381→4424). fixpoint + seed→cybs→cycc byte-identical; differential status-diff=0 both modes; ecb+cass+pi SELFHOST_OK; check.sh 124; self_compile 559 ms; cycc 1,020,040 B (unchanged — A-broad gated). B3 + B-explicit (the deep specialized-instance struct-return/param ABI) + aarch64 monomorph parity + the ≤8B-struct-by-value-2nd-call default bug → .39. See CHANGELOG [6.3.38].) PRIOR: **6.3.37** (v6.3.x cycle — **default-path correctness batch 3/3: generics-engine repairs (all gated `CYRIUS_MONOMORPH=1` → default byte-identical)**. Premise-check: **A2/A4 already fixed by .35's A1 `_jt_snapshot`** — only A3 survived. Fixed via a 5-agent root-cause workflow + adversarial re-sweep: **A3** — nested explicit type-args clobbered the first binary operand; the inline-replay materializes callee args into enclosing-frame slots but the frame-size patch reads the FINAL slot count → under-reserves → a live runtime `push` aliases the inline param slot. Fix = per-fn frame **high-water** `_flc_hwm` (parse.cyr global; raised at parse_fn.cyr:1060, reset at :2688, applied at :3733). **C1** — a by-value struct-param call as a bare statement mis-parsed (the inline-replay re-registers the param slot by NAME only, dropping its struct SLTYPE → `s.field` mis-parses); fix = exclude struct/Str/Result/Option/Tagged-param fns from the inline classifier. **B1/B2** — a multi-field generic struct return SIGSEGV'd (callee `var p: Pair<T>` mis-instantiated a bogus struct for the unresolved tparam `_gstc==0` → local sid != ret sid → struct-return degraded to scalar-rax) + an i64-explicit / i32-inferred receiver parse-errored (the LHS receive inference + asv retptr/pair consume paths only matched `IDENT (`, not `IDENT < ... > (`); fix = treat unresolved tparam as base (`parse_decl.cyr:1385 _gstc != 0`) + widen 3 receive sites to the generic-call shape. **B3 (struct type-arg on a generic fn) DEFERRED → .38** (base-emission rework — the one HIGH-risk unproven item; user-approved split). A confirm-the-tail-is-dry re-sweep found **2 residual gated cases → .38**: A-broad (explicit-type-arg 2-arg call arg-slot reuse — `_flc_hwm` reserved frame size not slot indices) + B-explicit (non-i64 explicit struct receiver — receive check compares BASE vs SPECIALIZED sid). All byte-identical default (differential status-diff=0); fixpoint + seed→cybs→cycc byte-identical; ecb+cass+pi SELFHOST_OK; check.sh 123→**124** (`_monomorph_repairs_gate`); self_compile 546 ms; cycc 1,020,040 B. Filed pre-existing **≤8-byte struct-by-value 2nd-call reads 0** default bug (repro's on .35). aarch64 monomorph has pre-existing gaps — the feature is x86-validated. See CHANGELOG [6.3.37].) PRIOR: **6.3.36** (v6.3.x cycle — **default-path correctness batch 2/3: struct value semantics + parser + closures**. **[5] >8-byte struct by-value PARAM** SIGSEGV'd — callee derefs a >8B struct param as a pointer, caller passed only the first 8 bytes. Fix = caller ADDRESS-passes, gated on a **NEW per-fn `_fnt_structmask`** (bit N = param N is a plain non-Str struct >8B; reclaims `0xA3A000` from the v6.3.28-relocated `ir_nodes`; populated in both passes via `_param_wants_struct_ptr`) — the reliable discriminator, because cyrius conflates inline-structs & struct-handles (both `SLTYPE=-sid`) and reuses the `-1` sentinel for freed block-locals so NO arg-side heuristic works (a naive fix regressed str_split/toml). Str/Result/handle/scalar params → value-passed; TCO dropped for struct-param callees. **[6]** `var b: P = a` copy-init → inline alloc + full word copy (value semantics). **[3]** `&&`/`||` split into two precedence levels (`&&` tighter — byte-identical, all corpus mixing parenthesized). **[4]** `for i in IDENT..hi` — a `.` before `.` is the range op. **[9]** `var g = f` propagates `CLOSURE_TYID`. Closeout: fixed a stale v6.3.35 fixture (`sub_byte_field_load.cyr` asserted the pre-sign-ext i8 zero-extension → RED on the released .35). Byte-identical on the corpus (differential status-diff=0); fixpoint + seed-derive byte-identical; ecb+cass+pi SELFHOST_OK; check.sh **123**; `struct_byval_param.tcyr` (10); self_compile 553 ms; cycc 1,015,928 B. 9d (closure through i64 param) filed v6.4. See CHANGELOG [6.3.36].) PRIOR: **6.3.35** (v6.3.x cycle — **default-path correctness batch 1/3: sub-i64 LOAD correctness** — first of a 3-way even split of the bug inventory two adversarial discovery sweeps surfaced (.36 = struct/parser/closure; .37 = generics-engine repairs). **Signed sub-i64 sign-extension** (locals + struct fields + call-args, x86 `movsx`/`movsxd` + aarch64 `ldurs*`/`ldrs*`, driven by a negative load width; signedness tracked with NO new heap — a signed local is marked by a negative sentinel in the 0-for-scalars slice-width table, all 3 consumers guard `>0`; a sub-i64 field is always signed since the struct-def only recognizes i8/i16/i32; signedness read off the type-name low byte 'i' vs 'u') — `var a:i32=-1; a<0` now true, unsigned `uN` stays zero-extended. **Typed sub-i64 GLOBAL scalar** LOAD SIGSEGV (x86 `EVLOAD_W` addr in RCX but read `[rax]`) + INIT-returns-0 (shared `_EMIT_GVAR_STATIC_INITS` only wrote `vsize==8`) both fixed, cross-arch. **A1** (generics inline-instance `PARSE_FN_DEF` resets the enclosing fn's jump-target table → LASE mis-eliminates a branch-join load; snapshot/merge fix; gated) landed early (a .37 repair). cycc self-compiles **byte-identical** (no sign-ext-sensitive `iN` in its own source → zero self-host risk); aarch64 qemu + **pi SELFHOST_OK** (real ARM); differential **status-diff=0** (7 codegen-corrections, all sub-i64 users); `subword_signed_load.tcyr` (11) + corrected stale `types.tcyr` assert (`i16=65535`→ signed `-1`); native aarch64 binary regen'd; self_compile 550 ms; cycc 1,015,912 B. DEFERRED narrow: signed sub-i64 *globals* sign-ext (rare) + `*iN` pointer (#7, none exist). See CHANGELOG [6.3.35].) PRIOR: **6.3.34** (v6.3.x cycle — **default-path miscompile fix: a sub-i64 SCALAR local on the LEFT of `+`/`-` was pointer-scaled**. Surfaced by the v6.3.34 monomorph-engine discovery sweep (7 dimensions, each adversarially verified) — which ALSO found 8 gated generics-engine miscompiles (filed as the [`monomorph-engine-bug-inventory`], scheduled **.35 repairs / .36 finalize generics**); the user re-sequenced this default-path bug to be fixed FIRST as .34. `var a: i32 = 40; a + 2` gave **48** (= 40 + 2·sizeof(i32)); `var c: i32=30; var d=40; c + d` → **190**; i16 → scale 2; `a - 2` scaled too. Pre-existing since ≥v5.11.69 (cc5 reproduces), latent because sub-i64 `var` locals rarely sit left of `+`/`-` (i8 scale-1 by luck, i64 scale-0, params + right-operand immune). Root cause: `PARSE_FACTOR`'s local-load set the pointer scale (`SPSC`) to the scalar's byte width `lt` — right only for a pointer-to-lt element. **Fix**: reset scale to 1 for `lt < 8` (SLTYPE 1/2/4 = i8/i16/i32 scalars); `lt == 8` left alone (a `*i64` typed pointer — scalar i64 is SLTYPE 0, no sub-i64 typed pointers exist → `points.cyr` `*i64` scaling preserved =25). `tests/tcyr/subword_scalar_arith.tcyr` (12 asserts); differential = only the intended scalar-arith corrections; two-step bootstrap (one-step-stable, cycc 1,011,816 B); ecb+cass+pi SELFHOST_OK. Also filed: a separate pre-existing **global** typed sub-i64 scalar load SIGSEGV. See CHANGELOG [6.3.34].) PRIOR: **6.3.33** (v6.3.x cycle — **generics arc tail: generic STRUCTS now actually instantiate (v6.3.10 shipped them broken) + multi-type-param + nested generics**. Premise-check (distinguishing tests) found the roadmap premise partly wrong: control-flow in generic bodies ALREADY worked, but v6.3.10 generic STRUCTS were **non-functional** — `var x: T<Conc>` never instantiated because `CYRIUS_MONOMORPH` was read AFTER pass-1, but structs + their `GSTP` type-param encoding register IN pass-1 → `GSTP==0` → the instantiation condition never fired → silent fallback to the base i64 struct. The v6.3.10 fixture passed on non-distinguishing tests (i32-as-i64 sums equal; `h.tag` reads the same at offset 8 or 16; `h.v.x` untested). **Fix: read `CYRIUS_MONOMORPH` BEFORE pass-1** (`main.cyr`; default env-unset → `_MONOMORPH_OK` stays 0 → byte-identical). Then: nested field access into a struct-typed generic field (`h.v.x`); multi-type-param distinct (`conc1` threaded through `_mint_generic_name`/`_instantiate_generic_struct`/both sites — `Base$c0$c1`); nested generics `Box<Pair<i32>>` (`_parse_one_type_arg` recursion + the `>>`-as-two-`>` close via `_consume_gt`/`_ta_pending_gt` + `LOOKAHEAD` depth −2). An **adversarial-verification workflow before handoff** (4 lenses, skeptic-reproduced — run because this slot exists to end v6.3.10's broken-behind-a-weak-fixture mistake) then caught 3 more real bugs: **①** special/aggregate type-args (`f64v2`/`Result`/…, encoded −16..−21) slipped the struct guard AND aliased to `Base$i` → now HARD-ERROR at the `_parse_one_type_arg` choke point (gated); **②** a **pre-existing DEFAULT-PATH** bug — nested sub-i64 field access `x.y.z` (leaf i8/i16/i32) read/wrote 64-bit because `PARSE_FIELD_LOAD/STORE` took the width from the intermediate struct, not the leaf → fixed by `ft = GETFTYPE(S, isi, ifi)` in both chained blocks (this is *why* nested generics were observably wrong even though instantiation was correct; reproduced with a hand-written non-generic struct); **③** a pre-existing GATED miscompile (`if` + a generic fn's first-use inline instance emit + result folded into a live local clobbers the local) — FILED, scheduled **v6.3.34** (batched with the generic-fn-struct-type-arg follow-on into a generics-engine de-risking slot). Default-codegen impact is surgical: **differential 306 identical / 1 codegen-diff / 0 status-diff of 308** — the sole diff is the new `nested_subword_field.tcyr` regression test. The `generics_tail` fixture is now genuinely **distinguishing** (`b.v.a >> 30`: real=42, i64-fallback=46). Two-step bootstrap (stable fixpoint) + seed→cybs→cycc byte-identical; gate `_generics_tail_gate` (+ special-type reject `generics_special_reject.cyr`) check.sh 122→**123**; `nested_subword_field.tcyr` 6/6; ecb+cass+pi SELFHOST_OK; cycc 1,011,816 B. See CHANGELOG [6.3.33].) PRIOR: **6.3.32** (v6.3.x cycle — **Phase D: dissolve the "stdlib" category → built-in `std` default group (modularity arc lever-1 COMPLETION)**. `[deps].stdlib` is no longer a special hardcoded category — its universal core is a built-in **`std`** group `["string","fmt","alloc","io","vec","str","syscalls"]` (matches the `cyrius init` templates), registered by `_dep_register_default_std_group` (`cbt/deps.cyr`) right after `_dep_parse_groups`. A manifest writes `stdlib = ["std", <non-core>]`; `std` expands via the Phase B `_dep_expand_groups` (already applied to the stdlib list) in the SAME order → **byte-identical** to the explicit form, dropping the omit-one→SIGILL order-sensitivity. Registered AFTER the manifest's `[groups]` so a manifest's OWN `std` **overrides** the built-in; std-free manifests resolve byte-identical. Premise-check found descent's "29-element" list had already drifted to 18 (M6 leaves → `libro.deps` sidecar); migrated descent → `["std", <9 non-core>]` + pin 6.2.48→6.3.32, verified the identical 30-leaf resolution (cross-repo). cycc **byte-identical** (CLI-only, resolver not in the compiler); gate `_deps_std_group_gate` (check.sh 121→**122**); Phase A/B/C gates unregressed; vidya `dependencies.cyml` item 6. **Completes lever 1 (Phases A–D).** self-host fixpoint + seed→cybs→cycc; ecb+cass+pi SELFHOST_OK. See CHANGELOG [6.3.32].) PRIOR: **6.3.31** (v6.3.x cycle — **freelist allocator now agnos-aware — consumer-filed HIGH (every agnos `fl_alloc` was SIGSEGVing)**. `lib/freelist.cyr`'s two mmap sites used the Linux/BSD 6-arg form (length in arg2/`rsi`, addr=0 in arg1/`rdi`), but **agnos `mmap#27` is single-arg** (kernel reads length from arg1/`rdi`) → freelist passed length 0 → mmap returned 0 → first `store64` derefs NULL → SIGSEGV on every agnos `fl_alloc` consumer (libro audit chain, sigil crypto; found via mirshi-fanout running descent). Fix = new **`_fl_mmap(length)` dispatcher** (`#ifdef CYRIUS_TARGET_AGNOS → syscall(SYS_MMAP, length)`, else 6-arg), both alloc sites routed through it — mirrors the existing `_fl_map_flags` macOS `MAP_ANON` divergence. cyrius-NATIVE lib (no vendored repo). cycc **byte-identical** (freelist not in the compiler); Linux `fl_alloc` round-trip=42; agnos build routes the single-arg mmap; consumer-verified end-to-end on real agnos (descent runs, binds `:4000`). Gate `tests/freelist_agnos_mmap.sh` (Linux behavior + agnos build + `_fl_mmap` source-integrity; check.sh 120→**121**); self-host fixpoint + seed→cybs→cycc; ecb+cass+pi SELFHOST_OK. Reactive consumer-filed pull-in (user-sequenced as .31 before the roadmapped Phase D). **+ ganita re-vendored 1.0.1→1.0.2** (folded in): `ganita_f64_tanh` NaN-for-large-|x| fix (saturate ±1 for |x|>20, bit-exact; fixes GELU on GPT-2 outliers), no API change, byte-identical dist re-vendor, 6 math tcyr green. See CHANGELOG [6.3.31].) PRIOR: **6.3.30** (v6.3.x cycle — **redundant-reload elimination (O2 category 5): the perf arc's SECOND real win** (cycc −0.80%). Naive codegen emits, at statement boundaries, `mov [rbp-N],rax` (store) immediately followed by `mov rax,[rbp-N]` (reload) — the reload is DEAD (rax still holds it). An **in-emit** eliminator (fires DURING emit, before the picker, where the pair is genuinely adjacent — a post-pass byte-scan got 0 hits because the 378 final-adjacent pairs are *post-compaction* artifacts; emit-time has 1961 raw / **1354** safe): `EFLSTORE` records its post-emit CP+idx; a 64-bit `EFLLOAD` that is the very next emit (`GCP==_efl_store_cp`, same idx) SKIPS. **Fail-safe via CP-adjacency** (mirrors `_last_push_cp`) — any intervening emit moves GCP off the store CP → no skip; the only hazards are zero-byte control-flow ENTRIES landing on a reload, cleared in `EPATCH` (forward joins) + the 4 loop-top captures (`parse_ctrl.cyr`, back-edges). **cycc 1,015,608→1,007,472 B (−8,136, −0.80%)**; self_compile flat (~525 ms median). DEFAULT ON (`CYRIUS_RELOADELIM=0` opts out); two-step bootstrap; x86-only (aarch64/cx `EFLLOAD` don't consult it). **differential status-diff=0 in BOTH default+DCE modes (287 codegen-diffs, 0 behavior changes / 306)**; self-host fixpoint; seed→cybs→cycc; ecb+cass+pi SELFHOST_OK. Gate `tests/reloadelim.sh`+`tests/tcyr/reload_elim.tcyr` (check.sh 119→**120**). The .29 frame-trim + this are the direct-emit stream's clean wins (stream now tight — all sibling redundancy classes measure 0); the big reload firehose (~23K cross-BB join-reloads) needs copy-prop → the DEFERRED IR substrate. See CHANGELOG [6.3.30].) PRIOR: **6.3.29** (v6.3.x cycle — **callee-saved frame-trim: the perf arc's first REAL, measurable win** (cycc −1.6%, self_compile −1.5%). Every `#regalloc` fn saves+restores ALL 5 callee-saved regs (rbx,r12-r15) but the linear-scan picker uses fewer; since it allocates lowest-first and callee-saved regs are written ONLY by the picker, the used set is a prefix → the save/restore pairs for the unused `[_ra_used_n, _cur_fn_regalloc)` regs are DEAD. Track `_ra_used_n` in the picker; after emit, **pattern-scan** the fn for each unused reg's exact 4-byte save/restore bytes (they appear ONLY as real save/restores → no body false-match, and it finds ALL of a fn's multiple epilogues) → nop_runs → PARSE_FN_DEF's existing compactor deletes them with full jump/fixup/target repair. **cycc 1,031,944→1,015,608 B (−16,336, −1.6%); self_compile 538→530 ms.** DEFAULT ON (`CYRIUS_FRAMETRIM=0` opts out); two-step bootstrap; x86-only (aarch64/macho byte-identical — trim is `_AARCH64_BACKEND==0`-gated); skipped for `_cur_fn_has_closure` (inline closure/instance) + `CYRIUS_MONOMORPH=1` (token-replay path skips the picker → `_ra_used_n` understates the instance's use). The .28 x86 byte-peephole was measured ~nil; **this is the real IR-independent win.** ABI-critical, exhaustively verified: **201/201 tcyr exit-identical to default**, self-host fixpoint, seed→cybs→cycc, differential status-diff=0 (296 codegen-diffs = the trim applied). Gate `tests/frametrim.sh` (check.sh 118→**119**); ecb+cass+pi SELFHOST_OK; self_compile 530 ms; cycc 1,015,608 B. **DEFERRED (off the perf arc's critical path):** IR substrate productionization (re-emit path + `CYRIUS_IR=3` fixpoint fix) — the residual `CYRIUS_IR=3` bug is a probabilistic (ASLR-dependent) miscompile in `ir_lase`, deep/open-ended; the arc's new passes build on the sound .27 analysis, not the old transforms. See CHANGELOG [6.3.29].) PRIOR: **6.3.28** (v6.3.x cycle — **`CYRIUS_IR=3` substrate: root-caused + 2 real bugs fixed — it was NEVER "fundamentally blocked"** (a root-cause workflow disproved my earlier "needs a re-emit rewrite" excuse; the failures are BOUNDED, in-place-fixable bugs). The roadmapped x86 byte-peephole was investigated + found empirically **~nil** on the default stream (376/611 redundant reloads are cross-BB *join*-reloads → need copy-prop, blocked; stream already tight) → pivoted (user direction) to the substrate fix the arc needs. **Bug 1 (SIGILL on struct-init/`#derive`):** `EVADDR_X1` emits `movabs→RCX` but recorded `IR_LOAD_ADDR_G` (the RAX opcode) → `ir_dce_capped` eliminated the live RCX-writing movabs → SIGILL; fix = new opcode `IR_LOAD_ADDR_G_X1`(9) in `_ir_def_rcx_any`, kept OUT of every `_pure`/RAX set (isolated via `CYRIUS_DCE_CAP=39` pass/`40` fail); `derive_str_deserialize` under IR=3 → 5/5. **Bug 2 (0 bytes on cycc):** IR node buffer cap 262144, never reset per-fn, cycc demands ~280K → deliberate `SYS_EXIT(1)` during parsing; fix = relocate `ir_nodes`(16 MB)/`ir_cp`(4 MB) from `0xA3A000`/`0xF7B000` to the arena top (`0x5E9D000`/`0x6E9D000`, above the .27 liveness) + cap **1 048 576** + grow x86 arenas (`main.cyr` `0x5E9D000`→`0x7300000`; `main_win.cyr` `0x5F00000`→`0x7300000`; aarch64/macho untouched — IR x86-only; lazy VA → **zero real-mem cost on default builds**); two-step bootstrap. **`CYRIUS_IR=3` now compiles cycc: exit 0, 279 787 nodes; 27/30 sampled tcyr pass under IR=3.** + `CYRIUS_FOLD_OFF`/`LASE_OFF` bisection knobs (DCE_CAP/DSE_CAP methodology). **CARRY-OVER (filed):** the residual 3/30 IR=3 miscompiles (`alloc_str_extras`/`collections` SIGSEGV, `bigint` hang) are a **fixpoint cascade over-elimination** — each of const_fold/DCE/dead_store/LASE individually sound, only the four combined over-fire; deeper than the bounded opcode class, and **NOT on the perf arc's critical path** (the arc needs sound analysis — the .27 `ir_liveness_cfg` ✓ — + new passes, not the old transforms). Default codegen **byte-identical** (differential 306/306); self-host fixpoint + seed→cybs→cycc byte-identical; check.sh **118**; ecb+cass+pi SELFHOST_OK; self_compile 538 ms; cycc 1,031,944 B. See CHANGELOG [6.3.28].) PRIOR: **6.3.27** (v6.3.x cycle — **perf arc opens: cross-BB liveness + spill-interval detection (the regalloc substrate)**. AR-04 decision: build on the IR (cross-arch-capable — the only path to aarch64/riscv64 regalloc parity + the substrate v6.3.28 copy-prop / v6.3.29 cross-BB DSE need), gated under `CYRIUS_IR`. **Adversarial review of the full linear-scan design caught a fatal miscompile hole**: the `CYRIUS_IR` pipeline emits x86 during PARSING and the optimizer **patches codebuf IN-PLACE (shrink-only)** — it never re-emits (`ir_lower_all` is dead scaffolding, `IR_ENABLED==2` never activated), so promoting `push rax`(1B)→`mov r12,rax`(3B) would overwrite the NEXT instruction = silent miscompile of every arithmetic expr. So v6.3.27 ships the **safe ANALYSIS half** (emits zero code → default codegen byte-identical): **`ir_liveness_cfg`** = cross-BB backward-dataflow fixpoint (`live_out[BB] = ∪ live_in[successors]`, iterated to convergence; per-BB `ir_live_in`/`ir_live_out` u64 bitmaps in a new +512 KB arena region at the top; a RET/EPILOGUE BB gets `live_out==0`, more precise than the per-BB DCE's conservative "both live" seed) + **`_ir_find_spill_intervals`** (counts clean intra-BB `PUSH`..`POP` round-trips — the future register-promotion targets — abandoning any span with an opaque stack/reg op). Observable via `CYRIUS_IR_LIVENESS=1` (`bbs/iters/exit0/spills`); gate `tests/ir_liveness_cfg.sh` (behavior-safe default==CYRIUS_IR + converges <64-cap + exit0>0 + spills>0). This is the reusable substrate **.28/.29 build on directly**. The **register allocation + rewrite is DEFERRED** to a slot that first wires+proves the IR re-emit path byte-equivalent (the in-place-patch model can't grow an instruction) — filed `2026-07-02-ir-regalloc-rewrite-needs-reemit.md`. check.sh 117→**118**; self-host fixpoint + seed→cybs→cycc byte-identical (cycc grew 1,027,720→1,031,888 B for the new IR fns + the +512 KB arena); ecb+cass+pi SELFHOST_OK; self_compile 543 ms; cycc 1,031,888 B. See CHANGELOG [6.3.27].) PRIOR: **6.3.26** (v6.3.x cycle — **"Class B FFI / `fncall6` ABI fix" was a MISDIAGNOSIS; the real issue is TLS/`%fs`**. The slot (held from v5.9.x–v5.11.x) assumed a `fncall6` vs SysV calling-convention bug; proven empirically (real gcc-compiled stack-protected C called from cyrius) there is **none** — `fncall4/5/6/7` arg-passing + 16-byte stack alignment are correct, and a stack-protected extern-C fn taking 4/5/6/7 int args returns the right result via `fncallN`. The "fncall6 into extern-C wgpu is unreliable" folklore (mabda works around it by struct-packing to `fncall2`) was a **TLS/`%fs`** problem: any glibc-compiled C fn with an array local carries `-fstack-protector` and reads its canary from `%fs:0x28` in its prologue → it faults **regardless of arg count** if `%fs` isn't a glibc thread block. mabda's C-launcher (ADR-004) already supplies glibc's `%fs`, so the wgpu C-hook path works today. **Fix (residual hazard):** cyrius's `thread_local_init` `arch_prctl(ARCH_SET_FS)`-clobbers the host's `%fs`, and `thread_local_set(5,…)` writes `%fs:0x28` — either wipes the host canary/self-pointer and breaks every stack-protected C callee (any process mixing wgpu C hooks + sigil/patra thread-locals). New **`thread_local_use_foreign_tls()`** (`lib/thread_local.cyr`): the glibc-hosted consumer declares itself once; cyrius then leaves `%fs` untouched and keeps slots in a process-global fallback array (macOS/agnos path). **Explicit, not auto-detected** — a native `CLONE_SETTLS` worker also has a non-zero `%fs`, so `fs != 0 => foreign` would misclassify native workers and collapse their per-thread crypto lanes (the v6.3.25 collision class). Native path byte-identical behavior; cycc **byte-identical** (fnptr/thread_local lib-only + doc/test). Gate `tests/ffi_stack_protected_extern_c.sh` (stack-protected extern-C via `fncall4/5/6/7` + foreign-`%fs` no-clobber/canary-intact proof; check.sh 116→**117**). Docs: `lib/fnptr.cyr` header + `docs/ffi/fncall-abi.md`. Consumer follow-up (mabda, separate repo): drop the struct-packing workarounds + call `thread_local_use_foreign_tls()` in `deps/wgpu_main.c` if it links a cyrius thread-local user. Also: api-surface snapshot += the one new public fn; the flaky v6.3.15 array-locals negative-control hardened to retry 5× (was ~10%-per-run false-RED). Third consecutive consumer misdiagnosis (`.24` enum-shadow, `.25` slot-collision, `.26` TLS-not-ABI). ecb+cass+pi SELFHOST_OK; self_compile 537 ms; cycc 1,027,720 B. See CHANGELOG [6.3.26].) PRIOR: **6.3.25** (v6.3.x cycle — **consumer-filed TLS bug: multi-worker `RECORD_LAYER_FAILURE` root-caused to a thread-local SLOT COLLISION** (SecureYeoman / yeo-cy-test). The filing's "per-connection TLS state leak / aborted-handshake / needs-concurrency / max_conns>1" framing was **all incidental**. cyrius's `thread_local_get/set` is a 16-slot SHARED integer space with **no allocator**; **sigil's per-thread crypto-bank lane (`_SIGIL_CBANK_SLOT`) and patra's SQL-parse scratch both hardcoded slot 0** (patra owns 0-4). In a server linking both — a `run_pooled_tls` pool over a patra-backed API, exactly yeo-cy-test — a patra query clobbered sigil's pinned bank in slot 0; the next `cbank()` read patra scratch as the bank index and pointed sigil at the **wrong lane of the process-global banked crypto buffers** (`&X + cbank()*N`) → in-flight handshake key-schedule corruption → client `RECORD_LAYER_FAILURE`. **Deterministic every-4th-handshake, reproduces single-threaded at `max_conns=1`**; root-caused by bisection down to a heisenbug (disabling the concurrent HTTP pool, adding *any* syscall in the TLS worker loop, or growing the pool to 8 all hid it → timing/layout race on the shared bank buffer); confirmed by moving sigil slot 0→8 (`verify.py` 34/34 at `max_conns=4`, scenario 9h included). Fix = sigil source `src/crypto_scratch.cyr` `_SIGIL_CBANK_SLOT` 0→8 (**sigil 3.9.9**, separate-repo publish — user's git), re-vendored byte-identical into `lib/sigil.cyr`; a **slot-namespace registry** documented in `lib/thread_local.cyr` (0-4 patra, 8 sigil, 15 consumer; 5-7/9-14 free); regression gate `tests/thread_local_slot_collision.sh`. Consumer: bump sigil ≥3.9.9 and `max_conns` returns to >1. cycc **byte-identical** (sigil/thread_local lib-only, no compiler change). Two follow-ups filed: a proper `thread_local_alloc()` allocator + sandhi's `_SANDHI_RPC_POLICY_SLOT=16` macOS/agnos OOB. check.sh 115→**116**; self-host fixpoint + seed→cybs→cycc byte-identical; ecb+cass+pi SELFHOST_OK; self_compile 543 ms; cycc 1,027,720 B. See CHANGELOG [6.3.25].) **Earlier releases (6.3.24 back to v5.x): see CHANGELOG.md** — trimmed here (state.md keeps current + ~2 priors; the rest is canonical in CHANGELOG). |
