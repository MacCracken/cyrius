# Cyrius — Current State

> Refreshed every release. CLAUDE.md is preferences/process/procedures (durable);
> this file is **state** (volatile). Bumped via `version-bump.sh` post-hook.
>
> **Consolidated 2026-06-08 (v6.1.4):** the per-patch session-close log
> (104 entries, ~5,600 lines back to v5.x) + the stale v6.0.4-frozen
> structured sections were pruned — that detail is canonical in
> [`CHANGELOG.md`](../../CHANGELOG.md) (per-patch) and
> [`completed-phases.md`](completed-phases.md) (arc retrospective). This file
> now holds only the **active cycle** + current state.

## Current state

| | |
|---|---|
| **Version** | **6.3.16** (v6.3.x cycle — **Language Refinements**; **var-decl / struct-local codegen fixes (P2)** — the roadmapped "var-decl codegen pair" slot, 3 pre-existing bugs in `parse_decl.cyr` (shared frontend → all 7 forks). **(a) inferred struct-local from a struct-returning call** — `var p = mk();` (no type annotation) allocated a scalar slot → callee wrote its aggregate to a garbage retptr → SIGSEGV (explicit `var p: T = mk();` worked). Fix: broaden the asv detection gate to `pscale <= 0` + INFER `pscale = -callee_sid`, routing through the same aggregate-slot + `&p`-retptr / `rax:rdx`-pair codegen as the explicit form; Str/≤8B keep the scalar/pointer inferred path (byte-id). **(c) single-≤8B-field struct-local field access** — `var b: B1; b.val=42; return b.val;` segfaulted on x86 + aarch64 (cx already correct). A 1-slot struct-local has no `-1` filler, so `PARSE_FIELD_LOAD`/`STORE`'s inline-vs-pointer disambiguation ("prev slot == -1 sentinel?") misread it as a single-slot POINTER-to-struct → `mov [slot]` (deref the value) not `lea &slot`. Fix (both sites): `STRUCTSZ <= 8` struct-local holds its value INLINE → force `lea`; only `STRUCTSZ > 8` single slots are pointers (Str). Gated `_TARGET_CX == 0` (cx boxes structs → pointer mode correct there; its struct-byval parity test stays green). FIRST attempt (reserve a filler) REVERTED — perturbed cx layout + struct-return. **(b) string-literal global init** (`var S = "hello";`) — already fixed in an earlier v6.3.x; regression-locked. `tests/tcyr/struct_local_codegen.tcyr` covers all 3. Verified x86 + aarch64 (REAL pi native) + cx = 42 (field-access + by-value); cx >8B struct-byval hard-errors identically to explicit (pre-existing cx pair-return limitation, out of scope). check.sh 109/109; seed→cybs→cycc; ecb+cass+pi SELFHOST_OK; bench 546 ms; cycc 1,027,664 B (unchanged). 3 issues closed→archived. See CHANGELOG [6.3.16].) PRIOR: **6.3.15** (v6.3.x cycle — **array locals PER-THREAD by DEFAULT** — the v6.3.13 str_builder concurrency fix (array LOCALS were a shared global `.bss` buffer) flipped OPT-IN → DEFAULT-ON (`CYRIUS_STACK_ARRAYS=0` opts out). Both .13 blockers fixed: m128 16-align parity-pad (`&arr` disp ≡ 8 mod 16 → `pxor/aesenc xmm,[arr]` #GP-free) + `secret var` zeroise via `ELOAD_LOCAL_ADDR`. AUTO-FALLBACK: array over the per-fn 16384-slot budget stays global+`note:` (sole case sigil `hash_file_into` 256KB, already global → no regression). Feared "ecosystem footgun" = FALSE ALARM (buggy awk mis-flagged element-typed arrays); precise + 11-file agent audit = 295 bare-array locals, 0 over-runs, ZERO stdlib changes; only 6 `.tcyr` daimon-idiom suites fixed → element-typed. Two-step bootstrap; cycc SHRANK 1,111,616→1,027,664 B. check.sh 109/109; ecb+cass+pi SELFHOST_OK; bench 544 ms. See CHANGELOG [6.3.15].) PRIOR: **6.3.14** (v6.3.x cycle — **AGNOS syscall peer — 8 missing wrappers (LIB-ONLY)**. New `SysNrAgnosProc` enum + 8 `sys_*` wrappers (`sys_klug`#36, `sys_execwait`#37, `sys_fbinfo`#38, `sys_blit`#39 4-arg a4=r10, `sys_kbscan`#42, `sys_spawn_path`#43, `sys_sched_yield`#44, `sys_exec_redirect`#62). cycc BYTE-IDENTICAL (lib-only); api-surface +8; check.sh 109/109; ecb+cass+pi SELFHOST_OK; bench 533 ms. See CHANGELOG [6.3.14].) **Earlier releases (6.3.14 back to v5.x): see CHANGELOG.md** — trimmed from here (state.md keeps current + ~2 priors; the rest is canonical in CHANGELOG). |
