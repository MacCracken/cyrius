# Cyrius — Current State

> Refreshed every release. CLAUDE.md is preferences/process/procedures (durable);
> this file is **state** (volatile). Bumped via `version-bump.sh` post-hook.
>
> **Consolidated 2026-06-08 (v6.1.4):** the per-patch session-close log
> (104 entries, ~5,600 lines back to v5.x) + the stale v6.0.4-frozen
> structured sections were pruned — that detail is canonical in
> [`CHANGELOG.md`](../../CHANGELOG.md) (per-patch) and
> [`completed-phases.md`](completed-phases.md) (arc retrospective). This file
> now holds only the **active cycle** + current state.

## Current state

| | |
|---|---|
| **Version** | **6.3.15** (v6.3.x cycle — **Language Refinements**; **array locals are PER-THREAD by DEFAULT** — the v6.3.13 str_builder concurrency root-cause fix (array `var arr[N]` LOCALS were a fixed shared global `.bss` buffer aliased by EVERY thread) flips OPT-IN → **DEFAULT-ON**. `CYRIUS_STACK_ARRAYS=0` opts back to legacy global. Both v6.3.13 default-on blockers fixed: (1) SSE **m128 16-align** — a one-slot parity pad when `&arr` disp ≡ 8 (mod 16), so `pxor`/`aesenc xmm,[arr]` (AES-NI/crypto/TLS) stays #GP-free (global arrays got 16-align via the v5.5.21 totvar pad; stack slots were 8-aligned); (2) **`secret var buf[N]` zeroise** now addresses the LOCAL slot (`ELOAD_LOCAL_ADDR`=`lea [rbp-disp]`, via `FINDLOCAL`+slot-delta size), not the global `EVADDR`. **AUTO-FALLBACK**: an array whose slots would exceed the per-fn 16384-slot `SFLC` budget stays global (with a `note:`) instead of overflowing the slot table — the SOLE ecosystem case is sigil `hash_file_into`'s 256 KB I/O buffer, already global before → NO regression; every concurrency-relevant crypto fn (≤~5000 slots) routes per-thread. **STDLIB CLEAN**: my initial "ecosystem-wide footgun" alarm was a BUGGY awk mis-flagging element-typed `var x: i64[N]` as bare — a precise width+element-typed-aware scan found NO literal over-accesses in `lib/`/`programs/`, and an 11-file agent audit confirmed **295 bare-array locals, 0 over-runs, ZERO stdlib changes**. Only 6 `.tcyr` suites used the daimon under-declared-array idiom (bare `var scratch[7]` = 7 B written as 56 slot-bytes — benign in `.bss`, frame-corrupting on the stack) → corrected to element-typed (`var scratch: i64[7]`): fncall_ceiling/regalloc/slices_field_access/slices_indexing/parser_cosmetics; `secret.tcyr` (`return &key`+read-after = use-after-return on the stack) + `element_typed_array.tcyr` (in-fn byte-gap probes = `.bss`-layout-specific) REWRITTEN layout-independent (stack-scan scrub probe w/ plain-var control; write-all-elements + sentinel roundtrip). Two-step bootstrap (cc1 transition→cc2 stable, cc2==cc3); **cycc SHRANK 1,111,616→1,027,664 B** (arrays left `.bss`). `_array_local_threadsafe_gate` polarity flipped (default per-thread-clean; `=0` = corrupting control). check.sh 109/109; seed→cybs→cycc; ecb+cass+pi SELFHOST_OK; bench 544 ms. Closes `issues/2026-06-30-array-locals-stack-default-on-m128-align.md`. See CHANGELOG [6.3.15].) PRIOR: **6.3.14** (v6.3.x cycle — **AGNOS syscall peer — 8 missing wrappers (LIB-ONLY)**. The agnos kernel implements all 64 syscalls (0–63 contiguous) but `lib/syscalls_x86_64_agnos.cyr` omitted 8 the kernel dispatches. Added `SysNrAgnosProc` enum + 8 `sys_*` wrappers: `sys_klug`#36, `sys_execwait`#37, `sys_fbinfo`#38, `sys_blit`#39 (4-arg a4=r10), `sys_kbscan`#42, `sys_spawn_path`#43, `sys_sched_yield`#44, `sys_exec_redirect`#62. cycc BYTE-IDENTICAL (lib-only); api-surface +8; check.sh 109/109; ecb+cass+pi SELFHOST_OK; bench 533 ms. See CHANGELOG [6.3.14].) PRIOR: **6.3.13** (v6.3.x cycle — **str_builder concurrency ROOT-CAUSED + per-thread array-local fix (OPT-IN)**. Root cause (ultracode 3-agent workflow): `var arr[N]` LOCALS were allocated at a fixed GLOBAL/BSS address shared by EVERY thread (scalars already stack/per-thread); any concurrent path using an array-local aliased one global buffer → ~87% cross-thread splice. FIX (opt-in `CYRIUS_STACK_ARRAYS=1`): `PARSE_ARRAY` routes array locals to per-thread STACK slots (ceil(size/8) slots, fillers + named slot at the deepest offset; existing `&arr`/`arr[i]` paths already had `FINDLOCAL`→`ELOAD_LOCAL_ADDR`, ONE shared-frontend flip covers all 7 forks). `THREAD_STACK_SIZE` 64KB→2MB. Default gated-off → byte-id vs 6.3.12; default-on hit m128-align + TLS-probe → deferred to 6.3.15 (now shipped). See CHANGELOG [6.3.13].) **Earlier releases (6.3.13 back to v5.x): see CHANGELOG.md** — trimmed from here (state.md keeps current + ~2 priors; the rest is canonical in CHANGELOG). |
