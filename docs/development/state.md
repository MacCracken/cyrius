# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.6.4 — bumped 2026-09-14, tag PENDING** (the bump commit; release-gate GREEN on ecb/ach/cass/pi, check.sh 246/246, seed-derive, bench 744 ms). The repair release for the six issues filed after the 6.6.3 handoff, one bite each: ① string literals ≥ 64 KB read from their second byte (+ cx addresses past 0xFFFF, cx `x*2`, a cx crash-after-diagnostic, `#deprecated` on tail calls); ② `&_private_fn` and seven sibling resolution paths compiled and were callable across files (+ method `self` on inline stack structs); ③ `public enum` re-exposed the NEXT declaration in a `private` file (+ struct/union/impl/`use`, derive codecs, the enum-derive `#ifdef` clobber); ④ the install store was writable under a RELEASED name and was stale in both directions (install.sh/pulsar/lsp refuse, check.sh stages its own home, `verify-store.sh`, the live store restored); ⑤ `cyrius deps` silently re-locked a changed stdlib file under an unchanged pin (+ bare `deps --lock` dropping commit pins, CRLF, 64 KB verify); ⑥ raw x86 syscall numbers ran as DIFFERENT syscalls on aarch64 — ⛔ every `cyrius run/test` child on native aarch64 exited 1 before `execve` for 59 releases (`cbt/build.cyr` raw 110), plus ESYSXLAT rows for fstat/lstat landed WITH fdlopen/dynlib declines, per-target `O_*`, the agnos `AO_NOFOLLOW`/`AO_EXCL` bridge, patra 1.14.3 / sigil 3.12.18 / mirshi 1.11.2 at source. ⚠ **After the tag: `sh scripts/install.sh --refresh-only` once at the tagged commit** (the bump's stamp names the pre-bump HEAD, dirty). ⛔ **patra 1.14.3 and sigil 3.12.18 pin 6.6.4 — they cannot be tagged until the cyrius 6.6.4 release exists** (patra's CI is red on that until then; its README tag was also fixed locally). **Prior: 6.6.3 — SHIPPED** (tag `6.6.3` @ `ab77e241`, 2026-09-12). |
| **cycc** | x86 **1,260,296 B** — **6.6.5 in flight** (`.text` **1,104,952** — re-derived from the shipped binary, `readelf -S` == 0x10DC38; the file size did not move because the +200 fits the text segment's page padding. It read 1,104,752 / 0x10DB70 before the bite-2 Mach-O entry-alignment follow-up; 6.6.4 shipped at 1,251,944). ⭐ **The x86 Mach-O landing now ESTABLISHES 16-byte alignment (`and rsp,-16`) instead of inheriting it** — measured on real ach, Darwin x86_64's entry rsp parity varies with the argv/env byte count (160/160 over a 20-argv0 x 16-env sweep), which is the THIRD base-alignment defect in this release after PE and UEFI. Both x86 forks (`main.cyr`, `main_x86_macho.cyr`) carry it; ELF/aarch64/agnos are unchanged and measured clean. ⭐ **6.6.5 bite 2 is the first codegen change in this minor whose output is NOT byte-comparable per binary**: calls emitted inside an expression now carry a 2-byte alignment pad, so 369 of 423 corpus binaries change by construction and `.text` grows +3,336 B on unchanged source (1,668 padded call sites) — the rest of the +8,352 is the new emitter/checker code. ⚠ NOT x86-only, though the CODEGEN change is: the statement-position `fncallN` → `PINDIRECT_CALL` lowering is shared-frontend, so **80 of 317 aarch64 corpus binaries differ** from 6.6.4's — 316/317 exit codes identical under qemu and the one that moves is a fix (139 → 0). cx compiles 1 of 316 `.tcyr`, so quote it over `programs/` + `benches/` instead: 43 of 43 byte-identical. **6.6.4 detail follows.** (`.text` 1,097,472 at 6.6.4; 6.6.3 shipped at 1,251,864, so +80 B). Bite 1 (string-literal pack widening, cx movhi slots, `#deprecated` on tail calls) was byte-different at the SAME size; bite 2 (`_vis_check` on eight resolution paths, instance polarity, array stamp, method `self` on inline structs) +16 — three new globals; bite 3 (`public` marker scoped to its item, pass-2 + PARSE_PROG clears, destructure, derive codecs inherit `public`, PARSE_PROG-path stamps, the enum-derive `#ifdef` clobber) +8 — the `public fn _cy_enum_name_eq` literal grew by 7 bytes in RW data (measured by reverting each file; `_PUB_CAN_ARM`'s code is absorbed by text-segment page padding); bites 4/5 touched no compiler source (same 1,251,888); bite 6 +56 — the raw-syscall diagnostic's 53-byte "not a syscall at all" (false for every number the bite fixed) became the 111-byte "not one the aarch64 stdlib declares (it may still be a DIFFERENT real syscall)". **seed-derive GREEN after every bite** — `seed → cybs → cycc` byte-identical. ⚠ Bites 1–3 and 6 are shared-frontend / aarch64-emitter changes, so every fork inherits them; the real-hardware ecb/ach/cass/pi leg ran GREEN at the 6.6.4 release gate (2026-09-14). Last tagged: **v6.6.3, 1,251,864 B**. |
| **Bootstrap / cross-OS** | **v6.6.4: ecb · ach · cass · pi all `SELFHOST_OK` + `LIBTEST_OK` (74/74 crossos) on REAL hardware** — and the pi leg now also BUILDS AND RUNS THE CLI (`cyrius run` under a throwaway home, zero raw-syscall warnings), because the compiler self-hosted there at every gate while the CLI's `cyrius run/test` had been dead on native aarch64 for 59 releases. v6.6.3 was the same four hosts, run ONE AT A TIME — fixed /tmp + remote paths clobber under concurrency. ⭐ `pi` is the load-bearing host this release: 6.6.3 changes `src/backend/aarch64/fixup.cyr`'s data-segment vaddr, and compiling the aarch64 fork locally proves it PARSES, not that cycc reproduces itself on ARM. seed **29,024 B** → cybs → cycc byte-identical. A green CI check is NOT this leg. ⭐ **v6.6.3 — the pi leg now ALSO self-hosts `src/main_aarch64_native.cyr`, the fork ARM users actually install.** It had only ever self-hosted `main_aarch64.cyr`, the CROSS source — the same shape as the macOS rot this leg exists to prevent, and the reason the full release gate passed GREEN while `#inline`-before-a-declaration failed to compile on the native ARM fork. Mutation-proven load-bearing: pointing the second compile back at the cross source makes pi's SELFHOST fail. ⭐ **6.6.4 (bite ④): the install store is verified against its TAGS** — `sh scripts/verify-store.sh` reports every tagged slot; the maintainer's `~/.cyrius` was restored (6.6.0–6.6.3 OK, stamped) after it was found stale in both directions. `check.sh` now stages a throwaway `CYRIUS_HOME` from the tree. |
| **Gates** | **v6.6.5 in flight — check.sh 248 / 0 GREEN + 165 shell gates** (`find tests/gates -name '*.sh' | wc -l`; bite 2 added `codegen/call_site_stack_alignment.sh`, a part (C) to `platform/ffi_stack_protected_extern_c.sh`, and TWO in-program gates: `_efi_entry_alignment_gate` — the UEFI entry-rsp park / landing seed / trampoline frame / the restore before all three compiler-emitted firmware `ret`s, asserted on emitted bytes because OVMF tolerates a misaligned CALL and only the RETURN faults — and `_efi_ovmf_fn_exit_gate`, a second OVMF boot of `programs/efi_fn_exit_probe.cyr`, which exits from INSIDE a fn body the way `lib/alloc.cyr` and `lib/bounds.cyr` abort; `programs/efi_probe.cyr` returns through its own hand-written `ret`, so the old smoke had never tested how the COMPILER ends a UEFI image, and the first cut of bite 2 shipped that route broken under every existing gate). **v6.6.4 — check.sh 246 / 0 GREEN + 164 shell gates; release-gate.sh GREEN.** **Thirteen** new gates this release (seven shell + six `.tcyr`, derived from `git diff --name-status 6.6.3 HEAD -- tests/`), every one mutation-proven: `string_token_decoders`, `string_literal_64k` (⚠ the odd-length group needed a 2-byte pad literal to bite), `cx_addr_past_64k` (four axes; the lexer INTERNS identical fillers), `cx_pow2_mul`, `public_marker_scoped_to_its_item`, `derive_enum_inside_ifdef`, `method_self_inline_struct` (crossos), `released_slot_written_from_tag` (17 axes), `deps_relock_refused` (14 axes), `raw_syscall_literals_routed` (allowlist DERIVED from ESYSXLAT's own `cmp x8,#N` words; lex_pp-exact `#ifdef ` spellings; 13 adversarial guard shapes), and three crossos tests `syscall_stat_x86_compat` / `open_flags_per_target` (exact `-ELOOP`/`-ENOTDIR` per target) / `fdlopen_dynlib_decline` (a 1,176-byte aarch64 ET_DYN fixture — the one assertion that separates a decline from the 6.6.3 accident). Also `syscall_xlat_generated` axes 3b/7/8 and `io_rdwr_agnos` axis 5 (O_NOFOLLOW/O_EXCL refused under mirshi ≥ 1.11.2). ⭐ Two of this release's review findings were gates that would have read green over the defect (a decode with Rn unchecked; guard spellings looser than the preprocessor's) — **a check that shares a defect with the thing it checks reads GREEN**. |
| **Corpus** | **316** `.tcyr` (**76** in `crossos/` — 6.6.5 bite 2 added `call_site_stack_alignment` + `nested_call_value_regressions`; both counts DERIVED, `find tests/tcyr -name '*.tcyr' | wc -l` — ⚠ **240 (76%) never run on non-x86 hardware**: the cross-OS leg's selector is that directory, so anything outside it is x86-only until CI's full-corpus aarch64 job sees it — the cross-OS selector is that DIRECTORY, the `vr01_` prefix was retired at v6.5.11) · **103** `lib/*.cyr` (`boxed.cyr` restored the boxed tagged-union primitives) · **84** `programs/*.cyr` · api-surface **5,156** · heap map **102** regions (gate-derived; ⚠ a raw `grep -cE '^#   0x' src/main.cyr` says **145** because it also matches 18 `FREED` markers — run `sh tests/gates/memory/heapmap.sh`). |
| **Active minor** | **v6.6.x** — value-form `Result` + language ergonomics. Opened at v6.6.0; **v6.5.x closed at v6.5.73** (there is no `.74` — that number was cut in error and re-cut as 6.6.0). Slot plan in [roadmap.md](roadmap.md): `.2`–`.6` are a reserved **repair window**, then the proposal queue, then the committed ergonomics list. |
| **Shipped this minor** | **.0** the `Result` / `Option` / `Either` **value-form flip** — construction allocates ZERO bytes (was 1600 B per 100 `sock_send`), shipped WITH the ecosystem (8 sibling stdlibs migrated at source, pin-bumped, released and re-vendored); it changed the ARITY of every value, so `payload()` and `tagged_new()` are DELETED; plus top-level `var t, v = f();`, the mixed-return diagnostic, and ⛔ **the v6.5.57 aggregate-copy P0 found at the cut** (live seventeen releases, invisible to the fixpoint because cycc's own source never uses the shape) · **.1** the two filed issues — `f64_exp`/`f64_exp2` **NaN for ±inf** in BOTH implementations (polyfill + emitted x87; the guard must precede the range reduction, which computes `inf - inf`) and AGNOS `clock_now_ns()` reading the **frozen `timer_ticks` clock** (#40 → #95 `rdtsc`) — plus the `cyriusly install` **ETXTBSY** failure (`cp` writes through the directory entry; now temp + atomic rename) and five stdlib folds. · **.2** the repair release — `tagged_new()` restored for a live consumer the deletion survey could not see; two SIMD P0s (a destination read from a frame slot nothing wrote; a top-level intrinsic that SIGSEGV'd); `map_u64` answering untruthfully for its own sentinel keys on five paths; `object;` builds exporting libc-reserved names; a vacuous SIMD gate; `cyrius build` overwriting the running compiler; `check.sh` reporting green over seven gates that never ran · **.3** the six issues the 6.6.2 ecosystem sweep filed, plus the fork-parity and Windows-checkout CI repairs · **.4** the six issues filed after the 6.6.3 handoff (see *Version*) — the 64 KB string-literal pack, `&_private_fn` across files, the `public` marker leak, the writable released install slot, the silent stdlib re-lock, and the raw x86 syscall numbers that had killed `cyrius run/test` on native aarch64 for 59 releases. ⚠ Closed-minor detail for v6.5.x and earlier lives in `completed-phases.md` + the CHANGELOG; this row carries the CURRENT minor only, and was the v6.5.x list until the 6.6.1 refresh. |
| **In-flight** | **Nothing — 6.6.4 is bumped and gate-green, awaiting the user's commit + tag.** Post-tag: `sh scripts/install.sh --refresh-only` at the tagged commit (reconciling write; `verify-store.sh` should then read `stamp tag-commit` for 6.6.4), then tag patra 1.14.3 / sigil 3.12.18 (pin 6.6.4) and mirshi 1.11.2 (pin 6.6.2, independent). The 6.6.4 bites, one per commit: ① ✅ · ② ✅ · ③ ✅ · ④ ✅ · ⑤ ✅ · ⑥ ✅. |
| **Next up** | **Slot `.3` — per-item `private` silently privatises the whole file.** ⚠ **Still next, but the version numbers have drifted from the slot labels:** 6.6.3 went to the sweep's repair set and 6.6.4 to the post-handoff filings, so this lands as **6.6.5** at the earliest — alongside the two issues 6.6.4 filed (private impl-method forward call; fn-local global slots), and the roadmap's `.3`–`.6` labels have not been re-slotted (that is the user's call). `.2`'s pinned occupant (the `cyrius build` overwrite guard) SHIPPED in v6.6.2 along with a much larger repair set than the slot anticipated. `_TL_VIS` handles token 153 by calling `_PRIV_MARK(FM_FILEID(...))` — a FILE-level flip — so `private fn h()` compiles clean and privatises the entire file, `main` included. Twelve-plus releases live, no diagnostic. Default taken in the roadmap: make the per-item form a hard error pointing at the file-level declaration. |
| **Open queue** | **2 open issues** (was 6 at slot open — all six closed by bites ①–⑥ and archived; two FILED from bites ②/③ remain): `2026-09-13-fn-local-global-slots-shadow-other-files.md` (Medium, pre-existing, surfaced by bite ③'s stamps: a fn-local struct literal / oversized array is a GLOBAL slot in the flat namespace and shadows other files' globals — before the stamp that was a silent miscompile, now a misattributed diagnostic); the new `2026-09-13-private-impl-method-forward-call-fail-open.md` (Medium) is the one residual bite ② could not pack — a 7-fork pass-1 change (impl bodies are brace-skipped in pass 1, so a forward call to a private impl method resolves with no fileid and `_vis_check` fails open). ⭐ **Read a filing's scope as a report about what the reporter builds, never as the bug's boundary** — every 6.6.4 filing widened under investigation (one path → eight; one leak → six declaration kinds; one writer → four). |
| **Ecosystem** | **The 6.6.2/6.6.3 sweep CLOSED 2026-09-12.** Derived from committed HEADs: **125** repos with a `cyrius.cyml` — **14** on 6.6.3, **110** on 6.6.2, and **1** below (agnos 6.6.1, another agent's). hisab is also another agent's and is **not** to be updated. dhvani **2.2.4** holds the sound stack's versions, with hisab kept at **2.11.2** (naad 2.2.4 and svara 3.5.5 were rebuilt and pass against it); jalwa **1.4.5** aligns its shared pins to dhvani. **Still open, recorded but not fixed:** same-arity duplicate fns (agnostic 22, mehman 19, hadara 2, aegis 1, kriya 1 — warnings only); ~30 `path = "../sibling"` + stale-`tag` pairs (deliberately not done). ⚠ **Stale docs:** `ecosystem-migration-6.6.2.md` still reads "~11% DONE, DO NOT ARCHIVE", and its outlier #4 is superseded by the (now archived, FIXED v6.6.4) raw-syscall issue; vidya's `types.cyml` structural facts are still stamped cycc 6.6.1 / 1,247,608 B. |
| **W1 status** | n/a — reactive-window bookkeeping belonged to the v6.5.x minor and is retired with it. v6.6.x reserves `.2`–`.6` as its repair window instead; see [roadmap.md](roadmap.md). |
| **Open decisions for the user** | **None blocking.** The three standing items are recorded in [roadmap.md](roadmap.md) *Open questions* WITH stated defaults, so work starts under them rather than waiting: the self_compile budget (owned by the later perf track — ⚠ note both halves of the old candidate pair, ≤700 ms and ≤1.20 MB, are now exceeded at 743 ms / 1,251,864 B (6.6.3), so it is an input to re-decide, not a target that was missed), the growth-tax audit (review with it), and macOS concurrency ordering (default: keep last; pull forward if a consumer appears). **Waiting on the user's git calls (sibling repos, not blocking cyrius):** commit + tag **patra 1.14.3**, **sigil 3.12.18** and **mirshi 1.11.2** (patra/sigil are pinned to 6.6.4, so tag them AFTER the cyrius 6.6.4 tag; mirshi stays on its 6.6.2 pin); commit the CHANGELOG placement fixes in bayan, mabda, sandhi, sigil, vani and yantra (each release shipped with its own entry buried); commit or discard the blank-line diffs in ganita, niyama and sankoch; discard the stale `cyrius.lock` edits in mabda, sigil and sankhya (their content differs from what shipped green); review samvada's 11 uncommitted doc files (dated 09-10, origin unconfirmed); commit the two new issue files (cyrius, kavach). |

> Full arc detail, the 12-slot sequence and per-arc length estimates live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
