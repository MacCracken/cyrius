# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.6.3 — SHIPPED** (tag `6.6.3` @ `ab77e241`, 2026-09-12). The repair release for the six issues the 6.6.2 ecosystem sweep filed, plus a refold of all twelve stdlibs. `#inline` no longer disarms every following `#derive` (a 10-repo hard block, five of them with no local fix); `continue` binds to its own loop at every nesting level; `CYRIUS_DCE=1` no longer emits a PT_LOAD the code was never patched against; `cyrius distlib` no longer OOM-kills CI runners (20,320 MB → 57 MB); `cyrius.lock` is emitted sorted so a committed lock verifies on another machine; `#derive` composes with `public` and the generated accessors inherit it. ⚠ **CI went red on the first push and the release was re-cut at the same version:** the `#inline` fix had reached one of the seven compiler forks — native aarch64 failed to compile, and `#inline` was silently inert on aarch64, both Mach-O forks, PE and (pre-existing) cx — and a committed file named `c -l)\|XX\|` aborted the Windows checkout. Both were fixed and gated before the tag. **Prior: 6.6.2 — SHIPPED.** |
| **cycc** | x86 **1,251,888 B** — **6.6.4 IN FLIGHT** (bite 3 of 6 landed; 6.6.3 shipped at 1,251,864, so +24 B). Bite 1 (string-literal pack widening, cx movhi slots, `#deprecated` on tail calls) was byte-different at the SAME size; bite 2 (`_vis_check` on eight resolution paths, instance polarity, array stamp, method `self` on inline structs) +16 — three new globals; bite 3 (`public` marker scoped to its item, pass-2 + PARSE_PROG clears, destructure, derive codecs inherit `public`, PARSE_PROG-path stamps, the enum-derive `#ifdef` clobber) +8 — the `public fn _cy_enum_name_eq` literal grew by 7 bytes in RW data (measured by reverting each file; `_PUB_CAN_ARM`'s code is absorbed by text-segment page padding). **seed-derive GREEN after every bite** — `seed → cybs → cycc` byte-identical. ⚠ All three are shared-frontend changes, so every fork inherits them; the real-hardware ecb/ach/cass/pi leg runs at the release gate, not per bite. Last shipped: **v6.6.3, 1,251,864 B**. |
| **Bootstrap / cross-OS** | **v6.6.3: ecb · ach · cass · pi all `SELFHOST_OK` + `LIBTEST_OK` on REAL hardware**, run ONE AT A TIME — fixed /tmp + remote paths clobber under concurrency. ⭐ `pi` is the load-bearing host this release: 6.6.3 changes `src/backend/aarch64/fixup.cyr`'s data-segment vaddr, and compiling the aarch64 fork locally proves it PARSES, not that cycc reproduces itself on ARM. seed **29,024 B** → cybs → cycc byte-identical. A green CI check is NOT this leg. ⭐ **v6.6.3 — the pi leg now ALSO self-hosts `src/main_aarch64_native.cyr`, the fork ARM users actually install.** It had only ever self-hosted `main_aarch64.cyr`, the CROSS source — the same shape as the macOS rot this leg exists to prevent, and the reason the full release gate passed GREEN while `#inline`-before-a-declaration failed to compile on the native ARM fork. Mutation-proven load-bearing: pointing the second compile back at the cross source makes pi's SELFHOST fail. ⭐ **6.6.4 (bite ④): the install store is verified against its TAGS** — `sh scripts/verify-store.sh` reports every tagged slot; the maintainer's `~/.cyrius` was restored (6.6.0–6.6.3 OK, stamped) after it was found stale in both directions. `check.sh` now stages a throwaway `CYRIUS_HOME` from the tree. |
| **Gates** | **v6.6.3 — check.sh 242 / 0 GREEN + 157 shell gates; release-gate.sh GREEN.** **Eight** new gates this release (six shell + two `.tcyr`, derived from `git diff --name-status 6.6.2 6.6.3`), every one mutation-proven in both directions: `deps_lock_sorted` (behavioural — the order IS the artefact, unlike its structural sibling `deps_family_expansion_ordered`), `distlib_leaf_lookup_memory` (peak RSS; mutation takes it 44 MB → 1,614 MB), `derive_with_public` (5 axes — axis 5 is the anti-vacuous half that catches an unconditional `public`), `dce_data_vaddr_frozen` (STRUCTURAL deliberately — no in-tree fixture crosses a 2 MB bucket under DCE, and the reason is written into the gate), `directive_fork_parity` (a directive must be consumed AND armed in all seven compiler forks — axis 2 compiles the same fixture twice per fork and requires the outputs to differ, because byte-identical output is the proof of inertness), `tracked_paths_portable` (every tracked path must check out on Windows; it reads the INDEX, which is what CI checks out), plus `inline_before_derive` and `nested_continue_binds` as .tcyr. `crossos/inline_directive.tcyr` also gained a real `#derive`, so the ecb/ach/cass/pi leg now covers the `#inline`+`#derive` interaction. ⚠ **DERIVE the shell-gate count** — `find tests/gates -name '*.sh' \| wc -l`. |
| **Corpus** | **308** `.tcyr` (**70** in `crossos/` — ⚠ **238 (77%) never run on non-x86 hardware**: the cross-OS leg's selector is that directory, so anything outside it is x86-only until CI's full-corpus aarch64 job sees it — the cross-OS selector is that DIRECTORY, the `vr01_` prefix was retired at v6.5.11) · **103** `lib/*.cyr` (`boxed.cyr` restored the boxed tagged-union primitives) · **84** `programs/*.cyr` · api-surface **5,156** · heap map **102** regions (gate-derived; ⚠ a raw `grep -cE '^#   0x' src/main.cyr` says **145** because it also matches 18 `FREED` markers — run `sh tests/gates/memory/heapmap.sh`). |
| **Active minor** | **v6.6.x** — value-form `Result` + language ergonomics. Opened at v6.6.0; **v6.5.x closed at v6.5.73** (there is no `.74` — that number was cut in error and re-cut as 6.6.0). Slot plan in [roadmap.md](roadmap.md): `.2`–`.6` are a reserved **repair window**, then the proposal queue, then the committed ergonomics list. |
| **Shipped this minor** | **.0** the `Result` / `Option` / `Either` **value-form flip** — construction allocates ZERO bytes (was 1600 B per 100 `sock_send`), shipped WITH the ecosystem (8 sibling stdlibs migrated at source, pin-bumped, released and re-vendored); it changed the ARITY of every value, so `payload()` and `tagged_new()` are DELETED; plus top-level `var t, v = f();`, the mixed-return diagnostic, and ⛔ **the v6.5.57 aggregate-copy P0 found at the cut** (live seventeen releases, invisible to the fixpoint because cycc's own source never uses the shape) · **.1** the two filed issues — `f64_exp`/`f64_exp2` **NaN for ±inf** in BOTH implementations (polyfill + emitted x87; the guard must precede the range reduction, which computes `inf - inf`) and AGNOS `clock_now_ns()` reading the **frozen `timer_ticks` clock** (#40 → #95 `rdtsc`) — plus the `cyriusly install` **ETXTBSY** failure (`cp` writes through the directory entry; now temp + atomic rename) and five stdlib folds. · **.2** the repair release — `tagged_new()` restored for a live consumer the deletion survey could not see; two SIMD P0s (a destination read from a frame slot nothing wrote; a top-level intrinsic that SIGSEGV'd); `map_u64` answering untruthfully for its own sentinel keys on five paths; `object;` builds exporting libc-reserved names; a vacuous SIMD gate; `cyrius build` overwriting the running compiler; `check.sh` reporting green over seven gates that never ran · **.3** the six issues the 6.6.2 ecosystem sweep filed (see *Version*), plus the fork-parity and Windows-checkout CI repairs. ⚠ Closed-minor detail for v6.5.x and earlier lives in `completed-phases.md` + the CHANGELOG; this row carries the CURRENT minor only, and was the v6.5.x list until the 6.6.1 refresh. |
| **In-flight** | **6.6.4 — the repair release for the six issues filed after the 6.6.3 handoff.** Bites land one per commit in this order: ① string literal ≥ 64 KB (+ cx addresses past 0xFFFF, cx `x*2`, closure crash-after-error, `#deprecated` tail call) ✅ · ② `&_private` + seven sibling resolution paths, generic-instance polarity, private arrays, method `self` on inline stack structs ✅ · ③ `public enum` marker leak (+ struct/union/impl/`use`, pass-2 `var` skip, destructure, derive codecs, PARSE_PROG-path stamps) ✅ · ④ `install.sh --refresh-only` writes a released slot (+ pulsar/lsp writers, check.sh throwaway home, `verify-store.sh` + the live store restored) ✅ · ⑤ `cyrius deps` silent re-lock (+ bare `deps --lock` dropping commit pins) · ⑥ raw x86 syscall numbers on aarch64 (+ per-target `O_*`, `cbt/build.cyr:995`, hashseed/sigil raw numbers, patra/sigil at source + re-vendor). Then release gate → bench → `version-bump.sh 6.6.4`. |
| **Next up** | **Slot `.3` — per-item `private` silently privatises the whole file.** ⚠ **Still next, but the version numbers have drifted from the slot labels:** 6.6.3 went to the sweep's repair set, so this lands as **6.6.4** at the earliest, and the roadmap's `.3`–`.6` labels have not been re-slotted (that is the user's call). `.2`'s pinned occupant (the `cyrius build` overwrite guard) SHIPPED in v6.6.2 along with a much larger repair set than the slot anticipated. `_TL_VIS` handles token 153 by calling `_PRIV_MARK(FM_FILEID(...))` — a FILE-level flip — so `private fn h()` compiles clean and privatises the entire file, `main` included. Twelve-plus releases live, no diagnostic. Default taken in the roadmap: make the per-item form a hard error pointing at the file-level declaration. |
| **Open queue** | **4 open issues** (was 6 at slot open — four closed by bites ①–④, two FILED from bites ②/③): the remaining hisab deps-relock filing + the 2026-09-12 raw-syscall filing are the 6.6.4 bites ⑤–⑥ above; `2026-09-13-fn-local-global-slots-shadow-other-files.md` (Medium, pre-existing, surfaced by bite ③'s stamps: a fn-local struct literal / oversized array is a GLOBAL slot in the flat namespace and shadows other files' globals — before the stamp that was a silent miscompile, now a misattributed diagnostic); the new `2026-09-13-private-impl-method-forward-call-fail-open.md` (Medium) is the one residual bite ② could not pack — a 7-fork pass-1 change (impl bodies are brace-skipped in pass 1, so a forward call to a private impl method resolves with no fileid and `_vis_check` fails open). ⭐ **Read a filing's scope as a report about what the reporter builds, never as the bug's boundary** — every 6.6.4 filing widened under investigation (one path → eight; one leak → six declaration kinds; one writer → four). |
| **Ecosystem** | **The 6.6.2/6.6.3 sweep CLOSED 2026-09-12.** Derived from committed HEADs: **125** repos with a `cyrius.cyml` — **14** on 6.6.3, **110** on 6.6.2, and **1** below (agnos 6.6.1, another agent's). hisab is also another agent's and is **not** to be updated. dhvani **2.2.4** holds the sound stack's versions, with hisab kept at **2.11.2** (naad 2.2.4 and svara 3.5.5 were rebuilt and pass against it); jalwa **1.4.5** aligns its shared pins to dhvani. **Still open, recorded but not fixed:** same-arity duplicate fns (agnostic 22, mehman 19, hadara 2, aegis 1, kriya 1 — warnings only); ~30 `path = "../sibling"` + stale-`tag` pairs (deliberately not done). ⚠ **Stale docs:** `ecosystem-migration-6.6.2.md` still reads "~11% DONE, DO NOT ARCHIVE", and its outlier #4 is superseded by the open issue; vidya's `types.cyml` structural facts are still stamped cycc 6.6.1 / 1,247,608 B. |
| **W1 status** | n/a — reactive-window bookkeeping belonged to the v6.5.x minor and is retired with it. v6.6.x reserves `.2`–`.6` as its repair window instead; see [roadmap.md](roadmap.md). |
| **Open decisions for the user** | **None blocking.** The three standing items are recorded in [roadmap.md](roadmap.md) *Open questions* WITH stated defaults, so work starts under them rather than waiting: the self_compile budget (owned by the later perf track — ⚠ note both halves of the old candidate pair, ≤700 ms and ≤1.20 MB, are now exceeded at 743 ms / 1,251,864 B (6.6.3), so it is an input to re-decide, not a target that was missed), the growth-tax audit (review with it), and macOS concurrency ordering (default: keep last; pull forward if a consumer appears). **Waiting on the user's git calls (sibling repos, not blocking cyrius):** commit the CHANGELOG placement fixes in bayan, mabda, sandhi, sigil, vani and yantra (each release shipped with its own entry buried); commit or discard the blank-line diffs in ganita, niyama and sankoch; discard the stale `cyrius.lock` edits in mabda, sigil and sankhya (their content differs from what shipped green); review samvada's 11 uncommitted doc files (dated 09-10, origin unconfirmed); commit the two new issue files (cyrius, kavach). |

> Full arc detail, the 12-slot sequence and per-arc length estimates live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
