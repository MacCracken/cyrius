# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.5.36 — gate GREEN on all 5 steps** (fixpoint 1,178,864 B; seed→cybs→cycc byte-identical; `check.sh` 199/0; corpus 284/284; cross-OS **ecb + ach + cass + pi** each `SELFHOST_OK` + crossos 55/55 on REAL hardware; bench `self_compile` **711 ms** vs `.35`'s 716, flat). ⛔ **Two CRITICAL defects that were live in shipped releases.** (1) **Enum constants ≥ 2^62 were silently corrupted in `.31`–`.35`** — `enum { K = 0x7FFFFFFFFFFFFFFF }` was **-1**, `--strict` clean, corpus green; szal found it. The fold table packed `(1 << 63) | val`, i.e. 65 bits in a 64-bit slot, and v6.5.32 papered over it with a bit-62 heuristic that **wrote the resulting truncation down as a specification**. Presence is now OUT of band (`_vecp_base`, lazily allocated per the `_fnt_tparams` precedent — no new heap region, no fork edit, no `_grow_g*` change); `ENUM_CONST_VAL` is deleted. (2) **On ELF-aarch64 `sys_pause()` issued `flock` and `sys_signalfd()` issued `fsync`** — the stdlib declared the NATIVE 73/74 but ESYSXLAT's x86-compat rows rewrite 73→32 and 74→82, and a compat row cannot tell a native number from the x86 number it matches. Both moved to the ≥1000 private alias band with rows appended **last** in the chain. kybernet's whole aarch64 target was non-functional; every gate was green. Prior: **6.5.35 — SHIPPED**. |
| **cycc** | x86 **1,178,864 B** — +16 B from `.35` (the enum presence table is lazy; the aarch64 alias rows are outside `main.cyr`'s closure). Seed 29,024 B → cybs → cycc byte-identical. |
| **Bootstrap / cross-OS** | **ecb** · **ach** · **cass** · **pi** — all `SELFHOST_OK` + crossos **55/55** on REAL hardware at **6.5.36**. ⚠ **pi was UNREACHABLE on the first attempt** (`agnosarm.local` did not resolve; ping and ssh both failed) and the gate was **held open, not reported green** — an unreachable host is not a passing host. It came back and passed. ⭐ pi is the leg that matters most this release: the alias-band fix is aarch64 codegen, and qemu-user is not a substitute (it could not surface the v6.4.42 aarch64 epoll defect). Under qemu the corpus is 54/55, the one failure being `thread_detach` — verified identical with the `.35` emitter, i.e. a qemu thread-emulation limit, not a regression. ⚠ Run ONE host at a time — fixed /tmp + remote paths clobber under concurrency. |
| **Gates** | `check.sh` at `.36` · **82** shell gate scripts (derived — COUNT, never increment). ⭐ **`enum_negative_value.sh`'s axis 5 was REWRITTEN, and why matters**: it required every reader to route through the shared `ENUM_CONST_VAL` decoder — a guard against hand-rolled masks that was **correct about the symptom and wrong about the disease**, because the decoder itself could not work. It stayed GREEN through all five corrupted releases: *a gate that pins the mechanism rather than the property passes while the mechanism is wrong.* It now asserts presence is out of band (no in-band tag, no mask on an enum-table access, every reader tests `GVECP`) plus a behavioural axis 6 over the full i64 range. |
| **Corpus** | **284** `.tcyr` in 15 buckets (**55** in `crossos/`) · **101** `lib/*.cyr` · **97** `programs/**/*.cyr` · api-surface **5089**. New: `frontend/enum_const_full_i64_range.tcyr` (16 assertions bracketing 2^62; mutation-proven — the released 6.5.35 fails 9 of them) and `crossos/syscall_alias_band_and_ioctl.tcyr` (mutation-proven: the pre-fix aarch64 build fails the signalfd assertion). |
| **Active minor** | **v6.5.x** — generated-code quality (opened at v6.5.0; **v6.4.x closed at v6.4.86**) |
| **Shipped this minor** | **.0** file-scoped `public`/`private` · **.1** arity-aware overload dispatch + 4 stdlib folds · **.2** the `ir_const_fold` jump-span miscompile (**IR=3 mismatches 35 → 8**), `xrmdir`, yukti 2.3.0's six agnos ABI defects · **.3** diagnostic lines after `include`, `install.sh` ETXTBSY strand, `version-bump.sh` same-version early exit · **.4** `vec_sort_by`/`vec_select_nth` (the agnosai filing), sigil 3.12.2 / yukti 2.3.2 / sandhi 1.9.8 / mabda 4.0.8 folds, the vacuous sign-efi gate, fold-version table rot · **.5** the `CYRIUS_IR=3` switch-dispatch miscompile (filed as LASE, actually **DCE**) — closes `switch_dispatch`, one of the eight residuals .2 recorded, so **7 remain** — + bayan 1.4.0 · **.6** `sys_exit_group` on all 3 syscall peer families + `async_await_readable_ms`; macOS-x86's untranslated 231 → silent SIGSYS; `proj-tcyr`'s 8-bit exit truncation (256 failures scored PASS); `cyrius fuzz` blind to scaffolded harnesses; sandhi 1.9.9 + vani 1.1.3 folds · **.7** hisab's `alloc_reset` allocator-invalidation; the aarch64 `fchownat` private-alias band (≥1000, both real candidates owned); `#@incdir` file-relative includes with the CVE-16 escape closed at byte-0 + relative-only; `sys_chdir` called-but-undefined; `signal_default` + the `x*` family; subfolder callout for test/bench/fuzz; missing-output-dir bare FAIL · **.8** szal's `thread_join` lost-wakeup deadlock (P1); `thread_create_detached` (210 MB VA leak → 0); the `i64::MIN` formatter class across 12 sites incl. 2 in the compiler + sakshi 2.4.8; agnos `#97 chan_op` minted (`#96` fork deliberately not); `coverage`'s four fail-open/truncation defects; the doc/vet/deny/audit fail-open family; `distlib --all/--check`; the cross-OS gate batched + honest; bayan 1.4.1 fold · **.9** agnos `CH_ENDOW` + `sys_spawn_path_env` (both blocking agnos bite 7); the three-state futex mutex (392→48 ns, and W1 item 3's stated prerequisite was wrong); the growable arena + exhaustion policy — an exhausted arena was a SIGSEGV layers away, and adopting arenas made a leak into a crash · **.10** `distlib`'s `.deps` sidecar under-reported (8 of 12 for setu) and thereby DISABLED `cyrius deps`' consumer check — now declaration ∪ include-scan, base bundle only; `alloc_via` was two-thirds call plumbing (15.1 → 11 ns) · **.11** test suite → subfolders (`vr01_` prefix → the `crossos/` **directory**) with three silent-green readers closed first, incl. CI scoring a fake PASS because cycc on EMPTY stdin exits 0 with a runnable binary; `MAP_ANONYMOUS` was Linux's 32 on Darwin (`mmap_anon()` → 0 for every caller); **macOS threading now RUNS the worker** via a serial fallback — the wrong constant had been the accidental safety net for the missing backend, and fixing it turned the no-op into SIGSYS; `dir_list`/`is_dir` per-call 4 KB → stack (`dir_walk` 4443 → 239 B/entry) · **.12** sigil **3.12.4** (written this release — closes 3.12.3's residual by localising the bignum engine scratch; the banking existed for a cyrius quirk WE fixed at 6.3.15 and never propagated) — an RSA verify BYPASS (888/400,000 forged accepted upstream; partial by its own account, PSS still banked); `CYRIUS_STACK_ARRAYS=0` silently re-opened it (our flag revoking their fix) → now warns; `lib sync` dropped package DIRECTORIES (`unicode`) at exit 0 — the un-updated twin of a v6.1.x `cyrius deps` fix — plus fail-loud on any unsatisfied declaration; cx emitted a SELF-RESTARTING .cyx at exit 0 for an undefined call (1026 loops; native gated since v6.3.2, cx never got it); tree walks followed symlinks (**41 passed for a corpus of ONE**) → `is_symlink` + depth cap, symlinked FILES still run; `bench_string` 56-byte stack overflow SIGSEGV since v6.3.15, invisible behind the non-blocking bench step; `*.fcyr`/`*.bcyr` DIRECTORIES compiled as files; `dir_list_into` (0 B listing) · **.13** cx `callptr` (opcode 105 — three defects that had to land together: `ELOAD_FN_ADDR` a return-0 stub, `main_cx.cyr` never setting `_fixup_base`, `ECALLIND` a hard error) + agnos `#98 ptrscan` · **.14** the tail-call frame-escape P0 (`return f(p)` freed the frame `p` pointed into; the v5.8.16 guard was SYNTACTIC and missed every aliased form) + sigil **3.12.6** (the whole PSS workspace localised — PSS verify had the same authentication bypass 3.12.3 fixed for v1.5, and the compiler defect is why three releases could not close it) + `distlib`'s bundle self-check, which compiled to `/dev/null` and had never run · **.15** the macOS-arm64 constant + syscall gap closure (**ecb full corpus 11 → 6**): ten constant VALUES the aarch64-Linux peer leaked onto Darwin — ESYSXLAT renumbers NUMBERS, never VALUES, and Linux `SIGCHLD` 17 IS Darwin's `SIGSTOP`, so a child-exit wait silently watched for a stop; `SIGPWR` was 30 = Darwin's `SIGUSR1`; plus the macho ESYSXLAT gap set (`pipe2`/`fchmod`/`faccessat` never mapped) where **Darwin's `pipe` returns both fds in REGISTERS** so a renumber alone was insufficient; and `sys_access` passed 3 args where Darwin validates the 4th — every macOS call returned −22, which had been silently SKIPPING `tls_native_scaffold`'s entire OpenSSL block (a vacuous pass, now honestly red); sakshi 2.4.9 + both fold tables de-rotted. **Reverted at .15**: a `Result` unboxing that passed EVERY gate and still reported a failed file open as SUCCESS in a retaining loop. ⭐ **ach's leg of the release gate then caught TWO of the .15 fixes one fork behind** — `SIGPWR = 30` was still live on the x86-macOS peer (30 IS Darwin's `SIGUSR1`), and `sys_fchmod` **SIGSYS'd on every Intel-Mac** because `EMACHO_SYSXLAT` had no `91 → BSD 124` route, so the number reached Darwin unprefixed. Both fixed at source; `sys_fchmod` had been broken on ach since it existed and only surfaced because .15 added the first crossos assertion that CALLS it · **.16** macOS `uname`/`sysinfo` per-arch routes, sakshi 2.4.10, two issues filed from measurement · **.17** hisab's capturing-closure SIGSEGV, a syntax error in an UNCALLED fn, `distlib` rejecting correct bundles, sankoch 2.7.7 · **.18** `platform/fdlopen` closed (the last full-corpus failure on both Macs; `#naked` had been consumed-and-IGNORED by two forks since v6.2.27), `cyrius fmt` corrupting string literals — it was rewriting **1,239 lines of this repo's own `src/main.cyr`** — sigil 3.12.7 · **.19** five consumer filings (four agnosai, one majra) + **CVE-39** (40 of 103 truncated constructs SIGSEGV'd or HUNG cycc) + **CVE-40** (recursive descent had NO depth bound — PROVEN stack exhaustion, ~16 KB/level; it took FOUR passes to cover the class) · **.20**–**.33** (see CHANGELOG — Slots 0/0b/1b, bands A–D, the D2/D3 unpinned releases, band T's consumer/DX cluster, and the `.33` cybs `parse_if_else` bootstrap P1) · **.34** **band E, the IR substrate**: three IR=3 miscompiles, one per pass (LASE width elimination, `ir_const_fold` state carried across its own NOPs, `ESTOC` recording no IR node so DCE killed the field store's address register) taking default-vs-IR=3 divergence to **0 of 282**; plus `CYRIUS_PKG_VERSION` from included files, `cyrius port --language=python`, and the bayan/patra folds |
| **In-flight** | **`.35` — COMPLETE, gate green on all five steps; awaiting the maintainer push + tag.** ⚖️ **One thing deliberately NOT shipped, recorded with its evidence so the next attempt starts from it**: extending only values defined BEFORE the loop top (`first < t`) passes the corpus **282/282** and takes cycc a further 4,096 B smaller — but the rule assumes any local whose first reference is inside the body is redefined every iteration, and this pass's byte scan cannot prove that. It sees `48 89 85` (store) and `48 8B 85` (load) but has no DOMINANCE information, so it cannot distinguish a definition that dominates all in-body uses from one guarded by a branch. v5.6.22's defect was exactly this class and took releases to surface, so 282 passing tests is not sufficient evidence. That refinement, made sound, is where an in-loop runtime win would come from. |
| **Next up** | **Band G — Slot 6, SIMD register residency**, the substrate's payoff. ✅ Item 3 shipped at `.24`; **item 1** (register-resident value-form f64v arithmetic) is genuinely unbuilt — all 15 emitters in `float.cyr` are memory→register→op→memory loops, the ymm one included. ⛔ **Item 2 is NOT a build**: the wrapper inliner is LIVE and fires today, gated to generics — it is a gate-widening question. ⚠ Re-derive the simd-site count at slot entry (last measured **29**, not the 25 an older pin carried) and check `f64v4_fmadd`, which item 3 never widened. ⚠ Band F left the **vector register class** unbuilt: the picker's byte matcher still only recognises REX.W mov to/from `[rbp+disp32]`, so no xmm/ymm local is a candidate — that is band G's natural home now that time-sharing works for the integer file. |
| **Open queue** | **RE-DERIVED at slot close 2026-08-28: 12** open issues · **2** proposals · **351** archived. `.36` closed and archived **eight**: both Criticals (enum bit-62, aarch64 ESYSXLAT), `sys_ioctl`, `getenv`'s 8 KB window, the TLS CA bundle, the two agnos wrapper filings already marked Built, and the cross-OS-full-corpus premise. ⚠ Untouched and worth reading before `.37`: **owl `private` fns still collide across files (High)** — `private` is enforced on *reference* but not on *definition*, so another file's same-named fn replaces a private helper including for that file's own internal calls, and the override **bypasses the arity check** with no diagnostic at all. |
| **W1 status** | ✅ **CONSUMED — all 13 spent (.4 → .16).** ⛔ And **W2 is consumed too**: budgeted 3 at `.19`–`.21`, but `.19` shipped as five consumer filings and `.20`/`.21` are now Slots 0/0b. **W1 (13) + W2 (3) = 16 reactive releases of budget; `.4`–`.19` is exactly 16 releases of reactive spend.** Items 8 and 9 never landed and are re-homed to `.22` (Slot 1b). ⚖️ **Maintainer budget call surfaced in roadmap.md**: the observed reactive rate is ~80 % (16 of 20 releases) against W3+W4's remaining 9, so either widen the windows or let filings displace releases explicitly rather than silently. |
| **Owed to the maintainer** | **SIX decisions open** — re-derived 2026-08-22; the previous row said "FIVE" and then listed six, and every figure in it was stale. (`break` semantics was SETTLED 2026-08-12 — C semantics, recorded in guide + vidya + CHANGELOG; do NOT re-open. The `.21` packing question is SPENT — it shipped.) **1. Slot 9 design** — the committed "unbox the scalar case" was implemented at `.15` and **reverted**; both storage relocations are disproven, so the survivors are **escape analysis or a scope-tied arena with reclaim**, a different size class, and the slot cannot be scoped until this is chosen. **2. Cross-OS full-corpus default flip** — ecb/ach/pi are at **282/282**; cass sits at **32** (not the 7 this row used to say), labelled PE-incompatible by the runner and needing triage into skip-with-a-named-reason versus real. **3. Reactive budget** — ~80 % observed against W3+W4's 9 remaining. **4. stiva Half B** — re-confirm it is wanted before spending a release on a compiler-level CPS transform that `.26`/`.27` evidence says is unjustified for the two filed features. **5. Per-item `private`** — `private fn h()` still compiles with **no diagnostic** and privatises the whole file including `main`; **eleven** releases live, and the new owl High filing now leans on this area. **6. `archived/README.md` indexes 38 of 343** — every 6.5.x-era archive is unindexed; backfill, or drop the all-files promise in `issues/README.md`. Plus the two standing answered questions, re-derived: self_compile **716 ms** against a candidate ≤700 ms cap (**over it**), and cycc **1,178,848 B** against ≤1.20 MB, with Slots 6/9/11 still to land. |

> Full arc detail, the 12-slot sequence and per-arc length estimates live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
