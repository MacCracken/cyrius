# Cyrius — Current State

> Refreshed every release. CLAUDE.md is preferences/process/procedures (durable);
> this file is **state** (volatile). Bumped via `version-bump.sh` post-hook.
>
> **Consolidated 2026-06-08 (v6.1.4):** the per-patch session-close log
> (104 entries, ~5,600 lines back to v5.x) + the stale v6.0.4-frozen
> structured sections were pruned — that detail is canonical in
> [`CHANGELOG.md`](../../CHANGELOG.md) (per-patch) and
> [`completed-phases.md`](completed-phases.md) (arc retrospective). This file
> now holds only the **active cycle** + current state.

## Current state

| | |
|---|---|
| **Version** | **6.3.9** (v6.3.x cycle — **Language Refinements**; **generic FUNCTIONS — i64 monomorphization** — `fn foo<T>(...)` over i64, the first half of the 2-release generics arc (user 2026-06-30: generic structs + non-i64 + explicit syntax split to **v6.3.10**, sharing one instantiate-once engine). On the proven AR-01 token-replay substrate, gated behind `_MONOMORPH_OK` (default off) → **cycc self-host byte-identical** (differential vs 6.3.8 confirms cycc — which uses no generics — unaffected). `_capture_tparams` (replacing `SKIP_GENERICS`, gated) records `<T>` into lazy-alloc `_fnt_tparams` (0=non-generic, no heap-map slot). Scoped `T→concrete` binding (`_tp_n0/_tp_c0/…`, token `<0`=-width / `>0`=sid) consulted by `_tp_resolve` at 4 sites (param-type / local `:T` / `slice<T>` / `sizeof`), each a guarded prefix → inert until bound. **ARCHITECTURE: the base fn IS the i64 instantiation** — body emitted ONCE with `T→i64` bound (`_bind_generic_call(fi,-8,-8)` before `PARSE_PROG`), so i64 calls are plain direct calls (dedup-by-construction, no instantiate-once needed). Positional inference; `: T` return accepted; ceiling 16→32. **LOAD-BEARING FIX: `: T` return triggered the struct-retptr ABI** — the pre-param-loop return-type rough-scan saw `: T` as neither scalar nor struct (`FINDSTRUCT(T)=0`) → default `stash=8` → reserved arg-reg 0 for a hidden retptr → shifted user params by one (`id<T>(x:T):T{return x}` stored x into rsi, read 0; disasm-confirmed 2 stores). Fix=recognize a tparam as i64-scalar (`_is_tparam_of`) in the rough-scan. **`generic_fn_i64.cyr` + `_generics_fn_i64_gate` (check.sh 104→105): exit 42 under flag, REJECTED off.** Removed dead `_skip_body_tokens` (body-deferral superseded by base-as-i64). **Self-host fixpoint + differential + seed→cybs→cycc byte-identical; check.sh 105/105; generic fns correct on x86 + aarch64(qemu); ecb+cass+pi SELFHOST_OK; bench 520 ms; cycc 1,089,248→1,094,000 B (+4,752).** NEXT: v6.3.10 = generic structs + non-i64 instantiate-once + explicit `foo<i64>(x)`. See CHANGELOG [6.3.9].) PRIOR: **6.3.8** (v6.3.x cycle — **Language Refinements**; **closures capture by value** — the planned v6.3.7 follow-up. A closure body referencing an enclosing local (a *free variable*) is now captured **BY VALUE** into a heap env object `[fn_ptr, cap0, …]` at construction; the closure VALUE *is* that object, dispatched via `callptr` **AUTO-DISPATCH** (callee tagged `CLOSURE_TYID` 0x40000003 → load fn ptr from `[obj]`, pass obj as the hidden env arg1 — call sites identical to non-capturing). Built on the v6.3.7 local-table snapshot/restore. `_cl_prescan_captures` walks the body on a throwaway cursor: a free var = FINDLOCAL-fails + not a param/shadow + not an `IDENT(` call name + IS in the enclosing snapshot. A capturing closure gains a hidden env param (slot 0; user params shift +1); a captured read = `load64(env+(1+capi)*8)`. Requires `include lib/alloc.cyr` + `alloc_init()` (fail-loud if `alloc` unresolved). Non-capturing closures stay bare fn ptrs (no alloc). FIXED: stale-name scratch slot → false 'duplicate variable' (the shared local tables aren't cleared per-fn → a later same-named `var` tripped duplicate-decl; anonymize the scratch + `callptr` spill slots `name=-1`). PE: capturing errors fail-loud (env + `ECALLPTR_PE` align unverified) — non-capturing works. **`tests/tcyr/closures_capture.tcyr`** (7 cases). **Self-host byte-identical (closure-path-only; differential vs 6.3.7 confirms cycc unaffected); seed→cybs→cycc OK; check.sh 104/104; capture 7/7 on x86 + aarch64(qemu) + regenerated native-aarch64 self-hosts byte-identical & runs capture 7/7; PE non-cap runs / cap fails-loud (wine); closures.tcyr 8/8; ecb+cass+pi SELFHOST_OK; bench 507 ms; cycc 1,081,696→1,089,248 B (+7,552).** Closures arc COMPLETE (E-base .7 + E-capture .8). NEXT: v6.3.9 real generics/monomorphization on the Phase-0 substrate. See CHANGELOG [6.3.8].) PRIOR: **6.3.7** (v6.3.x cycle — **Language Refinements**; **non-capturing closures `|x| body` actually work** — slot-entry premise-check found them a SCAFFOLDED-BUT-NON-FUNCTIONAL, untested, unused feature (corpus 'closure' hits were all `|x|` math abs-value; parse code existed but mis-emitted), NOT 'working closures missing capture.' Planned base-first split (user 2026-06-29): this slot fixes the base, **lexical capture = v6.3.8 follow-up**. FIVE codegen bugs, all from the closure being an anon fn emitted INLINE in the enclosing fn while sharing its mutable state: (1) PARAM PASSING (`|x| x`→0) — inherited `_cur_fn_regalloc` → ESTOREPARM shifted params past phantom callee-saved slots + body read stale regs; fix=save/reset/restore all `_cur_fn_*`; (2) FRAME never EPATCHFRAME'd (ESUBRSP placeholder stayed 0 → 0-byte frame clobbered param slot 0 on first push; v6.2.41 'output-identical' claim was untested); (3) zero-param `||` lexed as logical-or, never reached the parser; (4) enclosing-local CLOBBERING — closure reused the SHARED local tables (names/depths/types/slice `S+0x5D9D000`+) from slot 0, overwriting the enclosing fn's locals; fix=snapshot/restore the 4 tables; (5) regalloc post-pass byte-scan REWROTE the inline closure → SIGILL; fix=`_cur_fn_has_closure` skips the post-pass. + unique `__clNNNNN` naming (was single-digit → 11th closure aliased 1st in REGFN). **`tests/tcyr/closures.tcyr`** = first-ever closure tests (8 cases). Guide Closures section + vidya `closure_inline_emit_shares_enclosing_fn_state`. **Self-host byte-identical (closure-path-only; differential vs 6.3.6 confirms cycc unaffected); seed→cybs→cycc OK; check.sh 104/104; closures run on x86 + aarch64(qemu) + PE(wine); ecb+cass+pi SELFHOST_OK; bench 501 ms; cycc 1,079,440→1,081,696 B (+2,256).** Roadmap: v6.3.7=base closures, inserted v6.3.8=lexical capture, slots shift (generics→.9, async/float→.10, gates→.11-13, closeout→.14). See CHANGELOG [6.3.7].) **Earlier releases (6.3.6 back to v5.x): see CHANGELOG.md** — trimmed from here (state.md keeps current + ~2 priors; the rest is canonical in CHANGELOG). |
