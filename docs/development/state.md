# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.5.14** — **one codegen P0, shipped complete**: `return f(p)` freed the frame `p` pointed into. v5.8.16 §8 had guarded this by scanning the arg list for a literal `&`, so the aliased form (`var p = &buf; ... return f(p)`) walked past it — a silent wrong answer, reachable only at **≤6 args** (cycc already declines TCO above 6, which is why the same helper called with 7 was fine). Found because sigil could not localise its RSA-PSS buffers for THREE releases; the shared lanes it forced them to keep were an authentication bypass. Also: `distlib`'s bundle self-check compiled to `/dev/null` and had therefore **never once run**. Prior: **6.5.13** · **6.5.12** — sigil 3.12.3 (an RSA signature-verification BYPASS) plus the deferred-repair queue. Theme, sharper than .11's: **five of seven fixes had been MASKED** — by a wrong constant, a non-blocking gate, an opt-out flag, or a fix that landed on only one fork. Prior: **6.5.11** — the test suite reorganised into subfolders (`tests/tcyr/<bucket>/`, `tests/gates/<bucket>/`; the `vr01_` prefix retired for the `tests/tcyr/crossos/` **directory**) plus a catch-up batch off the open queue. Theme: three of five fixes were MASKED by something else broken — most sharply, a wrong `MAP_ANONYMOUS` was the accidental safety net for macOS's missing thread backend. |
| **cycc** | x86 **1,142,104 B** · seed 29,024 B → cybs → cycc byte-identical. (+16 B over 6.5.13 — the `_fn_local_addr` tail-call guard; a fn that never takes a local's address is byte-identical.) |
| **Bootstrap / cross-OS** | **ecb** (macOS-arm64) · **ach** (Intel-Mac x86-macho) · **cass** (Windows PE) · **pi** (aarch64) — all `SELFHOST_OK` + `crossos LIBTEST_OK` on REAL hardware at **6.5.14**. ⚠ The selector is now the **directory** `tests/tcyr/crossos/`, not the `vr01_` filename prefix. **ecb full corpus 23 → 11** (`CYRIUS_CROSS_OS_FULL=1`): −5 packaging, −7 concurrency. |
| **Gates** | `check.sh` **165 / 0** + **44** shell gate scripts under `tests/gates/<bucket>/`, ALL live, 0 orphans — **35** registered by exact path in `programs/checks/*.cyr`, **10** driven from `scripts/check.sh` (`heapmap.sh` is in both; `main.cyr` also reaches it as `_heapmap_gate()`). New at .14: `toolchain/distlib_bundle_selfcheck.sh` (5 axes; the old check compiled to /dev/null and had never run) + `crossos/tailcall_frame_escape.tcyr` (mutation-proven: 4 of 7 fail against the 6.5.13 binary). ⚠ **COUNT these, never increment** — a first pass here wrote 43 by adding one to .12's stale 42 and missing .13's `codegen/cx_indirect_call.sh`. ⚠ Re-derive with `find tests/gates -name '*.sh' \| wc -l`; earlier revisions read 10 → 16 → 19 because each release *incremented* the number instead of counting. |
| **Corpus** | **262** `.tcyr` in 15 buckets (**38** in `crossos/`) · **100** `lib/*.cyr` · api-surface **4830** (+2 at .12: `fs::is_symlink`, `fs::dir_list_into`) · heap **100** regions / 0 overlaps |
| **Active minor** | **v6.5.x** — generated-code quality (opened at v6.5.0; **v6.4.x closed at v6.4.86**) |
| **Shipped this minor** | **.0** file-scoped `public`/`private` · **.1** arity-aware overload dispatch + 4 stdlib folds · **.2** the `ir_const_fold` jump-span miscompile (**IR=3 mismatches 35 → 8**), `xrmdir`, yukti 2.3.0's six agnos ABI defects · **.3** diagnostic lines after `include`, `install.sh` ETXTBSY strand, `version-bump.sh` same-version early exit · **.4** `vec_sort_by`/`vec_select_nth` (the agnosai filing), sigil 3.12.2 / yukti 2.3.2 / sandhi 1.9.8 / mabda 4.0.8 folds, the vacuous sign-efi gate, fold-version table rot · **.5** the `CYRIUS_IR=3` switch-dispatch miscompile (filed as LASE, actually **DCE**) — closes `switch_dispatch`, one of the eight residuals .2 recorded, so **7 remain** — + bayan 1.4.0 · **.6** `sys_exit_group` on all 3 syscall peer families + `async_await_readable_ms`; macOS-x86's untranslated 231 → silent SIGSYS; `proj-tcyr`'s 8-bit exit truncation (256 failures scored PASS); `cyrius fuzz` blind to scaffolded harnesses; sandhi 1.9.9 + vani 1.1.3 folds · **.7** hisab's `alloc_reset` allocator-invalidation; the aarch64 `fchownat` private-alias band (≥1000, both real candidates owned); `#@incdir` file-relative includes with the CVE-16 escape closed at byte-0 + relative-only; `sys_chdir` called-but-undefined; `signal_default` + the `x*` family; subfolder callout for test/bench/fuzz; missing-output-dir bare FAIL · **.8** szal's `thread_join` lost-wakeup deadlock (P1); `thread_create_detached` (210 MB VA leak → 0); the `i64::MIN` formatter class across 12 sites incl. 2 in the compiler + sakshi 2.4.8; agnos `#97 chan_op` minted (`#96` fork deliberately not); `coverage`'s four fail-open/truncation defects; the doc/vet/deny/audit fail-open family; `distlib --all/--check`; the cross-OS gate batched + honest; bayan 1.4.1 fold · **.9** agnos `CH_ENDOW` + `sys_spawn_path_env` (both blocking agnos bite 7); the three-state futex mutex (392→48 ns, and W1 item 3's stated prerequisite was wrong); the growable arena + exhaustion policy — an exhausted arena was a SIGSEGV layers away, and adopting arenas made a leak into a crash · **.10** `distlib`'s `.deps` sidecar under-reported (8 of 12 for setu) and thereby DISABLED `cyrius deps`' consumer check — now declaration ∪ include-scan, base bundle only; `alloc_via` was two-thirds call plumbing (15.1 → 11 ns) · **.11** test suite → subfolders (`vr01_` prefix → the `crossos/` **directory**) with three silent-green readers closed first, incl. CI scoring a fake PASS because cycc on EMPTY stdin exits 0 with a runnable binary; `MAP_ANONYMOUS` was Linux's 32 on Darwin (`mmap_anon()` → 0 for every caller); **macOS threading now RUNS the worker** via a serial fallback — the wrong constant had been the accidental safety net for the missing backend, and fixing it turned the no-op into SIGSYS; `dir_list`/`is_dir` per-call 4 KB → stack (`dir_walk` 4443 → 239 B/entry) · **.12** sigil **3.12.4** (written this release — closes 3.12.3's residual by localising the bignum engine scratch; the banking existed for a cyrius quirk WE fixed at 6.3.15 and never propagated) — an RSA verify BYPASS (888/400,000 forged accepted upstream; partial by its own account, PSS still banked); `CYRIUS_STACK_ARRAYS=0` silently re-opened it (our flag revoking their fix) → now warns; `lib sync` dropped package DIRECTORIES (`unicode`) at exit 0 — the un-updated twin of a v6.1.x `cyrius deps` fix — plus fail-loud on any unsatisfied declaration; cx emitted a SELF-RESTARTING .cyx at exit 0 for an undefined call (1026 loops; native gated since v6.3.2, cx never got it); tree walks followed symlinks (**41 passed for a corpus of ONE**) → `is_symlink` + depth cap, symlinked FILES still run; `bench_string` 56-byte stack overflow SIGSEGV since v6.3.15, invisible behind the non-blocking bench step; `*.fcyr`/`*.bcyr` DIRECTORIES compiled as files; `dir_list_into` (0 B listing) · **.13** cx `callptr` (opcode 105 — three defects that had to land together: `ELOAD_FN_ADDR` a return-0 stub, `main_cx.cyr` never setting `_fixup_base`, `ECALLIND` a hard error) + agnos `#98 ptrscan` · **.14** the tail-call frame-escape P0 (`return f(p)` freed the frame `p` pointed into; the v5.8.16 guard was SYNTACTIC and missed every aliased form) + sigil **3.12.6** (the whole PSS workspace localised — PSS verify had the same authentication bypass 3.12.3 fixed for v1.5, and the compiler defect is why three releases could not close it) + `distlib`'s bundle self-check, which compiled to `/dev/null` and had never run |
| **In-flight** | Nothing mid-arc. **6.5.14 release gate GREEN, all 5 steps** — check.sh 165/0, seed-derive OK, `SELFHOST_OK` + `crossos LIBTEST_OK` on ecb/ach/cass/pi (real hardware), bench 678 ms. Push + tag are the maintainer's. sigil 3.12.6 is folded and 65/65. |
| **Next up** | W1 items **8, 9** (items 1, 2, 3, 4, 5, 6, 7 shipped — **item 5, `dir_list`, shipped at .11** and went WIDER than filed: `is_dir` was the dominant cost and the filing never named it). ⚠ **Item 7 shipped at .8** — the gate is batched and prints its own coverage, and `issues/archived/2026-07-14-release-gate-cross-os-runs-only-vr01-glob.md` is closed `✅ SHIPPED v6.5.8`; only the *flip the full corpus on by default* half is held back, deliberately, behind the open `2026-08-05-cross-os-full-corpus-23-failures-on-ecb` (`CYRIUS_CROSS_OS_FULL=1` runs it today). Then the IR substrate (Slot 3, `.17–.18`): **10** residual default-vs-IR=3 divergences (3 compile, 7 runtime) + the regalloc walls. |
| **Open queue** | **13** issues · **2** proposals · **301** archived. **+1 filed at .14**, with its reason NAMED: `2026-08-08-stdin-input-buf-1mb-cap-reached-by-sigil-bundle` — sigil's dist bundle (1,079,068 B) passed cycc's 1 MB stdin `input_buf`, and raising it relocates `tok_names` (nested in the same megabyte) ⇒ heap LAYOUT change ⇒ two-step bootstrap, which is one of the named file-don't-pack reasons. Consumers are NOT affected (`include` is resolved from disk, verified); `distlib`'s self-check now routes through a generated include entry, which is load-bearing until this is raised. **+2 filed at 6.5.11**, both maintainer DECISIONS rather than tasks: `2026-08-07-macos-arm64-inherits-linux-signal-and-errno-constants` (10 constants confirmed wrong on real ecb; `ESYSXLAT` renumbers syscalls, never VALUES — a Slot 11 prerequisite) and `2026-08-07-cyx-indirect-call-opcode-design-decision` (a PERMANENT `.cyx` opcode, pinned next release). Nothing archived at .11 — `macos-threading` was NARROWED (filed symptom fixed, real concurrency still Slot 11), not closed. **2 archived at .12** (301 total): `agora-fs-dir-list` (both halves shipped) and `net-cyr-x86-only` (closed WITHOUT its §4, which premise-checking on real pi disproved; §9's AF_UNIX moved to the roadmap backlog rather than vanishing with the archive). |
| **W1 status** | **WIDENED 5 → 13 patches (.4 → .16)**. **9 of 13 spent** (.4 item 4; .5 the cyrius-doom IR=3 bug; .6 the agnosai/sandhi pair; .7 item 1; .8 items 2 + 6 + 7; **.9 = item 3, the three-state mutex**; .10 the setu/agnosai pair — reactive consumer filings, no numbered item; **.11 = item 5** plus the suite reorg and three reactive repairs; **.12** the sigil security fold + the deferred-repair queue — reactive, no numbered item). ⭐ Item 3 shipped WITHOUT its roadmapped `atomic_swap` prerequisite, which vetting showed was never needed. Items 8, 9 remain, .13–.16 to run them in. |
| **Owed to the maintainer** | Two answered open questions in [roadmap.md](roadmap.md): the **self_compile budget** and **v6.5.x committed item 5** (growth-tax audit) both belong to the later performance track and are to be reviewed **together** when it opens. |

> Full arc detail, the 12-slot sequence and per-arc length estimates live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
