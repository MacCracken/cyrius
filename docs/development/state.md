# Cyrius — Current State

> Refreshed every release. CLAUDE.md is preferences/process/procedures (durable);
> this file is **state** (volatile). Bumped via `version-bump.sh` post-hook.
>
> **Consolidated 2026-06-08 (v6.1.4):** the per-patch session-close log
> (104 entries, ~5,600 lines back to v5.x) + the stale v6.0.4-frozen
> structured sections were pruned — that detail is canonical in
> [`CHANGELOG.md`](../../CHANGELOG.md) (per-patch) and
> [`completed-phases.md`](completed-phases.md) (arc retrospective). This file
> now holds only the **active cycle** + current state.

## Current state

| | |
|---|---|
| **Version** | **6.3.12** (v6.3.x cycle — **Language Refinements**; **W^X — `cyrld` emits separate code/data PT_LOAD segments** for every userland ELF. Was ONE `RWE` PT_LOAD (code writable + data executable — textbook W^X violation); now TWO page-aligned segments: text `R E` (ELF hdr + PHs + `.text`, NOT writable) + data `RW ` (`.data`+`.rodata`+`.bss`, NOT executable). On agnos (1.50.6 loader already `PF_X`-aware → ZERO kernel work) this is full segment-level W^X. **DEFAULT ON; `CYRIUS_WX=0` opts out** to single `RWE`. Mach-O/PE/kernel/`.so`/object paths unaffected (`_emit_fmt==0`+kmode 0 guard); x86 + aarch64 both (separate backends). **MECHANISM:** data vaddr rounded up to a fresh **2 MB page** (`_wx_data_vaddr`; agnos huge-page granularity), file offset to the ELF `p_align` (4 KB x86 / 64 KB aarch64) so `p_vaddr ≡ p_offset (mod p_align)` → ≤2 MB *virtual* gap, near-zero file bytes. The 2nd PH shifts `.text` 120→176, so `_entry_base`/`e_entry` move to match. **FIXUP ↔ EMITELF_USER in LOCKSTEP** via shared `_wx_data_vaddr(base+text_foff, cp)`: `dbase` feeds every var/string reloc, so patched code lands on the relocated data segment. **TWO-STEP BOOTSTRAP to flip default-on** (old single-seg cycc can't emit W^X → cc1 transition intermediate, cc2 stable W^X fixpoint; cc2==cc3). seed→cybs→gen2==build/cycc W^X (cybs emits single-seg gen1; gen2 W^X — `_wx_*` compile through cybs). **`wx_probe.cyr` + `_wx_segments_gate` (check.sh 107→108): reads emitted PHs — default e_phnum==2, PH0 flags==5 (R E), PH1==6 (R W), runs; CYRIUS_WX=0→1 PH flags==7 (RWX). Verified x86 + aarch64 + `CYRIUS_TARGET_AGNOS` (readelf -lW); ecb+cass(unaffected, Mach-O/PE)+pi(W^X, real aarch64) SELFHOST_OK; CYRIUS_WX=0 differential byte-identical vs 6.3.11; bench 538 ms; cycc 1,107,280→1,111,576 B (+4,296).** NEXT: v6.3.13 = str_builder concurrent-corruption root-cause (P1). See CHANGELOG [6.3.12].) PRIOR: **6.3.11** (v6.3.x cycle — **Language Refinements**; **async / await — first-class Futures over the cooperative epoll runtime** (the language trio's third member). `async fn` + `await` as sugar over `lib/async.cyr`, gated `_ASYNC_OK` (`CYRIUS_ASYNC=1`, default off → **cycc self-host byte-identical**; differential vs 6.3.10, x86 + aarch64). **MODEL: deferred-then-forced Futures, NOT stackless coroutines** (matches the run-to-completion runtime). `async fn f(p0..pn)` compiles to a **constructor** that allocs a heap Future `[ &f$impl, argc, args… ]` and returns it (body = hidden `f$impl`, reached only by pointer; the call does NOT run the body); `await fut` → `future_force(fut)` calls the bundled impl via `fncallN`. The Future reuses the CLOSURE-ENV heap construction (alloc + fn-addr fixup + field stores); `await` is a factor-level prefix; Futures spawnable (`async_spawn_future`) + forced by `async_run`. 0–6 params; needs `lib/alloc.cyr` + `lib/async.cyr`. Tokens 134/135 lexed UNCONDITIONALLY (no bare async/await ident in src/lib → byte-identical), feature gated in pass 2 via lazy `_ensure_async_gate`. Dispatch: shared `_IS_FN_KW` routes token 134→fn path in all 7 forks; pass-1 `_prescan_fn_sig` consumes the prefix. **LANDMINE (fixed): the pass-2 DCE name-read at `GTI+1` assumes cursor-at-`fn`** — the `async` prefix shifted it → mis-marked the async fn unreachable → stubbed it (`add` undefined). Fix: consume `async` INSIDE the fn-dispatch block (cursor→`fn`, DCE/stub logic intact) + arm `_pending_async` only on the reachable path (no leak to a DCE-stubbed async fn). **`async_await.cyr` + `_async_await_gate` (check.sh 106→107): 0/1/3-param + await-on-call + stored + spawn/run → 42 under flag, REJECTED off. Verified x86 + aarch64(qemu); self-host fixpoint + differential + seed→cybs→cycc byte-identical (`_async_emit_constructor` compiles through cybs); ecb+cass+pi SELFHOST_OK; bench 533 ms; cycc 1,101,944→1,107,280 B (+5,336).** DEFERRED (documented): true stackless coroutines (suspend/resume mid-body — needs poll-runtime rework), force-once Future memoization, async generic/struct-returning fns, **f32 scalar arithmetic** (native-float Tier A tail — f64+ops+NaN + f32 conversions already shipped .18/.19/.41; remaining f32-arith is consumer-less). NEXT: v6.3.12 = W^X `cyrld` separate code/data PT_LOAD (P1). See CHANGELOG [6.3.11].) PRIOR: **6.3.10** (v6.3.x cycle — **Language Refinements**; **non-i64 generics: instances + structs + explicit syntax** — second half of the generics arc; the full non-i64 surface on ONE **instantiate-once engine**, gated `_MONOMORPH_OK` (default off → cycc self-host byte-identical; differential vs 6.3.9). **Explicit `foo<i64>(x)`** via non-consuming `<`-disambiguation lookahead (`LOOKAHEAD_IS_TYPE_ARG_CALL`; gated + generic-fn-callee + `foo<…>(` shape → `a<b` unaffected). **Non-i64 fn instances** (`add<i32>`): mint `foo$ty` (`$` sep), `FINDFN`-dedup, on miss **RE-INVOKE `PARSE_FN_DEF`** on the base's verbatim tokens (`_fnt_defstart`, stored +1 — token 0 is valid!) with name (`_inst_name`) + concrete-binding (`_inst_conc0/1`) overrides → full fidelity (params/masks/struct-args/body) for free; jumped over (`EJMP0`/`EPATCH`) + enclosing-state save/restore + `_cur_fn_has_closure` regalloc-skip. **Generic structs**: base IS the i64 instance (`val:T`→untyped 8B field; `Pair<i64>` already worked); non-i64 re-invokes `PARSE_STRUCT_DEF` (registration-only, no codegen) + field `:T` `_tp_resolve` consult → `Box$ty` via `REGSTRUCT`+`FINDSTRUCT`-dedup, wired at the `var x: Box<conc>` type site. KEY: re-invoke the CANONICAL emitter with overrides, don't replicate it. **`generics_full.cyr` + `_generics_full_gate` (check.sh 105→106): `add<i32>`+`Pair<i32>`+`Holder<Point>` (struct-type-arg, tag read past a 16B Point field — proves specialized layout) → 42 under flag, REJECTED off. Verified x86 + aarch64(qemu); fn×struct mix; self-host + differential + seed→cybs→cycc byte-identical; ecb+cass+pi SELFHOST_OK; bench 523 ms; cycc 1,094,000→1,101,944 B (+7,944).** Filed pre-existing 1-field-struct segfault (P2, non-generic). Generics arc COMPLETE (.5 substrate→.9 i64 fns→.10 non-i64+structs). DEFERRED: nested `Outer<Inner<T>>`, distinct `Pair<T,U>`, control-flow in generic bodies. NEXT: v6.3.11 async/await + native-float Tier A. See CHANGELOG [6.3.10].) **Earlier releases (6.3.10 back to v5.x): see CHANGELOG.md** — trimmed from here (state.md keeps current + ~2 priors; the rest is canonical in CHANGELOG). |
