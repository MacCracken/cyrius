# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.4.44** — **async arc 5b "W" step R2: Windows IOCP timers + subprocess + combinator parity.** `async_with_timeout`/`async_interval` (a waitable-timer→threadpool→IOCP bridge) + `async_spawn_process`/`async_run_process` (`CreateProcessW` + threadpool-wait reap via `RegisterWaitForSingleObject`→`PostQueuedCompletionStatus`→`GetExitCodeProcess`, kill `TerminateProcess`) + verified `async_join_all`/`async_select` parity. First **permanent Windows async gate** (`tests/win/async_iocp_pe.cyr`, run on cass EVERY release). The subprocess work + that gate uncovered + fixed **two pre-existing Win64 ABI bugs**: (1) a **PE reroute stack-misalignment** — 8 kernel32 reroutes used a fixed `sub rsp` that only 16-aligned on the right parity, so `CloseHandle` faulted `0xC0000005` in the deep async resume (→ rbx-anchored dynamic 16-align); (2) a **retptr-shifted deep-stack-param homing off-by-one** — a value-form-SIMD/struct return with >6 params homed its deep stack args one slot too deep (`f32v8_make` lanes 5-7 garbage on cass since v6.4.31; the prologue post-loop was missing the in-loop's `_pp_shift`). Also: agnos `sys_readdir` (#81); re-vendored **sankoch 2.5.1** (zstd decode + tar) / **patra 1.12.9** (off-Linux `.patra` opens) / **vani 1.0.0**. Per-release detail → [CHANGELOG](../../CHANGELOG.md). |
| **cycc** | x86 **1,086,840 B** (+344 vs v6.4.43 — the R2 timer/subprocess reroutes + the `_pl_shift` prologue fix, net of the reroute-align refactor SHRINKING the emitter ~4 KB by routing 8 inline reroutes through shared helpers) · self-host fixpoint byte-identical · seed-derive byte-identical · check.sh **141** · self_compile 615 ms |
| **Bootstrap / cross-OS** | seed (29 KB asm) → cybs → cycc byte-identical · ecb (macOS/arm64) + cass (Windows/PE) + pi (aarch64) `SELFHOST_OK` + **VR-01 LIBTEST_OK on all three** (release-gate GREEN) · **the full Windows IOCP async surface runs end-to-end on real cass** — R1 client (resolve→connect→send→recv) + R2 timers/interval/subprocess/`join_all`/`select`, all folding to RC 42 in `tests/win/async_iocp_pe.cyr` every release · `f32v8_make` all-8-lanes correct on cass (256-bit value-form return ABI) · the aarch64 epoll reactor works on pi (v6.4.42) · cx portable-`.cyx` SIMD round-trip on each host's native cxvm |
| **Active minor** | **v6.4.x** — ABI / Language-Features (opened at v6.4.0; v6.3.x closed at v6.3.45) |
| **In-flight arc** | **async arc 5b — the IOCP-Windows "W" step, 3 releases; R1 SHIPPED (.43), R2 SHIPPED (.44).** Foundation + the 5 tokio-parity gaps (.33–.41) + consolidation (.42) DONE. **W-step R1 (.43)** = the Windows IOCP async CLIENT (reactor + 13 ws2_32/IOCP reroutes + connect/recv/send + DNS + callptr `>4` fix). **W-step R2 (.44)** = timers (`async_with_timeout`/`async_interval` via a threadpool→IOCP bridge) + subprocess (`async_spawn_process`/`async_run_process`) + `join_all`/`select` parity, all on real cass; PLUS the first permanent Windows async gate and two pre-existing Win64 ABI fixes (PE-reroute 16-align, retptr deep-stack-param homing). **R3** = overlapped `AcceptEx` server (+`GetAcceptExSockaddrs`). Arc is FOUNDATION-FIRST; cx SIMD (.32) + Win64 value-form SIMD (.31) COMPLETE. |
| **Next up** | **async arc 5b W-step R3** — the overlapped `AcceptEx` server (+`GetAcceptExSockaddrs`) so sandhi/daimon can `accept()` on Windows: `WSASocketW` listener → `bind`/`listen` (both need PE reroutes) → posted `AcceptEx` with the accept socket + address buffer → completion parks/wakes like connect. **This closes the async "W" arc — it is one piece of a LARGE, ongoing v6.4.x**, not a minor close: scalar-float completion, diagnostics, function visibility (`pub`/`private`), UEFI Secure-Boot signing, the Intel-Mac x86_64 Mach-O tail, and reactive agnos/consumer repairs all remain in 6.4.x (large minors run ~45–99 releases; we are at .44 with plenty ahead). `tantu` extraction is a **reserved future-minor** deliverable (repo name held) — NOT next, do not sequence it. Filed follow-ons: single-waiter-per-fd multiplex; mid-body suspend substrate = FUTURE ARC (v6.5.x IR substrate + no consumer); `_cur_fn_ret_stash` disp↔idx substrate. thoth `--win` end-to-end = post-release downstream re-vendor (sandhi re-vendors `async` from .44). |
| **Committed after** | **async runtime arc 5b (F1 ✓ .33; F2 ✓ .34; primitives next) → UEFI Secure Boot signing → function visibility (`pub`/`private`) → scalar-float completion → diagnostics → Intel-Mac (x86_64 Mach-O) tail.** **v6.5.x = perf-quality** (IR/regalloc substrate + passes; SIMD register-residency pulls into 6.4.x post-NEON) · **v6.6.x = language ergonomics** · **RISC-V → v6.7/v6.8**. Full slot table + async prose in [roadmap.md](roadmap.md). |

> Full arc detail, per-arc length estimates, and the Phase-5 5a/5b plan live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
