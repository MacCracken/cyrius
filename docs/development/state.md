# Cyrius — Current State

> Refreshed every release. This file is **state** (volatile) — a SNAPSHOT of where the
> project is right now, bumped via `version-bump.sh`. It deliberately holds **no
> per-release narrative** (canonical in [`CHANGELOG.md`](../../CHANGELOG.md)) and **no
> backlog** (the full pin sequence + length map is in [`roadmap.md`](roadmap.md); parked /
> v7+ items in [`roadmap-future.md`](roadmap-future.md)). CLAUDE.md holds the durable
> preferences / process / procedures.

## Current state

| | |
|---|---|
| **Version** | **6.6.5 — bumped 2026-09-19, tag PENDING** (the bump commit). The repair release for the nine issues open at 2026-09-17, one bite each: ① SIMD register picker · ② call-site rsp alignment + the PE / UEFI / x86-Mach-O entry bases · ③ pass-1 visibility authority (+ the method call path's missing callee masks) · ④ aggregate storage class — the inline-vs-pointer layout was GUESSED from the neighbouring slot's name, silently wrong since 5.8.17 · ⑤ 14 unnamed syscalls named + routed on aarch64 (SYS_UNLINKAT 35 → 263) · ⑥ bench floor + Windows QueryPerformanceCounter · ⑦ dep-cache verification (`CVE-43`) · ⑧ one CLI argument rule (+ the first working Windows tool spawn) · ⑨ cyrlint cross-line. Release gate GREEN on ecb/ach/cass/pi, check.sh clean, seed-derive, bench 771 ms. ⚠ **After the user tags: `sh scripts/install.sh --refresh-only` once at the tagged commit.** **Prior: 6.6.4 — SHIPPED** (tag `6.6.4` @ `84130a7a`). |
| **cycc** | x86 **1,310,864 B** in the 6.6.6 tree (bite 14's struct-result paths; closeout restamps) · **6.6.5** shipped **1,294,040 B** (`.text` 1,127,672; 6.6.4 shipped at 1,251,944, so **+42,096**: the call-site alignment padding (~+3.3 KB of `push`/`pop` pads alone), bite 3's pass-1 prescan machinery (+8.2 KB), bite 4's recorded aggregate layout and frame-object literals (+11 KB), bite 5's syscall rows and bite 6's two PE reroutes. Per-bite figures are in CHANGELOG [6.6.5]). **seed-derive GREEN after every compiler bite** — `seed → cybs → cycc` byte-identical. `build/cycc-native-aarch64` regenerated in bite 5 (it had been a 2026-07-02 build) and verified on real pi. Last tagged: **v6.6.4, 1,251,944 B**. |
| **Bootstrap / cross-OS** | **v6.6.4: ecb · ach · cass · pi all `SELFHOST_OK` + `LIBTEST_OK` (74/74 crossos) on REAL hardware** — and the pi leg now also BUILDS AND RUNS THE CLI (`cyrius run` under a throwaway home, zero raw-syscall warnings), because the compiler self-hosted there at every gate while the CLI's `cyrius run/test` had been dead on native aarch64 for 59 releases. v6.6.3 was the same four hosts, run ONE AT A TIME — fixed /tmp + remote paths clobber under concurrency. ⭐ `pi` is the load-bearing host this release: 6.6.3 changes `src/backend/aarch64/fixup.cyr`'s data-segment vaddr, and compiling the aarch64 fork locally proves it PARSES, not that cycc reproduces itself on ARM. seed **29,024 B** → cybs → cycc byte-identical. A green CI check is NOT this leg. ⭐ **v6.6.3 — the pi leg now ALSO self-hosts `src/main_aarch64_native.cyr`, the fork ARM users actually install.** It had only ever self-hosted `main_aarch64.cyr`, the CROSS source — the same shape as the macOS rot this leg exists to prevent, and the reason the full release gate passed GREEN while `#inline`-before-a-declaration failed to compile on the native ARM fork. Mutation-proven load-bearing: pointing the second compile back at the cross source makes pi's SELFHOST fail. ⭐ **6.6.4 (bite ④): the install store is verified against its TAGS** — `sh scripts/verify-store.sh` reports every tagged slot; the maintainer's `~/.cyrius` was restored (6.6.0–6.6.3 OK, stamped) after it was found stale in both directions. `check.sh` now stages a throwaway `CYRIUS_HOME` from the tree. |
| **Gates** | **v6.6.5 in flight — check.sh GREEN + 172 shell gates** (`find tests/gates -name '*.sh' | wc -l`; bite 5 added `platform/esysxlat_row_order.sh` (the ESYSXLAT chain has no early exit, so it fails when any later row's SOURCE is an earlier row's PRODUCT — a property, not a row list; **58 rows decoded**, ledger of **4**, and swapping the sendmsg/ftruncate pair also fails **12** assertions in the crossos test, measured under qemu, not the 6 the first cut guessed) and `platform/syscall_peer_kernel_agreement.sh` (**4 peers against the committed kernel tables**, 385 x86_64 + 329 aarch64 facts, per-peer declaration floors 100/90/100/15 against 109/99/111/18, ledger of **5**); review round 2 gave `platform/aarch64_syscall_shadow.sh` a third axis and an anti-vacuity floor — axis 2 read `PASS … 0 declarations over 0 files` and exited 0 over an EMPTY tree walk, and its `SYS_*` regex could see neither consumer shape this release names (`GWL_NR_FTRUNCATE = 46`, `n = 83;`); ledger now **9**, DERIVE it with `grep -cE '^#   [0-9]+\.'`; bite 4 added `codegen/fn_local_storage_class.sh` (**25 rows / 11 axes, nine mutants** — DERIVE both: the gate's own `PASS … N of N rows` line and `grep -cE '^#   [0-9]+\.' ` on its MUTATION LEDGER; this cell said 20/9/ten while the ledger and the CHANGELOG said seven, which is the drift the derivation rule exists for. The row count is itself DERIVED inside the gate from a static grep of its own `run_case` sites and must AGREE with the counter the rows increment, after review round 1 found a hand-tuned floor of 14 against 18 rows; two files, a `private` file, rename-invariance, an env var, an include-free source, the over-budget warning's own line number, a captured-aggregate operator dispatch, and the qemu/cxvm legs labelled as emulators) and `codegen/hidden_temp_census.sh` (5 axes: its axis 3 now attributes the 11 `_HTEMP` call sites PER LOWERING — its floor of 9 left the mutant its own ledger named GREEN — and review round 2 added axis 5, which attributes all 13 `_varn_base` GLOBAL registrations to their enclosing fn, because axes 1/2 only pinned name-0 and the defect this bite repaired was a global registered with a REAL name inside a fn; round 2 also made axes 1/2 whitespace-tolerant after `S64(_varn_base + zz*8, 0);` walked straight past them), an axis 9 on `frontend/coroutine_midbody_suspend.sh` for `&struct-local` across a suspend — typed AND literal, the literal rows added in round 2 after a struct literal in a suspending `async fn` was found reading 5 for 56 — plus three new `.tcyr` (`crossos/aggregate_storage_class`, `crossos/hidden_temp_reentrancy`, `concurrency/struct_literal_threads`) and an aggregate-capture group in `lang/closures_capture`; bite 3 added `frontend/private_forward_reference.sh` — a static 7-fork parity axis plus a forward-vs-backward refusal DIFFERENTIAL, so the expected value comes from a different compiler path rather than a list in the gate — and extended `frontend/visibility_private.sh` (17 forward-order rows), `frontend/public_marker_scoped_to_its_item.sh` (3) and `frontend/private_per_item_rejected.sh` (4 — axes 3b/4/5/6, derived from `git diff HEAD -- ` that file, not counted by hand); 17 mutants built and run (`grep -cE '^#   M[0-9]+'` on the gate header), two of which disproved a premise-check prediction and two of which — M13/M14 — were only written after review round 1 found the method-arg loop still missing two of PARSE_FNCALL's three callee masks; review round 2 added TWO more gates, `frontend/method_call_runs_every_callee_gate.sh` (11 rows — `grep -cE '^(vrow|drow) [a-z_]+ '` — 8 of them red on the 6.6.4 compiler — a DIFFERENTIAL against the identical free fn rather than a list, because three consecutive rounds each found one more callee gate the method path was missing) and `codegen/tail_call_literal_divert_depth.sh` (6 rows; it PINS ITS OWN `ulimit -s` so the TCO verdict is the gate's and not the box's — the fix's first cut armed the `: Str` divert at any paren depth and turned a correct bounded recursion into a SIGSEGV), with 8 further mutants N1–N5 / D1–D3 built as full cycc binaries, each reddening exactly its own rows; ⚠ a mutant harness that cd'd back to the repo produced byte-identical 'mutants' that all passed — `include "src/..."` resolves against CWD — so the harness now ABORTS when the built compiler compares equal to `build/cycc`; bite 2 added `codegen/call_site_stack_alignment.sh`, a part (C) to `platform/ffi_stack_protected_extern_c.sh`, and TWO in-program gates: `_efi_entry_alignment_gate` — the UEFI entry-rsp park / landing seed / trampoline frame / the restore before all three compiler-emitted firmware `ret`s, asserted on emitted bytes because OVMF tolerates a misaligned CALL and only the RETURN faults — and `_efi_ovmf_fn_exit_gate`, a second OVMF boot of `programs/efi_fn_exit_probe.cyr`, which exits from INSIDE a fn body the way `lib/alloc.cyr` and `lib/bounds.cyr` abort; `programs/efi_probe.cyr` returns through its own hand-written `ret`, so the old smoke had never tested how the COMPILER ends a UEFI image, and the first cut of bite 2 shipped that route broken under every existing gate). **v6.6.4 — check.sh 246 / 0 GREEN + 164 shell gates; release-gate.sh GREEN.** **Thirteen** new gates this release (seven shell + six `.tcyr`, derived from `git diff --name-status 6.6.3 HEAD -- tests/`), every one mutation-proven: `string_token_decoders`, `string_literal_64k` (⚠ the odd-length group needed a 2-byte pad literal to bite), `cx_addr_past_64k` (four axes; the lexer INTERNS identical fillers), `cx_pow2_mul`, `public_marker_scoped_to_its_item`, `derive_enum_inside_ifdef`, `method_self_inline_struct` (crossos), `released_slot_written_from_tag` (17 axes), `deps_relock_refused` (14 axes), `raw_syscall_literals_routed` (allowlist DERIVED from ESYSXLAT's own `cmp x8,#N` words; lex_pp-exact `#ifdef ` spellings; 13 adversarial guard shapes), and three crossos tests `syscall_stat_x86_compat` / `open_flags_per_target` (exact `-ELOOP`/`-ENOTDIR` per target) / `fdlopen_dynlib_decline` (a 1,176-byte aarch64 ET_DYN fixture — the one assertion that separates a decline from the 6.6.3 accident). Also `syscall_xlat_generated` axes 3b/7/8 and `io_rdwr_agnos` axis 5 (O_NOFOLLOW/O_EXCL refused under mirshi ≥ 1.11.2). ⭐ Two of this release's review findings were gates that would have read green over the defect (a decode with Rn unchecked; guard spellings looser than the preprocessor's) — **a check that shares a defect with the thing it checks reads GREEN**. |
| **Corpus** | **322** `.tcyr` (**81** in `crossos/` — 6.6.5 bite 6 added `crossos/win_qpc_clock.tcyr`, the companion the syscall-wrapper rule requires for the two new PE reroutes 0xF038/0xF039 (QueryPerformanceCounter/Frequency): the QPC-derived interval is checked against the SAME interval measured by GetTickCount64, a DIFFERENT clock, and the portable half (monotonic, sub-ms tick) runs on every host — 6/6 on Linux, 10/10 as PE under wine, mutation-proven three ways; bite 6 also rewrote `crossos/bench_timer_floor.tcyr` around a SCRIPTED CLOCK (**77** assertions; DERIVE from the binary's own `N passed` line — review round 2 added the stopped-clock group: a dead clock is UNMEASURABLE, not sub-floor, and `bench_clock_recheck` must RECOVER from the tick sentinel, which it could not, the recovery arm being unreachable) after its axis 2 was found to have measured the mabda-filed `min=0ns` on pi, written it up, and concluded 'That 0 is not a defect'; 6.6.5 bite 3 added `forward_ref_abi_binding`, **28** assertions covering forward-call ABI, split private bindings, pointer-mode vs inline struct args, all four callee masks on the method path and the Win64 hidden-retptr vector receive (DERIVE it: `grep -c '^assert_eq' tests/tcyr/crossos/forward_ref_abi_binding.tcyr`, or read the binary's own `28 passed` line — this cell said 18 for two review rounds after the test grew); bite 5 added `crossos/syscall_shm_fd_passing`, **59 assertions** / 52 reported groups on Linux-x86 and qemu-aarch64 and 10 as PE under wine (DERIVE: `grep -cE '^\s+assert_' tests/tcyr/crossos/syscall_shm_fd_passing.tcyr`, or read the binary's own `N passed` line) — ⚠ review round 2 moved the raw 83/84/87/89/33 group OUT of its `#ifdef CYRIUS_TARGET_LINUX` guard, which is what makes the five new arm64-macOS parity rows run on ecb/ach at all; bite 4 added `aggregate_storage_class` + `hidden_temp_reentrancy` (crossos) and `concurrency/struct_literal_threads`; bite 2 added `call_site_stack_alignment` + `nested_call_value_regressions`; both counts DERIVED, `find tests/tcyr -name '*.tcyr' | wc -l` — ⚠ **241 (75%) never run on non-x86 hardware**: the cross-OS leg's selector is that directory, so anything outside it is x86-only until CI's full-corpus aarch64 job sees it — the cross-OS selector is that DIRECTORY, the `vr01_` prefix was retired at v6.5.11) · **103** `lib/*.cyr` (`boxed.cyr` restored the boxed tagged-union primitives) · **84** `programs/*.cyr` · api-surface **5,187** (DERIVED, `wc -l < docs/api-surface.snapshot`) · heap map **102** regions (gate-derived; ⚠ a raw `grep -cE '^#   0x' src/main.cyr` says **145** because it also matches 18 `FREED` markers — run `sh tests/gates/memory/heapmap.sh`). |
| **Active minor** | **v6.6.x** — value-form `Result` + language ergonomics. Opened at v6.6.0; **v6.5.x closed at v6.5.73** (there is no `.74` — that number was cut in error and re-cut as 6.6.0). Slot plan in [roadmap.md](roadmap.md): `.2`–`.6` are a reserved **repair window**, then the proposal queue, then the committed ergonomics list. |
| **Shipped this minor** | **.0** the `Result` / `Option` / `Either` **value-form flip** — construction allocates ZERO bytes (was 1600 B per 100 `sock_send`), shipped WITH the ecosystem (8 sibling stdlibs migrated at source, pin-bumped, released and re-vendored); it changed the ARITY of every value, so `payload()` and `tagged_new()` are DELETED; plus top-level `var t, v = f();`, the mixed-return diagnostic, and ⛔ **the v6.5.57 aggregate-copy P0 found at the cut** (live seventeen releases, invisible to the fixpoint because cycc's own source never uses the shape) · **.1** the two filed issues — `f64_exp`/`f64_exp2` **NaN for ±inf** in BOTH implementations (polyfill + emitted x87; the guard must precede the range reduction, which computes `inf - inf`) and AGNOS `clock_now_ns()` reading the **frozen `timer_ticks` clock** (#40 → #95 `rdtsc`) — plus the `cyriusly install` **ETXTBSY** failure (`cp` writes through the directory entry; now temp + atomic rename) and five stdlib folds. · **.2** the repair release — `tagged_new()` restored for a live consumer the deletion survey could not see; two SIMD P0s (a destination read from a frame slot nothing wrote; a top-level intrinsic that SIGSEGV'd); `map_u64` answering untruthfully for its own sentinel keys on five paths; `object;` builds exporting libc-reserved names; a vacuous SIMD gate; `cyrius build` overwriting the running compiler; `check.sh` reporting green over seven gates that never ran · **.3** the six issues the 6.6.2 ecosystem sweep filed, plus the fork-parity and Windows-checkout CI repairs · **.4** the six issues filed after the 6.6.3 handoff (see *Version*) — the 64 KB string-literal pack, `&_private_fn` across files, the `public` marker leak, the writable released install slot, the silent stdlib re-lock, and the raw x86 syscall numbers that had killed `cyrius run/test` on native aarch64 for 59 releases. ⚠ Closed-minor detail for v6.5.x and earlier lives in `completed-phases.md` + the CHANGELOG; this row carries the CURRENT minor only, and was the v6.5.x list until the 6.6.1 refresh. |
| **In-flight** | **6.6.5 closing** — nine bites committed (`c7ff99fe` … `2152df67`), one per open issue; release gate + bench, then `version-bump.sh 6.6.5`. ⚠ **After the user tags: `sh scripts/install.sh --refresh-only` once at the tagged commit** (the bump's stamp names the pre-bump HEAD). ⛔ Before any sibling pins 6.6.5, thoth / kybernet / attn11 must switch the per-arch syscall literals 6.6.5 renumbers — each repo's roadmap carries the note. |
| **Next up** | **6.6.6 — the queue carried out of 6.6.5** (user, 2026-09-19): six filed issues (SIMD arg + ≥6 int args, global redeclaration reads 0, top-level block closure, directives inside multi-line strings, attribute-prefix comments, cyrius-init on Windows) plus six roadmap items (`--version` in four forks, the `cycc-native-aarch64` lockstep gate, remaining PE `sys_fork` sites + leaked temp dirs, the mutex timing bar, a `statfs` row for yukti, and a gate that can truncate a tracked file when `/tmp` is full). Order and process in [roadmap.md](roadmap.md) *v6.6.6*. ⚠ Slot `.3` (per-item `private`) was listed here as next until 6.6.5 — it SHIPPED at v6.5.56. |
| **Open queue** | **6 open issues**, all the 6.6.6 queue above. All nine issues open at the 6.6.5 open are archived with *Corrections to this filing* sections — every one widened under investigation (e.g. the fn-local-slot filing became the aggregate layout guess, silently wrong since 5.8.17; the lint-wrapper filing became ~60 CLI spellings incl. several that deleted or rewrote files; the deps filing became `CVE-43`). |
| **Ecosystem** | **The 6.6.2/6.6.3 sweep CLOSED 2026-09-12.** Derived from committed HEADs: **125** repos with a `cyrius.cyml` — **14** on 6.6.3, **110** on 6.6.2, and **1** below (agnos 6.6.1, another agent's). hisab is also another agent's and is **not** to be updated. dhvani **2.2.4** holds the sound stack's versions, with hisab kept at **2.11.2** (naad 2.2.4 and svara 3.5.5 were rebuilt and pass against it); jalwa **1.4.5** aligns its shared pins to dhvani. **Still open, recorded but not fixed:** same-arity duplicate fns (agnostic 22, mehman 19, hadara 2, aegis 1, kriya 1 — warnings only); ~30 `path = "../sibling"` + stale-`tag` pairs (deliberately not done). ⚠ **Stale docs:** `ecosystem-migration-6.6.2.md` still reads "~11% DONE, DO NOT ARCHIVE", and its outlier #4 is superseded by the (now archived, FIXED v6.6.4) raw-syscall issue; vidya's `types.cyml` structural facts are still stamped cycc 6.6.1 / 1,247,608 B. |
| **W1 status** | n/a — reactive-window bookkeeping belonged to the v6.5.x minor and is retired with it. v6.6.x reserves `.2`–`.6` as its repair window instead; see [roadmap.md](roadmap.md). |
| **Open decisions for the user** | **None blocking.** The three standing items are recorded in [roadmap.md](roadmap.md) *Open questions* WITH stated defaults, so work starts under them rather than waiting: the self_compile budget (owned by the later perf track — ⚠ note both halves of the old candidate pair, ≤700 ms and ≤1.20 MB, are now exceeded at 743 ms / 1,251,864 B (6.6.3), so it is an input to re-decide, not a target that was missed), the growth-tax audit (review with it), and macOS concurrency ordering (default: keep last; pull forward if a consumer appears). **Waiting on the user's git calls (sibling repos, not blocking cyrius):** commit + tag **patra 1.14.3**, **sigil 3.12.18** and **mirshi 1.11.2** (patra/sigil are pinned to 6.6.4, so tag them AFTER the cyrius 6.6.4 tag; mirshi stays on its 6.6.2 pin); commit the CHANGELOG placement fixes in bayan, mabda, sandhi, sigil, vani and yantra (each release shipped with its own entry buried); commit or discard the blank-line diffs in ganita, niyama and sankoch; discard the stale `cyrius.lock` edits in mabda, sigil and sankhya (their content differs from what shipped green); review samvada's 11 uncommitted doc files (dated 09-10, origin unconfirmed); commit the two new issue files (cyrius, kavach). |

> Full arc detail, the 12-slot sequence and per-arc length estimates live in
> [roadmap.md](roadmap.md). Reactive agnos + consumer-filed repairs interleave as
> separate (uncounted) slots.
