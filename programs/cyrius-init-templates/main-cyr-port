# {PROJ} — Cyrius port
# Ported from {RUST_LOC} lines of Rust

# Stdlib auto-included via cyrius.cyml

fn main() {
    alloc_init();
    println("{PROJ} ready");
    return 0;
}

var r = main();
syscall(SYS_EXIT, r);
