# {PROJ} — Current State

> Refreshed every release. CLAUDE.md is preferences/process/procedures
> (durable); this file is **state** (volatile).

## Version

**0.1.0** — ported from {SRC_LANG} ({DATE}) via `cyrius port`. {SRC_LOC} lines of {SRC_LANG} preserved at `{OLD_DIR}/` for parity reference.

## Toolchain

- **Cyrius pin**: `{CYRIUS_VER}` (in `cyrius.cyml [package].cyrius`)

## Source

- {SRC_LANG} reference: {SRC_LOC} lines at `{OLD_DIR}/` (frozen, do not edit).
- Cyrius port: scaffold only — `src/main.cyr` stub.

## Tests

_Replace with parity test status once tests land._

## Dependencies

Direct (declared in `cyrius.cyml`):

- stdlib — string, fmt, alloc, vec, str, syscalls, io, args, assert

## Consumers

_None yet._

## Next

See [`roadmap.md`](roadmap.md). The first milestone is typically {SRC_LANG}→Cyrius surface parity for the {SRC_LOC}-line subset.
